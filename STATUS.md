# STATUS — SMB1 TAS project

Updated: 2026-08-22 (session 8 — **P0.10 done**: Hetzner chosen + authenticated (`hcloud` 1.67.0, context `smb1-tas`), billing rules and sizing recorded (F92/F93; **powered-off servers still bill — delete, don't poweroff**; $/core-hour flat ⇒ the $300 cap is ~9,000 core-hours; the engine's serial merge caps useful cores at ~48). Account quota caps us at **8 dedicated cores** (~1.08× the laptop) and a limits increase is refused as the account is too new ⇒ run 4 stays on the laptop, near-term cloud value is horizontal, and the shared tiers (4–10× cheaper per core) get a sub-$1 benchmark first (P0.10a); session 7 — **P2.3c-2 (warp zone) done**: staged + coupled bound 461 at the WR's entry (x-only 329, WR 476), 0 violations, the WR's return proven optimal from its drop state (F89–F91, H35); a proof run still needs the cloud + an end-game (x,y) bound; **P2.3c-1 done** (`bfscx` generic external-memory engine + path reconstruction + core replay tool; F88: 3 frames on the WR's last 12 frames of 4-2, framerule-absorbed); **P2.3b-2 done**: block states (bumped bricks / question blocks / VRAM-buffer restore timing / parser schedule) in the model, exact on the WR and 340 trials (F84–F87); an uncapped `bfsc` control was OOM-killed at 20:09 together with the session — every search, even a control, goes under `systemd-run -p MemoryMax`; session 6 — P2.3c measured and specified (F82/F83); P2.3b done: lift modeled, 1-frame pipe gain verified on the core (F79), bottom route = wall walk (F80); P2.3a step 3 done: 4-2 in the model, three model bugs fixed, warp zone exact, lift confirmed as a WR mechanic; session 5 — run 3 of the whole-room search was OOM-killed after layer 85 (328M states; the kernel also took the Claude Code session down); engine rewritten as external-memory (parents streamed from layer files, bounded accumulators spilled to sorted runs, k-way merge; FxHash; `--resume`); validated exactly against run 3's counts, 1.8× faster; run 4 resumed from layer 85 under a 10 GB cgroup cap)
Phase: **P0/P1 → P2** — ground truth mostly done (P0.7, P0.8 remain), stage-1 fast core done
Record to beat: **17,868 frames** (HappyLee, TASVideos #1715M, 4:57.31) — last input on frame 17848 (0-based), then a 19-frame coast to the axe. A new movie must finish the game with an earlier last input.
ROM: verified byte-identical to TASVideos' (W) [!] (`tools/verify_rom.py`); classic-header copy in `roms/` on the Linux box (gitignored) — re-verified 2026-08-21.
Our best full movie: none yet
Host: Linux box (primary). Emulators: FCEUX/BizHawk in the rootless toolbox container `smb1`, Mesen2 native — run via `tools/{fceux,mesen2,bizhawk}_run.sh`, rebuild with `tools/toolbox_setup.sh` (see `docs/experiments/P0.1-tooling.md`). Git: private GitHub remote — commit after every unit, then push (document → STATUS → commit → push)

## Running jobs
- **Rule (2026-08-22 20:09 OOM):** never start a search without a cgroup cap, not even a short control — an uncapped `bfsc W42Main … 540 587 --check-path 47` (in-memory frontier) was OOM-killed and took the Claude session down; use `systemd-run --user --scope -p MemoryMax=2G -p MemorySwapMax=0 --quiet /abs/path/smb-opt …` (absolute paths) and keep the horizon short.
- Memory note 2026-08-22 18:50: run 4's cap lowered live to 8G (`systemctl --user set-property run-p172115-i172116.scope MemoryMax=8G`; anon ~3.2 GB, the rest is page cache) after the desktop showed "memory nearly full" — the OOM kills in the journal are the capped P2.3 test searches only. Do not run other capped jobs alongside run 4.
- **P2.1b-m3 whole-room run 4** (Linux box, `pgrep -x smb-opt`; started 2026-08-22 08:49 via `nohup systemd-run --user --scope -p MemoryMax=10G -p MemorySwapMax=512M --quiet sh runs/P2.1b-model/room_run_resume.sh 85 &` — copy in `tools/room_run_resume.sh`; an overrun kills only the search): external-memory engine `smb-opt bfs11cr … 1048 0 238 --goombas --threads 12 --layer-dir runs/P2.1b-model/room_layers --resume 85 --acc-mb 256` — relaxed model + goombas from the fixed entry, deadline 238 = one step before the WR's grab (the H28 test, proof direction), resumed from run 3's `layer_085.bin` (328,231,873 states after frame 1130). **Check first:** `tail runs/P2.1b-model/room_compact_d238_r85.log`. Outcomes: ends with `grabs (…)` + `earliest FPG grab` + a ready-made `reconstruct with: smb-opt bfs11c-path …` line (a candidate! run it with `--goombas --relaxed --out FILE` after `xz -dc` of any compressed layer it needs, then replay the WR prefix (records < 1048) + FILE on the core with `build/harness`/`tools/model_difftest.py` machinery; the relaxed model's inputs may need B held / A released to be real-game valid, see the relaxation argument in the experiment file); or `no grab found` (H28 refuted in the relaxed model ⇒ no 1-frame gain in the third room → record the proof in hypotheses.md, move P2.1b to Done); or it died (`.time` file: `Exit status`/signal; a cgroup kill = 137) → resume again from the newest raw `layer_NNN.bin` with a smaller `--acc-mb` or fewer threads. Progress 2026-08-22 23:40: see the log tail (frontier flat at ~700M since layer 133, ~1070 s/layer → ETA ~2026-08-24 early morning). Earlier: layer 125 (frame 1170), 603M unique, ~900 s/layer. Measured: layer 86 = 337.5M unique (×1.028), 495 s (467 s expand + 28 s merge of 24 spilled runs) vs run 3's 741 s; cgroup peak 6.6 GB incl. page cache (anon ~3.2 GB). Expected: ~152 more layers × ~500–550 s ≈ 21–23 h from 08:49 → done around 2026-08-23 08:00 if the frontier stays ~flat (it was growing ×1.02–1.03/layer). Memory: cgroup `memory.current` ≈ 3–5 GB (`cat /sys/fs/cgroup/user.slice/user-1000.slice/user@1000.service/app.slice/run-p172115-i172116.scope/memory.current`). Disk: each layer adds a ~5.5 GB raw file (`runs/P2.1b-model/room_compress.sh`, also running — `pgrep -f room_compress` — xz-compresses all but the newest 2 to ~200 MB) plus ~7 GB of transient run files in `room_layers/tmp/`; 200 GB free at launch. Layer files 0–85 are run 3's (identical format).

## In progress
- **P2.3c-2c** — 4-2 main-area bound + top-route reference. Started 2026-08-22 (session 8, alongside run 4; CPU-light: 2-thread capped searches only). Goal: (1) measure where the x-only `W42Main` bound (root 540 vs WR 588) is loose, (2) build a top-route reference path in the model by chaining position-goal segment searches (`bfscx --goal-x PX [--goal-y PY]`, new; `tools/bfscx_ladder.sh`, `tools/chain_inputs.py`), (3) replay it on the core (`tools/replay_check.py`) to see what the enemies do to it, (4) derive the staged main-area bound from the reference's slack profile. Acceptance: a reference path with its model length vs HappyLee's 577 (F36), its core replay verdict, and the slack accounting in `docs/experiments/P2.3c-2c-main-area.md`. Checkpoint: see the experiment file (established so far: geometry, the WR's slack profile 46→36 (entrance fall) →35 (tunnel) →4 (col-30 wall entry, steps 201–221) →1; `--goal-x` built + regression control identical; S1 ladder running, log `runs/P2.3c/top_s1_ladder.log`).
- **P2.1b-m3** — whole-room search engine. Started 2026-08-21 (session 4, late). Checkpoint 2026-08-22 (session 5): run 3 (in-memory frontier) OOM-killed after layer 85 — layer files 0–85 kept; engine rewritten as external-memory `bfs11c`/`bfs11cr` (parents streamed from `layer_NNN.bin`, per-thread accumulators capped at `--acc-mb` and spilled as sorted runs, k-way merge into the next layer file, `--resume N`, `--stop-step N`, FxHash heuristic maps; the broken in-binary reconstruction removed — `bfs11c-path` is the tool, the log prints the command); controls: 16-step control identical (2,256 grabs, same parent record, path reconstructs identically), resume-from-layer-40 reproduces run 3's counts for layers 41–46 exactly at 1.8× the speed; `tools/smb-opt-modes.patch` regenerated. Run 4 resumed from layer 85 under a cgroup cap (Running jobs). Remaining: (a) read run 4's outcome (candidate → reconstruct + replay on the core; no grab → H28 refuted in the relaxed model, P2.1b → Done); (b) optional landing bound (STANDING at y 176, x ≥ 3150 by step 236 — sound, only bites in the air). Details: `docs/experiments/P2.1b-pole-search.md` §P2.1b-m3 (run 3 outcome, run 4 engine).
- **P2.3a** — 4-2 top route (2-frame framerule target). Started 2026-08-22 (session 5, late). Steps 1–2 done (timeline; dump-derived block maps, `data/blockmaps/`). **Step 3 done 2026-08-22 (session 6)**: `src/case/w42.rs` (`W42Main`, `W42Warp`), `BB42`/`BB42Warp`, case-generic modes `tracec`/`bfsc`/`case W42*`, `tools/model_difftest.py --case`; alignment law F69; three MrWint-model bugs fixed from the WR trace (block-bounce timer F70, water-only joypad rule F71, coins end the collision routine F72) via `Options::BlockBounce` (default off: existing cases and run 4's layer keys unchanged) and coin-list handlers. **Warp-zone model exact** (WR 476 frames + 30k random/mutated frames, 0 differences, F75); main area exact up to the lift, which the WR rides and jumps off (F73). Pipe-entry rule + X windows (F74). `docs/experiments/P2.3a-w42-model.md`. `smb-opt case W42Warp` (MrWint's IDA\*) was tried and killed: after 10 min it was still on its first limit (329) with a 140M-state store → the x-only bound leaves ~150 frames of slack on the stair climb, so neither engine can finish the warp zone as is. **P2.3b done 2026-08-22 (session 6 cont.)**: the lift is in `W42Main` (`--lift 0`; spawn/descent/collision/rider/erase from the code, F73/F78), MrWint's scroll timing fixed (F77), main area exact on all 587 WR frames incl. the ride and on 120 near-lift mutated trials (F81) — the only remaining main-area gap is **block states** (`$23` bouncing / `$c4` used blocks after a bump: P2.3b-2). **A 1-frame gain exists on the WR's own route** (F79: Down one frame earlier at the wrong-warp pipe, verified on the core; absorbed by the framerule alone). The bottom route is a floor-level wall walk (F80). **P2.3b-2 done 2026-08-22 (session 7)**: block states in the model (`Options::BlockStates`, F84–F87): the bumped cell's `$23` phase, `$c4` conversion, the coin-brick interval timer, and the restore's dependence on `VRAM_Buffer1` (NMI flush only when `AddrCtrl` = 0; the parser's render bursts, coin erases, `ColorRotation` and the game-timer digits occupy it) — exact on the WR (587 rows, every bookkeeping field vs the dump) and on 100 random + 240 mutated block-bumping trials; W42Main key 13 → 20 bytes. The WR bumps the mushroom brick (28,7), the question block (51,7) and the vine brick (64,3). Remaining main-area omission: spawned items (H34). **Remaining:** (a) **P2.3c** — the proof-grade search (measured: reduced inputs only halve the space, F82; the warp zone's slack is the forced overshoot-and-return, F83 → external-memory `bfscx` + staged bounds, see the P2.3c row); (b) then the enemy-aware pass (goombas on the upper floor, koopa, beetles; piranhas in the warp zone).
- **P2.5a** — enemy model for 1-1 room 1 (H29 groundwork; CPU-light, runs alongside run 4). Started 2026-08-22 (session 5). Goal: a data-driven enemy model in the smb-opt patch for 1-1 room 1 (goomba pairs + green koopa incl. shell: spawn law from `E_GroundArea6` and the scroll position, walking, stomp/kick/injury rules), validated with `tools/model_difftest.py`-style random trials from the room-1 control frame until 0 differences vs the core. Acceptance: facts on the spawn law + koopa rules with dump evidence; difftest 0 differences incl. deaths/stomps/kicks; a `bfs11` room-1 case ready for P2.5b (deadline 367). Checkpoint 2026-08-22: the rule set is done and validated — spawn law (ScreenLeft + 303 ≥ column x; groups placed at ScreenRight), walk/phase, pipe turnaround, enemy–enemy turnaround, stomp lifetime (interval clock), offscreen erase, scroll law: `tools/room1_enemy_sim.py` reproduces the WR dump's enemy slots on all 369 room-1 frames with 0 mismatches and predicts both WR stomps; perturbation controls fail (F64/F65, `docs/experiments/P2.5a-room1-enemies.md`). **Remaining:** (1) port into the smb-opt patch as a data-driven enemy module for the room-1 case with `WithScrollPos` (state per enemy: status/slot/x/dir/phase/timer/collision bits; plus ScrollThirtyTwo + AreaParserTaskNum for the spawn-blocking frame; check MrWint's scroll threshold timing vs F65), (2) `tools/model_difftest.py` from the room-1 control frame (FCEUX row 197 ↔ QuickNES frame 194) until 0 differences incl. stomps/deaths, (3) a `bfs11` room-1 case (goal = vertical pipe entry at page 3 col 9, deadline 367) for P2.5b. **Prior (2026-08-22):** low — a stomp sets `Player_Y_Speed` = −4 with no A-boost (F59), so bounces cannot add height over the pipes, and the WR already takes two forced stomps in room 1 at the enemy-free optimum (F66); P2.3a (4-2) is queued ahead of the port.
- **P2.1b** — 1-1 third room. Done: FPG trigger + slide law (F51/F55); H27 refuted from the WR's stairs-top state (F57, with positive/negative search controls); pole mechanics (F60); goomba model validated (F56/F59); whole-room search attempted and sized (F61). Remaining = P2.1b-m3 (engine for the ~10⁸-states/layer whole-room enumeration, or a cloud box), which decides H28. When P2.1b-m3 reports, move P2.1b to Done.

## Next up (ordered — the top unblocked item is the next unit of work)

**Priority (user, 2026-08-22, docs/decisions.md):** after run 4, the next big run is the 4-2 main area with the top route (needs P2.3c-2c + the cloud box), then the 4-2 warp zone; 1-1 room 1 (P2.5a/P2.5b, H29) is queued behind the 4-2 work. Order of work: P2.3c-2c (main-area bound, laptop) → cloud decision → 4-2 main run → 4-2 warp run (after P2.3c-2b) → P2.5.

**Standing lead (H36, 2026-08-22):** 4-2 top route = HappyLee's "2 frames short" − the pipe-entry frame (F79, likely transfers) = 1 short; his bottom-route ending was 3 frames loose (F88), so the top route's unexamined ending is the most hopeful concrete target. Needs the main-area search with the top route allowed (P2.3c-2c + a run).

| ID | Title | Track | Size | Depends on | Acceptance |
|---|---|---|---|---|---|
| P0.10a | **Benchmark the reachable Hetzner tiers — under $1, highest leverage available.** The account quota caps us at 8 dedicated cores (F92), where a box is only ~1.08x the laptop (F93) — but the *shared* tiers are 4-10x cheaper per core and offer 16: cx53 16c/32 GB/320 GB at **$0.0561/h**, cpx51 16c at $0.1338/h, vs ccx33 8c at $0.2612/h. One hour of each costs ~$0.46 total. On each box: build `smb-opt`, run the `bfscx` 4-2 WR-suffix control (must reproduce `runs/P2.3c/ctrl_w42main_p575_d587.log` layer counts exactly) and a `--resume` of a run-4 layer, and record expand throughput (generated states/s) vs the laptop's logged 8.75M/s at 12 threads. First check whether the shared tiers are quota-limited too. **Delete every box the same hour** (F92: poweroff does not stop billing). | infra | S | P0.10 | A throughput/$ table for cx53 / cpx51 / ccx33 vs the laptop, in `docs/experiments/P0.10-cloud-sizing.md` §8, replacing the unmeasured 1.6x/core assumption; a go/no-go on shared tiers |
| P2.3c-3 | **(parked until a limits increase)** Parallelise `bfscx`'s k-way merge: have the expansion threads bucket their output runs by the top bits of the key, merge buckets independently, concatenate in key order — removes the only serial phase (F93). At <=16 cores the merge is just 4-6 % of wall time, so this buys almost nothing at the current quota; it becomes worth doing at 32+ cores. | infra | M | P0.10a, a limits increase | Merge wall time scales with `--threads`; layer files byte-identical to the serial merge on the 4-2 control |
| P2.1b-m3 | **Whole-room exact search engine.** Make `bfs11` (smb-opt patch) per-layer and compact: frontier as 10/11-byte compressed keys with a decompressor (the compression is lossless: x subpixel multiples of 16, speed fraction multiples of 4), parents/inputs appended to disk, per-layer dedup by sort/hash of keys (no global visited set; the goomba-relevance/frame keying stays), parallel expansion over 12 cores; add the landing bound "on the ground at X ≥ 3155 by step 236" (F60) on top of the x/y bounds. Run the relaxed model + goombas from the fixed entry with deadline 238 (proof direction), then the exact model on any candidate; replay candidates on the core. Expect ~4×10¹¹ model steps (F61). Alternative if engineering stalls: the current engine on a 64 GB cloud box (needs the cloud decision in "Needs user input"). | A | L | P2.1b-m2 | Either a grab ≤ step 238 replayed on the core (→ P4.1), or a complete search record refuting H28 |
| P2.5b | **Enemy-aware segment searches** (after run 4 and P2.5a): 1-1 room 1 with deadline 367 (H29), then the rest of P2.5 below. | A | L | P2.1b-m3, P2.5a | Search records; candidates replayed on the core |
| P2.3a | **4-2 top route — a 2-frame framerule target (F36).** ⚠ Unmodeled mechanic in this level: the **lift/platform** at page 3 col 10 (`E_UndergroundArea2` id $27) moves Mario's x without his own speed — the one mechanic that would make the x-bound unsound and that the player model lacks. Before trusting any 4-2 bound: determine whether the top route can touch it (block map `data/blockmaps/w42_main.txt`, the lift's travel range), and either prove it is unreachable on the searched paths or add platform carry (`Platform_X_Scroll`, standing-on-platform) to the model and difftest it. (1) From `docs/community-claims.md` and the WR dump, pin down what HappyLee's "top route" is and why it is 2 frames short of the framerule the WR's route misses by 13; (2) reconstruct 4-2's block map from the WR dump's block buffers (`$0500–$069F` as the screen scrolls; the top route's columns are the same columns) or from `tools/area_data.py L_*`, add a `w42` case to the smb-opt patch; (3) enemy-free exhaustive bounds per segment of the top route with `bfs11c` (deadline = the framerule) and compare with the claim; enemies (4-2 has goombas/koopas/buzzy beetles) as in P2.5a if the bound is tight. Rationale (F66/F67): every segment with a known optimum has the WR on it; 4-2 has none, and 2 frames is the smallest open framerule deficit. | A | L | P2.1b-m3 (engine) | Segment table (bound vs HappyLee's top route) + search records; any 2-frame gain replayed on the core |
| P2.3b | **4-2 lift in the model** (F73). Add `LargeLiftDown` to `W42Main`: spawn when the enemy parser reaches page 3 col 10 (scroll-driven — needs `WithScrollPos`, check its timing vs F65), x 940–(940+width: read `LargeLiftBBox`/`SPBBox`), y 80 + 15/16 px per frame from the spawn frame, rider placement `PositionPlayerOnVPlat`, side stop; state = spawn frame offset (or frame while relevant, as the goomba keying). Difftest from record 6584 with WR-mutated trials through the lift rides (the WR's 7012–7024) to 0 differences. Then run 4-2 main with deadline WR−1 (587) on the WR's own route as a control. | A | M | P2.3a step 3 | 0 differences incl. ≥ 50 lift rides; a `W42Main` search with the lift that reproduces the WR's 588 when the deadline is 588 |
| P2.3c | **Proof-grade search for vertical segments** (block states done, P2.3b-2; **engine done, P2.3c-1**: `bfscx CASE … [--lift N] [--stage] --layer-dir D --acc-mb M --resume N --stop-step N --check-path N` + `bfscx-path` + `tools/replay_check.py`; **warp-zone bound done, P2.3c-2**: staged + coupled two-phase x bound 461 vs WR 476, F89–F91, `docs/experiments/P2.3c-engine.md`) → remaining sub-steps: **(2b)** an end-game (x,y) bound for the warp zone's drop/fall/landing (the 15 remaining frames; or the Phase-B multi-root search of H35) so the frontier stops growing ×1.25/layer; **(2c)** main-area gates (pipe A top / row-7 bricks / pipe B) in `W42Main`'s bound with `--check-path` on the WR; **(3)** run the warp zone (deadline 475) and, after P2.3b-2, the main area top route (deadline 575 = 588 − 13) on the laptop under a cgroup cap or on a cloud box. Control: the 1-1 room-1 pipe entry must come out 368 (F63/F76). | A | L | P2.3b | warp zone + main area: exact enemy-free optima with proof records (vs WR 476 / 588, with the F79 frame) |
| P2.5 | **Enemy-aware segment search on the route** (P2.5a = the 1-1 room-1 model, in progress; P2.5b = the searches). Generalize `room_enemies` into a data-driven enemy model (spawn x/y/time from `E_*` level data, goomba walk, koopas incl. kicked shells, stomp/injury rules already ported), validate per segment with `tools/model_difftest.py --goombas`-style random trials until 0 differences, then per level: (1) enemy-free exhaustive bound per segment (MrWint cases / `bfs11`) vs the WR's segment time — gaps = room the no-enemy search finds; (2) enemy-aware search with deadline WR−1 where the WR sits on the bound — bounces are the only way past it (F63 logic). Order: 4-2 top route (2 short), 1-2 (5), 8-4 rooms (unquantized). First a cheap scan of the WR dump for every stomp on the route (those mark forced bounces). | A | L | P2.1b-m3 | Per-level segment table (bound vs WR) + search records; candidates replayed on the core |
| P2.1b-m2 | **(part 1 done — goombas in the model; part 2 = P2.1b-m3)** Goombas in the model + whole-room search. Extend smb-opt's 1-1 model with the two third-room goombas as frame-indexed obstacles (F58/F59: positions are functions of the frame; per-layer state adds each goomba's alive/defeated flag, `Enemy_CollisionBits` d0, the 1-frame `StompTimer`; collision only on even frames; boxes from `BoundBoxCtrlData`; stomp iff y speed > 0 else death; enable `WithScrollPos` or verify that screen-relative boxes never wrap near Mario). Validate with `tools/model_difftest.py --root-record 1048` (random inputs from the first control frame: deaths and bounces must match the core, incl. the WR's bounce at 1112 without injection). Then `bfs11` from frame 1045 (prefix 0, 239 steps, per-layer dedup while a goomba is alive, global after) with the FPG goal and deadline 1284 — the first single search of the room from the fixed entry. Memory: run under `ulimit -v`, one search at a time (15 GB box; the compact engine needs ~50 B/state). | A | M | P1.2-lite | Difftest from 1048 exact incl. enemy events; search record (layer counts, grabs) in `docs/experiments/P2.1b-pole-search.md`; any grab ≤ 1284 replayed on the core with `build/bfs --replay-check`-style evaluation |
| P2.1b | 1-1 third room, sound relaxation on the current core: (1) exact FPG trigger condition from `PlayerBGCollision`/`HandleClimbing`/`FlagpoleRoutine` (x, subpixel, y at the pole); (2) flat-world frontier — point the area/enemy parsers at their terminators, flatten the block buffer, BFS with physics-class + max-P dominance — earliest frame with x ≥ 3158 from the fixed entry (F47); (3) short-horizon search from the WR's stairs-top states for an on-arrival grab (H27) | A | M | P2.1a | Frontier frame with a proof sketch of the relaxation; FPG condition written down; H27 tested |
| P1.2 | Stage-2 core: static recompilation of the SMB1 ROM to C with cycle counting + minimal PPU timing (vblank, sprite-0); or, as a first step, a validated player-physics + BG-collision model with ~32-byte states (P1.2-lite) | A | L | P1.1 | Differential test vs QuickNES on ≥ 10M random frames incl. lag; fps recorded |
| P2.1c | Search engine v2 on the fast core: compact states, multi-process sharding, sound bounds; full 1-1 (all rooms) threshold search for H21 | A | L | P1.2, P2.1b | Ties the WR in 1-1; reports whether T_set − 1 is reachable, with the search record |
| P1.1b | Direct-link core: build Nes_Emu into the search binary, use `emulate_skip_frame` (no rendering), measure speedup; keep RAM-identity vs F45 | A | S | P1.1 | ≥ 2× fps over F46 with the same WR RAM trace |
| P2.2 | 8-4 exhaustive search (frame-granular; Bowser RNG, L+R, ending-input trick) | A | L | P2.1 | Report: faster path (verified in two emulators) or proof record |
| P2.3 | Threshold search for framerule N−1 in each flag/pipe level, in remaining-deficit order: 4-2 top route (2), 8-3 with FPG (7), 1-2 (8), 4-1 (9), 8-1 (18), 8-2 (19) | A | L | P2.1, P0.4 | Per-level report |
| P2.4 | Cross-level DP over reachable entry states (RNG / framerule phase) | A | M | P2.2, P2.3 | Best-known full route + proof record |
| P3.2 | RAM oracle: single-byte perturbation sweep per level on the fast core → jackpot map | B | M | P1.1 | `docs/experiments/P3.2-ram-oracle.md` |
| P3.3 | Write-reachability: H30 (block-buffer wrap write, $06B0–$06CF) is refuted for vines by the level data; remaining question = any other writer above Y $20 at an odd-page column 11/12 (enemy block bumps, coin erases); then the jackpot cells from P3.2 | B | L | P3.1 | Ledger entries with proof artifacts |
| P3.4 | Fuzzing / Go-Explore novelty search for anomalous states | B | L | P1.2 | Anomalies triaged in the ledger |
| P3.5 | NES Minus World re-examination with oracle + audit (WorldNumber = 36 OOB reads) | B | M | P3.1, P3.2 | Ledger entry with proof artifact |
| P4.1 | Assemble, verify in two emulators, draft submission text | ship | M | a result | User-reviewed before anything is submitted |

## Done
- 2026-08-22 — **P0.10 done** (session 8): cloud provider chosen and authenticated (Hetzner, `hcloud` 1.67.0, context `smb1-tas`), billing mechanics and machine sizing established — hourly rounded up with a monthly cap, **powered-off servers still bill (delete, don't poweroff)**, $/core-hour flat across dedicated tiers so the $300 cap is ~9,000 core-hours and size buys wall-clock only; the engine's serial k-way merge puts the useful ceiling at ~48 cores (F93). **Then the quota bit:** the account is new and capped at 8 dedicated cores, where a box is ~1.08x the laptop — so run 4 stays on the laptop, the near-term cloud value is horizontal (independent segment searches), and the shared tiers (4-10x cheaper per core) are worth a sub-$1 benchmark first (P0.10a). F92, F93, `docs/experiments/P0.10-cloud-sizing.md`.
- 2026-08-22 — **P2.3c-2 (warp zone) done** (session 7, late): staged route-phase bound (`--stage` hook, ext-aware `SearchGoal::distance_to_goal_heuristic_ext`) + the coupled two-phase x bound (`XPosHeuristic::build_overshoot_bound`, end classes restricted to the first crossing's right-moving classes): 461 at the WR's entry (x-only 329, WR 476), 0 violations; exhaustive controls show the WR's last 76 frames optimal from its drop state; frontier still ×1.25/layer at 15 frames of slack → cloud + end-game bound (F89–F91, H35). Main-area gates not started (P2.3c row).
- 2026-08-22 — **P2.3c-1 done** (session 7, late): `bfscx`/`bfscx-path` — the external-memory engine for any case with the object hook (24-byte records = key + ext; per-thread `--acc-mb` accumulators spilled as sorted runs, k-way merge, `--resume`, `--stop-step`, `--check-path` by binary search in the sorted layer file); identical to `bfsc` on the 4-2 WR-suffix control and on 1-1 room 1 (from layer 22; earlier layers differ by per-layer vs global dedup); first search result replayed on the core: 3 frames on the WR's last 12 frames of 4-2 (F88, framerule-absorbed); `tools/replay_check.py`; `docs/experiments/P2.3c-engine.md`.
- 2026-08-22 — **P2.3b-2 done** (session 7): block states in the smb-opt model (`Options::BlockStates`: `$23` bounce phase, `$c4` conversion, coin-brick interval timer, `VRAM_Buffer1` flush/occupancy law, parser task schedule) — exact on the WR's 587 rows (every field vs the dump) and 340 trials (F84–F87); H34 (item routes) recorded; `tools/block_state_check.py`, `tools/block_bounce_sim.py`, `tools/count_block_bumps.py`; `docs/experiments/P2.3b-2-block-states.md`.
- 2026-08-22 — **P1.3 done** (session 5, late): the x-bound audited against the real core in 1-1 room 3 — 1.9M checks, 0 violations, 0 unknown classes, bound tight (F68); `tools/heuristic_audit.py`, `smb-opt trace11bound`. Re-run for every new room/case (cheap); lifts/platforms remain the one mechanic that could break it.
- 2026-08-22 — **P2.5 scan done** (session 5): WR vs MrWint's enemy-free segment optima on the route — gap 0 on every solved segment (1-1 ×4, 1-2 opening, 8-4 ×5; F66); 8-4 runs at the speed cap on every frame outside two fixed accelerations (F67). Player-only search has unexplored room only in 4-1/4-2/8-1/8-2/8-3 (no optima exist) → new unit P2.3a (4-2 top route) queued ahead of the room-1 enemy port. `docs/experiments/P2.5-segment-scan.md`.
- 2026-08-22 — **P3.1 done** (session 4): `tools/oob_audit.py` (`--target $ADDR`: stores in reach with index provenance; JumpEngine sites) + `docs/oob-audit.md`: the H7 cells ($075F/$0750/$0751/$06D6) are unreachable by any indexed or pointer store; the one real OOB mechanism (block-buffer writes wrap for objects above the status bar) reaches $05E0–$06CF, incl. `EnemyFrenzyBuffer`/`SecondaryHardMode` → H30 (vine write → out-of-range JumpEngine), handed to P3.3.
- 2026-08-21 — **P0.7 done** (session 4): `docs/input-semantics.md` — every button's effect from the code (L+R = facing 3 / doubled friction / Right wins; Down cancels L/R; U+D = Up on vines; fresh-A jumps; pause ≥ 44 frames freezes timers + FrameCounter + logic but steps the LFSR; TimerControl); F62; H3 annotated; the 16 A/B/L/R inputs are complete for small Mario on land.
- 2026-08-21 — **P2.1b-m2 part 1 done** (session 4): goomba pair in the smb-opt model (`room_enemies`, `trace11room`, `bfs11 --goombas`, relaxed case `bfs11r`, `--check-path` controls), exact on 97k frames incl. 119 deaths / 35 glitch grabs (F56/F58/F59 corrected); pole mechanics F60 (the WR's real finish; WR on the x-frontier at the stairs top); whole-room attempts sized (F61); H28 = the residual question. `docs/experiments/P2.1b-pole-search.md`.
- 2026-08-21 — **P1.2-lite done** (session 4): MrWint's model built, fixed (running timer, fresh-A jumps), extended with the goombas, and validated exact against QuickNES on ~190k random/mutated frames in the 1-1 third room (F53/F56); modes in `tools/smb-opt-modes.patch` (`tools/build_core.sh` applies it); `tools/model_difftest.py`. `docs/experiments/P1.2-lite-smb-opt.md`.
- 2026-08-21 — **P2.1b-m done** (session 4): `smb-opt bfs11` model-side BFS; from the WR's frame-1229 state no glitch grab ≤ 1284 exists (H27 refuted there; lowest grab Y 161.875; F57); the slide law F55 fixes the objective (Y ≥ 162 at the grab); model validated exact on 82k random frames after two fixes (F56, `tools/model_difftest.py`); goomba facts F58/F59; `docs/experiments/P2.1b-pole-search.md`, `docs/experiments/P1.2-lite-smb-opt.md`.
- 2026-08-21 — **P0.8 done**: `docs/prior-tools.md` — the only exhaustive-search tool is MrWint/smb-opt (player-only model, segment searches, 4-4 wall clip); no whole-level/full-state search exists; Maru's best 1-2 is 5 frames from the framerule; F52; H21/H22 annotated; new unit P1.2-lite.
- 2026-08-21 — **P2.1a done**: `src/search/bfs.c` (in-process BFS engine, exact T_set evaluator verified on the WR, deadline pruning); full-state BFS measured infeasible (×5/layer, F49); WR acceleration profile and pole stop decoded (F47/F48); H21 restructured, H27 added; `docs/experiments/P2.1a-bfs-engine.md`.
- 2026-08-21 — **P1.1 done**: libretro QuickNES harness (`src/fastcore/harness.c`, `tools/build_core.sh`, `tools/fm2_to_inputs.py`, `tools/compare_ram.py`) replays the WR with RAM identical to the FCEUX dump on every row (F45, alignment law: FCEUX row r ↔ QuickNES row r−3, input record j → QuickNES frame j−2); 15.0k fps/instance, 104k fps on 12 threads, savestate 12.8 KB / 2.5 µs (F46); `docs/experiments/P1.1-fast-core.md`.
- 2026-08-21 — **P0.6 done**: `docs/warp-model.md` — WZC ∈ {0,1,4,5,6} at any pipe (proof), world 8 only from 4-2's $2F zone, all 58 pipe-destination commands tabulated, completion = axe with WorldNumber ≥ 7, Minus World closed at table level; `tools/warp_tables.py`, `tools/area_data.py`, `tools/ram_trace.py`; facts F38–F44; H5 refuted, H6/H13 refuted at table level, H7 sharpened; `docs/experiments/P0.6-warp-model.md`.
- 2026-08-21 — Research of the current record and community state; plan, process, and status scaffolding written (PLAN.md, PROCESS.md, this file, docs/*). Git initialized, initial commit. See docs/log.md.
- 2026-08-21 — **P0.2 done**: WR movie fetched to `data/wr/`, 17,868 frames confirmed, ROM verified against TASVideos hashes and the movie's romChecksum; `tools/fm2_info.py`, `tools/verify_rom.py`. Facts F1/F15–F17.
- 2026-08-21 — **P0.9 done**: `docs/community-claims.md` — 1-1 known as "1 frame short" since 2009 with no proof; 4-2 top route is 2 frames short (HappyLee); 8-3 FPG/242 = 3 frames; Maru's 8-4 idea; facts F35–F37, H25/H26. Still to mine: thread pp. 1–56/58/61, Maru's movie.
- 2026-08-21 — **P0.5 done**: `docs/timing-model.md` — every WR frame count derived from the code (NMI/timers/boot/loads/flag sequence/pipes/axe); T_set = grab + T + 159; control = load + 154 + w; Start rows 34–43 equivalent; facts F31–F34.
- 2026-08-21 — **P0.4 done**: framerule mechanism verified frame-exactly; slack/deficit per level (1-1 is 1 frame from the next framerule; 1-2 8, 4-1 9, 8-3 10, 4-2 13, 8-1 18, 8-2 19; 8-4 unquantized); `tools/slack_table.py`; facts F27–F30; H21–H24; `docs/experiments/P0.4-slack-table.md`.
- 2026-08-21 — **P0.3 done**: WR syncs in FCEUX and BizHawk/NesHawk with identical per-frame state; axe on fm2 frame 17867 (0-based); 24 lag frames; level-entry frames; `tools/fm2_to_bk2.py`, `tools/check_sync.py`, `tools/compare_dumps.py`, `tools/lua/wr_dump_*.lua`; dumps in `data/wr/`; facts F23–F26; `docs/experiments/P0.3-wr-sync.md`.
- 2026-08-21 — **P0.1 done**: FCEUX 2.6.6 (Lua), Mesen 2.1.1, BizHawk 2.11.1 (NesHawk) all run headless + scripted on the Linux box (toolbox container `smb1` for FCEUX/mono; no host sudo needed); wrappers `tools/*_run.sh`, reproduce with `tools/toolbox_setup.sh`; specs/versions/throughput in facts F18–F22; `docs/experiments/P0.1-tooling.md`.

## Loose ends (small, unassigned)
- (resolved in P1.1, F45) Start-press row: FCEUX applies fm2 record j in dump row j+2, so record 41 acts on row 43; `fm2_info.py` (record index) and the dump (row) were both right.
- The FCEUX/BizHawk dumps and the QuickNES output use different row origins (F45); any tool that mixes them must state the offset. `tools/check_sync.py`/`compare_dumps.py` are FCEUX/BizHawk-only.

## Blocked / Needs user input
- (optional, not blocking) **Native emulator install on the host** would let `tools/fceux_run.sh` skip the container: `sudo dnf install -y fceux xorg-x11-server-Xvfb mono-core mono-devel libgdiplus lsb_release cmake clang gdb strace` (RPM Fusion is already enabled on the host). Everything currently runs in the rootless toolbox container, so this is a convenience only.
- **Cloud — provider chosen and authenticated (P0.10 done 2026-08-22); now blocked on an account quota, not on the user.** Hetzner Cloud; `hcloud` 1.67.0 at `~/.local/bin/hcloud`, context `smb1-tas`, token in `~/.config/hcloud/cli.toml` (outside the repo — never copy it in), verified with `hcloud location list`. Sizing, billing rules and the revised plan: `docs/experiments/P0.10-cloud-sizing.md`; F92 (billing + quota), F93 (engine scaling).
  - **Quota:** the account is new — only ccx13/ccx23/ccx33 (**≤ 8 dedicated cores**) are offered; ccx43/53/63 need a limits increase and the console refuses the request as the account is too new. **Re-request once the account has some history / a paid invoice** — that is the gate on the 4-2 main-area proof run, and it needs no work from us until then.
  - **Consequence:** 8 dedicated cores ≈ 1.08× the laptop (F93), so **no reachable box is worth buying to replace the laptop for run 4** — let run 4 finish where it is. Near-term cloud value is *horizontal*: independent segment searches (P2.5b, the `bfscx_ladder.sh` deadline ladder, warp zone vs main area) that currently queue behind each other, one per small box, no engineering needed.
  - **Note for whoever runs it:** `hcloud context create` needs a TTY and fails under Claude Code's `! …` with `non-interactive tty detected`; the user runs it in their own terminal, or `--token-from-env` with `HCLOUD_TOKEN`. Never have the user type a token into a `!` command (it lands in the transcript).
- (optional, not blocking) **Ask Hetzner support whether sustained full-load use of a shared tier is acceptable** — needed only if P0.10a says the shared tiers are fast enough to want for multi-day runs. **This is outreach: needs the user's explicit go-ahead (CLAUDE.md).**

## Key numbers (each must be reproducible by a script in tools/)
| Quantity | Value | Source / script |
|---|---|---|
| WR movie length | 17,868 frames (4:57.31) | `tools/fm2_info.py` (V) |
| WR last input | frame 17848 (0-based); 19 input-free frames follow | `tools/fm2_info.py` (V) |
| WR first Start press | frame 41 (0-based); any Start on rows 34–43 gives the same 1-1 start (F31) | `tools/fm2_info.py`; `docs/timing-model.md` (V) |
| WR Left+Right frames | 85 (U+D: 0) | `tools/fm2_info.py --list-lr` (V) |
| RTA-timing equivalent | 4:54.032 | S |
| RTA-rules (no L+R) TAS | 4:54.265 — 14 frames slower | S |
| Framerule | 21 frames | S |
| WR axe frame (OperMode→2) | fm2 frame 17867 (0-based) = last frame of the movie; timer 317 | `tools/check_sync.py data/wr/fceux_wr.csv data/wr/happylee-supermariobros,warped.fm2` (V) |
| WR level-entry frames (fm2, 0-based) | 1-1 42, 1-2 1944, 4-1 3766, 4-2 6042, 8-1 7723, 8-2 10813, 8-3 12956, 8-4 15057 | `tools/check_sync.py` (rows −1) (V) |
| Per-level slack/deficit (frames) | 1-1 20/**1**, 1-2 13/8, 4-1 12/9, 4-2 8/13 (top route: 2, S), 8-1 3/18, 8-2 2/19, 8-3 11/10 (FPG+242: 7, S), 8-4 unquantized | `tools/slack_table.py data/wr/fceux_wr.ram` (V) |
| Per-level frames (load→next load) | 1-1 1902, 1-2 1870, 4-1 2228, 4-2 1729, 8-1 3042, 8-2 2143, 8-3 2101, 8-4 2810 (+43 boot/title) | `tools/slack_table.py` (V) |
| Lag frames in WR (before the axe) | 24 (7 boot, 1 after Start, 16 area loads; none in-level) | `tools/check_sync.py`, `tools/slack_table.py` (V) |
| 1-1 third room (QuickNES frames) | entry X 2616 speed 0, control after 1045; WR speed 40 by 1079, x 3157.3 at 1280, grab after 1285 (T 370), T_set 1814; sound-bound frontier at the pole: 1272 | `tools/ram_trace.py`, `build/bfs --replay-check` (V) |
| 1-1 pole search (root 1229, core) | v2: 1.52M states at frame 1243, no grab ≤ 1284; v4 (all bounds) killed at frame 1247, 1.94M states | `runs/P2.1b-root1229-*/stdout.log` (V) |
| 1-1 pole search (root 1229, model) | exhaustive to frame 1284: **no glitch grab**; earliest any grab 1279 (Y 152), lowest grab by 1284 Y 161.875; exact model: stopped at frame 1277 (34.8M frontier, 182M visited, 9.6 GB) — the relaxed model's run is the proof (relaxation argument in the experiment file) | `smb-opt bfs11 data/wr/wr_inputs.bin 1048 184 55 66:yspd=-1024 --root-frame 1229` (V) |
| Exact x-bound (MrWint table) | x ≥ 3158 earliest at frame 1279 from the frame-1229 state, 1278 from the room entry (frame 1045); WR arrives 1280 | `bfs11 … --root-frame 1229` / `… 1048 0 239 --root-frame 1045` ("heuristic at root") (V) |
| Model vs core differential test | 0 differences: player-only 1,600 trials (596 grabs, 71 FPG); with goombas 97,335 full-length frames / 700 trials from the first control frame, 119 deaths, 37 grabs (35 FPG) | `tools/model_difftest.py [--goombas --root-record 1048] --n … --seed …` (V) |
| 4-2 model vs core (P2.3a/b/b-2) | warp zone: 0 differences on the WR's 476 frames, 40×300 random, 40×480 mutated; main area (lift + block states): 0 differences on the WR's 587 frames (bookkeeping fields 0 mismatches vs the dump), 100 random, 240 mutated block-bumping trials (only koopa deaths remain, 28 trials) | `tools/model_difftest.py --case W42Warp --first 7247 …` / `--case W42Main --first 6584 --lift 0 …`; `tools/block_state_check.py` (V) |
| 4-2 main search control (20-byte key, block states) | `bfsc` and `bfscx` identical: layers 6, 16, 34, 70, 134, 673, 3472, 16472, 69489, 257001, goal at layer 10 with 16,194 transitions; WR suffix in the frontier at all 10 layers; `bfscx-path` reconstructs in < 1 s; 21 s total each | `runs/P2.3b-2/bfsc_ctrl_p575_d587.log`, `runs/P2.3c/ctrl_w42main_p575_d587.log` (capped 2G) (V) |
| 4-2 warp-zone bound at the WR's entry state | x-only 329 → staged 460 → coupled (right-moving end classes) **461**; WR 476; 0 violations on 476 steps; return from the WR's drop state: optimum = 76 = WR (75 impossible) | `runs/P2.3c/warp_stage3_p0_d475.log`, `warp_stage_p400_d76.log`, `warp_stage_p400_d75.log` (V) |
| 4-2 warp-zone frontier (bound 461, 15 frames of slack) | 9.2M states at layer 24, ×1.25/layer; return phase at zero slack ~21M/layer (76 layers, 1054 s) | same logs (V) |
| bfscx vs bfsc, 1-1 room 1 (deadline 368, 40 layers) | identical unique counts from layer 22 on (layer 40: 152,426); layers 1–21 differ by the global-vs-per-layer dedup (bfscx counts a state re-reached at a later frame again, e.g. +1 for standing still at layer 1) | `runs/P2.3c/ctrl_w11_d368_bfscx.log` vs `runs/P2.3a/ctrl_w11_d368.log` (V) |
| 4-2 frames available on the WR's own route | **3** on the last 12 frames before the wrong-warp pipe (exhaustive 12-step search + core replay, F88: entry at frame 7166 vs the WR's 7169; F79's Down-earlier was 1 of them); the other 6 WR pipe entries are earliest-possible; all absorbed by the framerule (deficit 13 → 10) | `tools/replay_check.py … --path runs/P2.3c/ctrl_w42main_path.bin --lift 0 --down`; `tools/pipe_entry_scan.py` (V) |
| 1-1 room-1 control: IDA\* vs full-input BFS | IDA\* (reduced inputs): 22 s, 283,205 states, 368 frames; full-input `bfsc` with deadline 368: 5.2M states/layer at layer 60, stopped | `runs/P2.3a/ctrl_w11_*.log` (V) |
| Whole-room search growth (relaxed model + goombas, deadline 238) | 27.1M unique states at frame 1093 (×1.3/layer), 107M visited, 6.4 GB — died at the cap; est. ~10⁸ states/layer saturated | `smb-opt bfs11r data/wr/wr_inputs.bin 1048 0 239 --goombas` under `ulimit -v` (V) |
| Whole-room search (relaxed + goombas, deadline 238, compact engine) | run 3: layer 85 (frame 1130) 328.2M unique states, 4.84G children/layer, growth ×1.024/layer, 741 s/layer, OOM-killed at 11.1 GB RSS; run 4 (external-memory engine, resumed from layer 85): layer 86 337.5M unique, 495 s/layer, anon RSS ~3.2 GB | `runs/P2.1b-model/room_compact_d238{,_r85}.log`; `tools/room_run_resume.sh` (V) |
| x-bound audit vs the real core (room 3) | 1,906,600 checks / 60,170 steps, 0 violations, 0 unknown classes, max gain/bound 1.0000 | `tools/heuristic_audit.py --n 200 --len 200 --seed 2` etc. (V) |
| WR vs MrWint segment optima (route) | gap 0 on all 10 solved segments (1-1: 368/111/174/66; 1-2: 71; 8-4: 79/70/40/70/74); 8-4 speed cap on every frame outside 2 accelerations | `docs/experiments/P2.5-segment-scan.md` (V) |
| Block-map reconstruction vs MrWint | 0 differing cells: BB11 cols 0–71 and 160–207, BB12 cols 0–191 | `tools/blockmap_from_dump.py data/wr/fceux_wr.ram 2446 3767 --compare third_party/smb-opt/src/blockbuffer/world1.rs BB12` (V) |
| 4-2 WR segments | main area control→wrong-warp pipe 588 (rows 6585→7173); warp zone control→world-8 pipe 476 (7248→7724) | `tools/ram_trace.py data/wr/fceux_wr.ram 6040 7725 AreaPointer GameEngineSubroutine Player_PageLoc --changes` (V) |
| Room-1 enemy reference sim vs WR dump | 0 mismatching rows / 369 frames (rows 197–565), stomps {323, 517} predicted, scroll law 0 mismatches; controls: 178 / 303 / 243 mismatching rows for perturbed rules | `tools/room1_enemy_sim.py` (V) |
| Search controls | from frame 1269: 15 steps → no grab (11,921 states), 16 steps → 2,256 FPG grabs incl. the WR's | `smb-opt bfs11 … 1048 224 {15,16} --goombas --check-path …` (V) |
| Stage-1 core speed | 15.0k fps/instance, 69k (6) / 104k (12 threads) aggregate; state 12,792 B, save+load 2.5 µs | `tools/build_core.sh` then `./build/harness … --input-skip 2 [--state-every 1]` (V) |
| Emulator alignment | FCEUX row r ↔ QuickNES row r−3; fm2 record j → FCEUX row j+2 / QuickNES frame j−2 | `tools/compare_ram.py … --offset -3 --from 10 --ignore 160-1ff --all` (V) |
| Reachable warp destinations | 1-2 → {4,3,2} or {−1,5,−1}; 4-2 ceiling → 5; 4-2 $2F → {8,7,6}; WZC ∈ {0,1,4,5,6} | `tools/warp_tables.py` + `docs/warp-model.md` §5.4 (V) |
| Ending-input coast length in WR | 19 frames: last A press on frame 17848, axe on 17867 | `tools/fm2_info.py`, `tools/check_sync.py` (V) |

## Spend
Cloud total: **$0 / $300 cap.** Provider: Hetzner Cloud (context `smb1-tas`). No resources exist yet — `hcloud server list`, `hcloud volume list`, `hcloud primary-ip list` all empty as of 2026-08-22.

**Standing cloud rules (P0.10, F92):**
- **A powered-off server still bills.** Hetzner bills until the server is *deleted*, regardless of power state. The off switch is `hcloud server delete`, never `hcloud server poweroff`.
- Billing is hourly **rounded up** (a 20-minute box costs a full hour), capped at the monthly price. No daily minimum, so short bursts are cheap and safe.
- **Every session's orient step runs `hcloud server list`** — a forgotten box is the only real way to burn this cap (ccx33 = $6.27/day; a ccx63 would have been $38.70/day).
- Delete at the end of every run; job scripts should end in `hcloud server delete`. Never leave an idle instance (PROCESS.md).
- Ancillaries that bill independently of any server: volumes $0.0767/GB-month while they exist, snapshots $0.0199/GB-month (compressed), primary IPv4 $0.60/month (use `--without-ipv4` or let it auto-delete). Do **not** enable automatic backups (flat 20 % of the server price).
- Record each spend here as it happens, not at the end.

## Model omissions (keep current — the cracks are most likely here)
Mechanics the smb-opt player model does not contain, or contains unvalidated. Every refutation must say which of these could matter in its room and why not.
| mechanic | status | where on the route | risk to bounds |
|---|---|---|---|
| enemies: stomps/bounces | modeled for 1-1 rooms 1 (P2.5a sim) and 3 (validated) | everywhere | bounces change y only (sound for x) |
| enemies: koopa shells (kick, carry, chain) | not modeled | 4-1, 4-2, 8-1…8-3 | no x effect on Mario; kills/deaths only |
| **moving platforms / lifts** | **4-2 lift modeled + validated 2026-08-22** (`W42Main --lift`, F81: exact on the WR ride and 120 near-lift trials); 8-x lifts not yet (same code path, `LargeLiftUp/Down`) | **4-2 (page 3 col 10), 8-x lifts** | vertical lifts do not move x (x-bound stays sound, F68); horizontal ones (8-x) would |
| block states after a bump ($23 for 14 frames — longer while VRAM_Buffer1 is busy — then $c4 for question/hidden/item/coin bricks, the brick itself for plain bricks) | **modeled + validated 2026-08-22** (`Options::BlockStates`, 4-2 main only so far: F84–F87; other cases need their cell lists + start phases) | any level with bricks on the route (1-1 room 1, 1-2, 4-2, 8-x) | a bumped hidden block becomes a platform; a $23 cell stops the floor-level wall walk (F80) |
| **spawned items** (mushroom from (28,7)/(55,7), star (81,7), vine (64,3) in 4-2; the WR bumps (28,7) and (64,3)) | **not modeled** (H34): the object is not created; a route that collects it is outside the model | 4-2 main, any level with item blocks on the route | big Mario / star change collision and brick breaking; only matters if a faster route needs the item |
| head/brick bumps, coin/powerup blocks | **fixed + difftested 2026-08-22**: block-bounce timer (F70, `WithBlockBounceTimer`), coins end the collision routine (F72, `coin_list_handler!`); second bumps / $c4 conversion exact via `BlockStates` (F84) | 1-1 room 1, 4-2, 8-x | none for x; affects y paths and landing frames |
| joypad above/below the screen | **fixed 2026-08-22**: disabled only in water areas (F71) | 4-2 warp zone (WR jumps over the screen top), 8-4 water | — |
| Down / pipe entry | entry = "possible" (Down not an input); add Down when replaying (F74) | every pipe | — |
| screen scroll / left-edge clamp | modeled (`WithScrollPos`); **timing fixed 2026-08-22** (F77, previous-frame relative x); parser/enemy-spawn timing law F78 (VRAM-busy frames not modeled) | everywhere | clamp only slows; spawn frames shift by ≤ 1 on VRAM-busy frames |
| swimming | modeled by MrWint, unvalidated by us | 8-4 water room | unknown until difftested |
| springs, vines, big Mario, fire | not modeled | not on the route (vine = 4-2 top area, unused) | — |
| lag frames, RNG, game timer | outside the model by design | handled in `docs/timing-model.md`; RNG matters in 8-4 Bowser | — |
