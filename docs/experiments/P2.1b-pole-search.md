# P2.1b — 1-1 pole approach: exact search from the WR's stairs-top state

**Goal (part 3 of P2.1b, H27).** From the WR state after QuickNES frame 1229 (stairs top, X 3033,
Y 48, speed 40, T 372), is there any input sequence that grabs the flagpole at frame ≤ 1284 (the WR:
1285)? A hit with T = 370 would move 1-1 to the next framerule (T_set ≤ 1813, −21 frames).

**Setup.** `src/search/bfs.c` (v1, full savestates) and `src/search/bfs_par.c` (v2: 2 KiB RAM
states + template savestate (F50), 10 forked workers, shared-memory arenas, parent-side dedup).
Sound pruning: speed +≤2/frame, cap 40 (F48); drop states that cannot reach x ≥ 3158 (F51) by
frame 1284. Inputs: 16 A/B/L/R combinations per frame.

**Commands.** See `runs/P2.1b-root1229/README.md` (v1) and `runs/P2.1b-root1229-par/README.md` (v2).

**Engine versions.** v1 `bfs.c` (12.8 KB savestates); v2 `bfs_par.c` (2 KiB RAM + template savestate, F50; 10 forked workers); v3 (compact states: only the bytes at addresses that ever differ from the root RAM, LMAX 384, ~200 used; verified to produce the identical state set as v2 at layer 7); bounds: (a) speed +≤2/frame (F48), (b) `--xpos-table`: MrWint's XPos max-distance table exported from smb-opt (`xpos-dump`, 70,688 x-physics classes keyed by $57:$0705, quantized $0700, $45, $33, $1D==0, $0703≠0) giving the exact model-derived minimum frames to reach x ≥ 3158, (c) `--y-target 162`: best-case descent (8.8 y speed += $90 per frame, cap 4 px/frame, +1 px rounding). Template caveat (F50): roots at/adjacent to lag frames break the reconstruction (root 923 collapsed to 1 state/layer) — use roots ≥ 930.

**Results so far.**
- v1 (12.8 KB states, 6 GB cap = 245k states): layers 1–10 (frames 1230–1239): 16, 127, 847,
  3,608, 11,616, 30,928, 70,096, 132,684, 219,936, ≥245,300 (cap; 950k children overflowed);
  pruning grows fast (layer 10: 289k pruned vs ~1.2M kept). 840 s single-threaded.
- v2 reproduces v1's counts and runs ~4.5× faster on 10 workers (layer 7: 12 s vs 54 s). Its long run
  (`runs/P2.1b-root1229-par`) hit the 1.52M cap at layer 14 (frame 1243): 16, 127, 847, 3,608, 11,616,
  30,928, 70,096, 132,684, 219,936, 354,364, 489,820, 709,928, 1,087,176, 1,518,208+ (×1.4/layer,
  pruned 289k→76k/layer, no terminal). v3 without new bounds reached the same 1.09M at layer 13
  (`runs/P2.1b-root1229-v3/stdout.partial.log`) before being restarted.
- Bounds (b)+(c) from root 1229: layer 11 = 292,660 vs 489,820 (−40%); pruned 103k at layer 7.
  From the control start (root 1040, full third room) bound (b) alone: frames 1051/1052/1053 →
  37,815 / 66,281 / 119,502 unique states vs 107,757 / ≥245,300 / — without it (xunk = 0: every
  real-game x-class was in the table). Growth ≈ ×1.8/layer there.
- Run v4 (`runs/P2.1b-root1229-v4`, bounds a+b+c, 7 GB, ~9M-state cap) launched 2026-08-21 ~20:35.

**Open.** Whether v4's cap holds to the deadline (≈55 layers); if it finishes with no terminal ≤ 1284
that proves no improvement from *this* frame-1229 state (modulo the bounds' soundness: (a) from the
code, (b) from the validated model F53, (c) from `ImposeGravity`/`MovePlayerVertically`). Next: the
full third room from root 1040 with (a)+(b)+(c) (growth ×1.8/layer at first; the y bound only bites
late), then roots 1176/1200; the flat-world frontier (part 2) was not started.

## Model-side search (P2.1b-m, session 4, 2026-08-21)
**Why.** The core run v4 (same root, bounds a+b+c) reached layer 18 (frame 1247) with 1.94M states at
500 s/layer (×1.25/layer) and was killed by the out-of-memory killer together with a model run; it would have
hit its 7 GB cap around layer 26 anyway. `runs/P2.1b-root1229-v4/stdout.log` is the record (inconclusive).
The validated player model (F53, F56) has no enemies — and none are ahead of the stairs top — so the same
question can be asked of the model with 11-byte states.

**Tool.** `smb-opt bfs11 INPUTS FIRST PREFIX MAX_STEPS [STEP:yspd=V] [--root-frame F] [--any-y] [--out FILE]`
(`tools/smb-opt-modes.patch`): replays the WR prefix in the model (goomba bounce injected at step 66),
then a layered BFS over all 16 A/B/L/R inputs, global dedup on the compressed player state (sound because
the model is time-invariant: the same state seen later can do nothing the earlier copy could not), bound =
MrWint's `BoundsHeuristic` (`get_steps_until_bounds_at_least(x ≥ 0xc5600, y ≥ 0x1a200)`, exact precomputed
x and y gain tables), deadline = MAX_STEPS. Records every `StateChangeFlag` with step/x/y and whether it
is a glitch grab (Y ≥ 162, F55); prints the earliest grab's input sequence (RLE) and writes it as
NES-order bytes. Memory ≈ 35 B/state (11-byte key, u32 parent, u8 input, frontier states only); the first
version kept full `State`s (≈130 B/state, 7 GB at 55M states — the OOM).

**Command.** `smb-opt bfs11 data/wr/wr_inputs.bin 1048 184 55 66:yspd=-1024 --root-frame 1229 --out …`
(prefix 184 records = frames 1046–1229; 55 steps = frames 1230–1284).

**Result (root 1229, deadline 1284).**
- Model as cloned (`NoRunningTimer`, held-A jumps — a relaxation of the real game in both respects, see
  `P1.2-lite-smb-opt.md`): layers 8, 55, 247, 814, 2,099, 4,044, 5,567, 10,085, 17,469, 27,996, 34,723,
  36,930, 38,700, 50,613 (frame 1243; the core had 667,076 at the same frame — 17× more, its RAM carries
  non-physics variety), … peak 6,280,121 at frame 1275, then the bound bites; 55.3M states visited, 380 s,
  **no glitch grab**. Grabs exist from frame 1279 (Y 152, X 3161) on; the lowest grab by frame 1284 has
  Y 161.875 (0x1a1e0) — one pixel above the glitch threshold, and a non-glitch grab waits ~47 frames for
  the flag (F55). `runs/P2.1b-model/root1229_d55.log`, compact re-run identical: `root1229_d55_v2.log`
  (163 s, 2.2 GB RSS).
- Corrected model (`WithRunningTimer` + `a_held`, exact on 82k random frames, F56): see
  `root1229_d55_v3.log` — stopped by hand at layer 48 (frame 1277): 34.8M states in the frontier, 182M visited, no grab, 9.6 GB RSS + 4.3 GB swap (9:42). The exact model has ~5× the states of the relaxed one (running timer × A-held). **Relaxation argument** (why the as-cloned run is already the proof): for every real input sequence there is a model input sequence with the same trajectory — set B whenever the real game has the run cap through `RunningTimer` (B has no other effect for small Mario), and clear A on ground frames where the real game ignores a held A (`GetPlayerAnimSpeed` masks A out; A has no other ground effect). So the relaxed model's reachable set ⊇ the real game's, and "no glitch grab ≤ 1284" transfers. Use the relaxed model for proofs and the exact one (or the core) for candidates.
- Heuristic at the root: 50 steps, i.e. x ≥ 3158 is unreachable before frame 1279 from this state (exact
  x-physics table), so the WR's arrival at 1280 is within one frame of the x-optimum from the stairs top.

**Whole room.** From the room's fixed entry state (frame 1045, F47) the same exact x-table gives 233 steps:
x ≥ 3158 cannot happen before frame 1278 on flat ground (the crude ≤ 2/frame bound said 1272, F48). The
WR reaches 3157.3 at 1280 and grabs at 1285 after the 5-frame embed creep; the deadline 1284 leaves 6
frames of slack over the x-bound, which is what keeps a whole-room BFS small. The enemy-free model probe
from frame 1045 (`root1045_d239.log`): exact model, `ulimit -v 9.5 GB`: ×1.19/layer at layers 20–25, ×1.5/layer by layer 45; died at the cap after layer 45 (frame 1090: 25.4M unique, 87M visited, 5.8 GB RSS, 5 min). The acceleration phase alone is too wide for the exact model with only the x/y bounds. The relaxed model and per-layer dominance/bounds are the levers (P2.1b-m2). The goombas (F58) are not in the model yet: a path
through them is invalid (death), and the WR's bounce is unavailable — so that probe is neither a proof nor
a complete candidate search; the goomba sub-unit (P2.1b-m2) closes both gaps.

**Conclusion.** H27 is refuted for the WR's stairs-top state (proof: exhaustive model BFS, sound dedup and
bounds, model exact on the relevant mechanics). Any 1-1 gain must come from an earlier divergence from the
WR (the first 183 control frames of the third room, or rooms 1–2, which fix the entry frame).

## P2.1b-m2 (session 4, later): goombas in the model, search controls, pole mechanics, whole-room attempts
**Goomba model.** `room_enemies` in the smb-opt patch (F58/F59 as corrected): positions are functions of the
frame, collisions only on odd model frames (even `FrameCounter`), boxes from `BoundBoxCtrlData`, stomp iff
Mario's integer y speed > 0 (or a stomp already happened this frame), else death; stomped goombas are
harmless. Validation (`tools/model_difftest.py --goombas --root-record 1048`, all after the parser fix that
made comparisons run to full length): random 200×120 (22,444 frames, 58 matching deaths), WR-mutated 300×100
(28,816 frames, 45 deaths), WR-mutated 200×245 to the pole (46,075 frames, 37 grabs of which 35 glitch grabs,
16 deaths) — **0 differences**. `trace11room` reproduces the WR's stomp at step 65 and grab at step 239 with
no injection.

**Search controls (exact model, root = WR state after step 223, frame 1269).** `--check-path`: the WR's own
continuation is in the frontier at every layer; with 15 steps (deadline = one before the WR's grab) no grab
exists (11,921 states); with 16 steps the search finds 2,256 glitch grabs at step 16 including the WR's
exact grab state (x 0xc5950, y 0x1a4b0). So the engine finds the WR's grab when it is inside the horizon and
reports nothing when it is not — the earlier "no grab ≤ 1284 from root 1229" stands, and the earlier horizon
arithmetic was right (step 239 of the room = the 56th step from root 1229; max_steps 55 = "strictly earlier").

**Pole mechanics (from the validated trace, F60).** The WR does not "embed and creep": it jumps off the
staircase top (step 184, rising to Y 20), falls at 4 px/f, is stopped by the pole's base block at X 3157–3159
(steps 233–235), lands on the ground *beside* the block at X 3158 (step 236: left foot on the ground column
197, right foot over the block), presses A+L+R (step 237: the jump is slow because x_spd_abs was reset by the
L+R friction; moving_dir flips to LEFT so the block's side push goes *right*), and touches the pole while
rising at Y 164 (step 239, lower side point in row 9). Why nothing simpler works: on the ground the lower
side point (Y+24) is in the base block's row for X ≥ 3155, so running in is impeded at X 3154; a jump from
≤ 3154 rises through the Y 162–167 window with the wrong X nibble; landing on the block top (Y 160) grabs
at Y 160 = not a glitch grab (F55). Hence a glitch grab needs a landing on the ground at X ∈ [3155, 3162]
*from above*, i.e. the 128-px descent from the staircase must end at the pole; running off the top edge
lands at X 3130 (model trace of a B+R-only variant: on the ground at 3130 by step 222, stuck at 3154 from
step 232). The WR's jump lengthens the descent from 36 to 53 frames to land at 3158.

**Where the WR stands.** The exact x-table says x ≥ 3158 at step 233 earliest from the entry; the WR is at
x 3033 at step 183 = exactly 50 frames (125 px at 2.5 px/f) before that: **the WR is on the x-frontier at
the staircase top**, and from its stairs-top state the pole approach is exhaustively optimal (F57). A
1-frame gain in the room therefore needs a *different stairs-top handoff* (y fraction, speed fraction,
timers, facing — everything but x) that lets the descent end at X 3155–3162 one frame earlier (H28).

**Whole-room attempts.** Exact model, no goombas, `ulimit -v 9.5G`: died after layer 45 (frame 1090; 25.4M
unique, 87M visited, ×1.5/layer). Relaxed model + goombas, `ulimit -v 11G`: layers 10/20/30/40/45/48 =
10.3k / 102k / 787k / 1.68M / 11.5M / 27.1M unique (107M visited), ×1.3/layer, died at layer 48–49 (frame
1093, 6:05, 6.4 GB RSS). The growth is the product of the x-slack window (≈12 px × 16 subpixels), the
speed fraction (64), the y fraction (256 — it persists across landings and is the lever for the final
descent timing) and air phases — a saturated frontier of order 10⁸ states per layer (estimate). With the
present engine (global visited set, 48-byte frontier states) that is ~100 GB; with a per-layer compact
frontier (10-byte keys + decompressor, parents on disk) it is 2–5 GB per layer and ~4×10¹¹ model steps ≈
10 core-hours — feasible on this box with parallel expansion, or on a cloud box with the current engine
(~64 GB). Sound extra bounds worth adding first: "on the ground at X ≥ 3155 by step 236" (the landing law
above) in place of "x ≥ 3158 by 238", and the goomba-relevance keying already in place.

## P2.1b-m3 (session 4, end): compact per-layer engine, scaling probes, whole-room run
**Engine (`bfs11c` / `bfs11cr`, smb-opt patch).** Frontier = sorted, deduplicated `Vec<[u8;16]>` records
(compressed player key 10/11 B ‖ goomba bits ‖ padding); parents are decompressed (`CompressedState::
decompress`, exact inverse — checked on the root), expanded in parallel (`--threads`, std threads, chunked
sort+dedup per thread, final sort+dedup), dedup per layer only (no global visited set; the deadline bound
keeps it finite); each layer's records optionally written to `--layer-dir/layer_NNN.bin` for offline path
reconstruction. Controls from frame 1269 (exact model, goombas): 15 steps → no grab, layer counts identical
to the global engine through layer 13 and slightly larger after (per-layer dedup re-admits earlier-layer
states); 16 steps → the same 2,256 glitch grabs. The in-binary backward reconstruction failed: the bit
packer is LSB-first, so the byte order of the records is not the x order my window search assumed — a
`bfs11c-path` offline mode (full scan per layer, threaded) is the fix, not yet written.
**Heuristic fix.** MrWint's `XPos/YPosHeuristic` panic on substates outside their precomputed tables; the
goomba bounce (y speed −4 with a non-jump fraction) creates such y-substates (slack-1 probe died at layer 64,
frame 1109). Unknown substates now return 0 (admissible, sound).
**Slack scaling (global engine `bfs11r`, relaxed + goombas, from the entry).** Deadline 234 (1 frame of
x-slack): layers 10/20/30/40/50/60/63 → 63 / 285 / 146 / 10.7k / 552k / 1.77M / 2.05M unique, 1:08, 1.4 GB
(then the heuristic panic); deadline 235: 340 / 1.8k / 2.5k / 83.6k / 3.9M (50) / 10.1M (57). Deadline 238,
compact engine, 12 threads: 0.99M (40) / 3.5M (43) / 5.1M (44) / 7.3M (45) / 14.0M (47, 152M children, 22 s)
— growth ×1.45 → ×1.36/layer, decelerating slowly; ~100 MB of layer files per 6M states. Even one frame of
slack leaves millions of states per layer in the goomba/flat section: the variety is not the y fraction
(cleared at every jump start) but jump phases × speed fraction × x subpixel.
**Run at session end.** `runs/P2.1b-model/room_run.sh` (restarted on the fixed binary ~23:40): relaxed model
+ goombas, deadline 238, 12 threads, `ulimit -v 13.5 GB`, layer files on (210 GB free). Expected: either a
memory/disk stop (record the last layer) or, after hours, a verdict on H28 in the relaxed model.
**Path reconstruction (`bfs11c-path`, later the same session).** Offline, from the layer files: for the target
record of layer l, scan layer l−1 (threaded), step every record with the 16 inputs (+ goombas at frame
root+l) and take the first whose child equals the target; repeat to layer 0. Control: from the 16-step run's
"grab parent record" the reconstructed 16 inputs ([6 NIL, 2 L, 2 NIL, L, NIL, R, A|L, A, R]) appended to the
WR's 224 records replay in the exact model to a glitch grab at step 239 (X 3161, Y 164).
**Run 2 (12 threads, `ulimit -v 13.5G`)** died after layer 50 (frame 1095; 28.8M unique, 346M children/layer,
44 s/layer, growth ×1.27 → ×1.23) with "memory allocation of 512 MB failed" at only 4.5 GB RSS: the
virtual-memory cap counts the 12 threads' pre-reserved 256 MB chunk buffers and the doubling vectors.
Run 3: chunk buffers 64 MB, no virtual cap, an RSS watchdog (kill when MemAvailable < 1.5 GB) instead.

**Run 3 outcome (2026-08-22 01:51).** Reached layer 85 (frame 1130): 328,231,873 unique states, 4.84 G children
generated, 287 M pruned, 741 s/layer; frontier growth had slowed to ×1.024/layer (77→85: 260.6 / 272.1 / 282.9 /
292.2 / 300.6 / 307.2 / 313.1 / 320.4 / 328.2 M). Killed by the **kernel OOM killer** (not the watchdog) at 11.1 GB
RSS during layer 86 — and the system-wide pressure also took the Claude Code session down. Cause, from the code: per
layer the engine held the parent vector (5.2 GB), 12 unbounded per-thread accumulators (~6 GB), 12 × 64 MB chunk
buffers and then the merged `next` vector (5.4 GB, `Vec` doubling spikes up to 2×) all at once — ≥ 15 GB at n =
328 M. A 10-second polling watchdog on MemAvailable cannot catch a multi-GB allocation burst. Layer files 0–85 are
intact (`room_layers/`, 0–83 xz-compressed, 84–85 raw).

**Run 4 engine (external-memory `bfs11c`/`bfs11cr`, same source file, 2026-08-22).** Same search semantics
(per-layer dedup of 16-byte records, same bounds, same goomba model), different storage:
- parents are streamed from `layer_NNN.bin` in 64k-record blocks (`read_at`), never held in memory;
- each thread's children go through a 2 M-record chunk buffer (sort+dedup) into an accumulator that is re-sorted every
  2 chunks and **spilled to disk as a sorted run** (`room_layers/tmp/run_SSS_TT_KKK.bin`) when it exceeds
  `--acc-mb` (256 MB);
- the runs are k-way merged (binary heap, dedup on output) straight into `layer_{NNN+1}.bin`; the runs are deleted;
- `--resume N` restarts at step N from an existing raw `layer_N.bin`; `--stop-step N` stops early without changing the
  deadline; the in-binary backward reconstruction (which was broken, LSB-first key order) is gone — `bfs11c-path`
  is the reconstruction tool and the log prints the exact command;
- the heuristic tables use an FxHash hasher instead of SipHash (`heuristics::FxMap`; two lookups per child).
Memory ≈ threads × (acc 256 MB + ≤ 3 chunks 96 MB) + merge buffers ≈ 4.5 GB at 12 threads, independent of the
frontier size. Launched under a **cgroup hard cap** (`systemd-run --user --scope -p MemoryMax=10G
-p MemorySwapMax=512M`, `runs/P2.1b-model/room_run_resume.sh`): an overrun now kills only the search.
Controls: (a) the 16-step control from frame 1269 reproduces every layer count of `root1269_d16_compact.log`, the
2,256 grabs and the same grab parent record (9 s, 82 MB RSS); (b) `--resume 40 --stop-step 46` on run 3's
`layer_040.bin` reproduces run 3's unique/generated/pruned counts for layers 41–46 exactly, 1.8× faster per layer
(0.8/1.3/2.0/3.0/4.4/6.3 s vs 1.5/2.4/3.7/…), 649 MB peak RSS at 10 M states. Run 4 = resume from layer 85 (log
`room_compact_d238_r85.log`): layer 86 = 337,539,988 unique (×1.028), 4.96 G children, 495 s (expand 467 s, merge 28 s of
24 runs) vs run 3's 741 s at layer 85; anon RSS ~3.2 GB, cgroup peak 6.6 GB including page cache.

## Segment comparison with MrWint's optima (F63)
| segment | WR (dump rows / model steps) | MrWint case (enemy-free, relaxed) | gap |
|---|---|---|---|
| 1-1 room 1: control → vertical pipe entry | rows 197 → 565 = 368 | `W11VertPipeEntry` 368 | 0 |
| bonus room: control → side pipe entry | rows 656 → 767 = 111 | `W11SubSidePipeEntry` 111 (11 or 13 coins) | 0 |
| third room: control → x ≥ 3009.06 | step 174 | `W11PipeSpeedup` 174 | 0 |
| third room: stairs → glitch grab | steps 174 → 240 = 66 | `W11Flag` 59 from x 3029 ≈ 8 frames later ⇒ 67 | WR −1 |
Entry states match (X 40 / X 24 / X 2616, speed 0). The bonus room has no enemies, so its 111 is a proof
of optimality for the real game (relaxation argument). Rooms 1 and 3 can only gain through enemy bounces
(H29) or a different stairs handoff (H28, running).

## The stairs-top seam census (2026-08-23, session 11, run 4 layer 183 as a byproduct)
How large is the handoff space MrWint's two-segment chaining collapsed to one hand-picked state (F52)?
Run 4's layer 183 (= frame 1228, the stairs-top step; relaxed model + goombas, deadline-238 bound) was
decompressed and scanned (`tools/layer183_census.py`; decode validated by the log's own max-x checksum —
the 16-bit x field decodes LSB-first, max 0xbd79 = the log's `max x 0xbd790`): **660,614,902 unique states,
every one of them within x ∈ [3016, 3032) — the entire surviving frontier is a 24-px window before the
pole — and 407,420,836 of them are in the stairs-top handoff window x ≥ 3024.** So the hand-chained seam
chose 1 state out of ~4×10⁸ deadline-feasible candidates (relaxed-model overcount of exact reachability;
one step of the 183–186 handoff range). F57 proved the pole approach optimal *from the chosen state*;
run 4 is the search over all of them — this census is the measure of exactly what that buys.

## Run 4 verdict (2026-08-24): H28 refuted — no glitch grab ≤ step 238
Run 4 (resumed from layer 85 on 2026-08-22, ~1,100 s/layer at 12 threads, paused twice for the 4-2 work) reached
layer 234 at 07:20 with the first grabs of the run — **33,281,312 grab transitions, every one at Y 160–161**, F55's
slow band (non-FPG: they wait ~47 frames for the flag) — and was OOM-killed two minutes later expanding layer 235
(10G peak against its cap). Recovery: `--resume 233` runs died the same death twice (silently, mid-235); `--resume
233 --stop-step 234` recovered the grab table; and layers 235–238 were searched by slicing the 216,599,395
layer-234 records into 8 disjoint parts and resuming each (`fpg_parts_driver.sh`, ~3 min/part): **every part's
earliest grab is non-FPG (steps 235–238) and no fpg=true row exists**. Soundness of the slicing: a layer expansion
is a function of its parent set; each parent lives in exactly one slice, so every path onward from layer 234 is
explored by some slice — the union of grab tables is complete for grab existence (cross-slice dedup loss only
repeats expansions). Layers 0–233 logged `grabs 0` throughout. Since the relaxed model + goombas over-approximates
the real game, the absence is a proof: **the 1-1 third room cannot save the framerule (H28 refuted, F116)**; with
rooms 1–2 at their enemy-free optima (F63), the bonus room proven, and the pole approach from the stairs top
proven (F57), **1-1 is closed** except H29 (bounce-assisted room 1, parked, low prior). Memory lesson for the
engine: grab bookkeeping is unbounded in the pole zone — a loose end (`bfs11cr`: count non-FPG grabs, key only
FPG ones). Layer files (71 G + 7 G of slices) are re-derivable; the logs are the search record.
