# P2.3e — the next framerule level: scan of 1-2, 4-1, 8-3

**Session 17, 2026-08-24.** Opened after the user challenged the campaign's allocation, and was right.

## 0. Why this unit exists — the allocation argument, in this project's own numbers

Two lines from `STATUS.md` "Key numbers", which have been sitting there since P0:

> **Per-level slack/deficit (frames):** 1-1 20/**1** (closed end to end), 1-2 13/**8** (5 on Maru's route, S),
> 4-1 12/**9**, 4-2 8/**13**, 8-1 3/18, 8-2 2/19, 8-3 11/**10** (FPG+242: 7, S), 8-4 unquantized
>
> **WR vs MrWint segment optima:** gap 0 on all 10 solved segments (1-1 x4, **1-2 opening**, 8-4 x5)

And F52, recorded in P0.8 and never acted on:

> Prior exhaustive-search tool: MrWint/smb-opt is a **player-physics-only** model (enemies ignored) used for
> **segment** searches from **hand-picked states**. **No whole-level or full-state search exists anywhere.**
> Best-known 1-2 (**Maru370 2024**) is **5 frames** from the next framerule.

Put together:

1. **The WR is not optimal, and that is already documented.** Maru370's 2024 1-2 is **3 frames faster than
   HappyLee's** (H22/F52). The 2011 WR is beatable on a level someone already beat it on.
2. **The levels we have spent the campaign on are the two that were already solved.** 8-4 is where MrWint
   solved five segments; 4-2 we closed ourselves. Meanwhile **4-1, 8-3, 8-1, 8-2 and all of 1-2 past its
   71-frame opening have never been exhaustively searched by anyone.**
3. **Our tooling is the thing F52 says does not exist** — whole-level, full-state, enemy-aware search with
   admissible bounds and core-verified replays. It was pointed at the pre-solved levels.
4. **Calibration:** the one time it was pointed at an unoptimised route (4-2's top route) it found **35
   frames** of movement (553 vs 588). That died only because 4-2's wrong warp charges a ~31-frame key tax
   (F123). **1-2, 4-1 and 8-3 have no such tax** — a frame found is a frame banked against the deficit.

**Order: 1-2 first** (cheapest deficit at 5, and unsearched), then 8-3 (7 with FPG+242), then 4-1 (9).

## 1. 1-2 — the scan

### 1.1 Frame accounting (fceux RAM rows, `data/wr/fceux_wr.ram`)

| event | row | note |
|---|---|---|
| 1-1 → 1-2 level transition | 1944 | AreaPointer `$29`, AreaNum 1 (the pipe intro) |
| intro area, GES 7 | 2109 | x 40 |
| enters the main area | 2443 | AreaPointer `$c0`, AreaNum 2 |
| **main area control frame (GES 7)** | **2469** | x 40, **Y 32** — Mario drops in from the entry pipe at the top |
| GES 8, normal play | 2486 | |
| area-change command parsed | 2711 | AreaPointer → `$c2` |
| area-change command parsed | 3435 | AreaPointer → `$25`, at x 2337 |
| **warp pipe entry (GES 3)** | **3766** | x 2860, Y 128 → World 3 / Level 0 = **4-1** |

**Main area = rows 2469 → 3766 = 1297 frames**, one span, ending in a vertical pipe entry. Note the warp
zone is **part of the same area** (AreaNum stays 2 to the end) — there is no intermediate room to seam at.

### 1.2 Block map — ALREADY DONE AND VERIFIED

`tools/blockmap_from_dump.py data/wr/fceux_wr.ram 2469 3766 --compare third_party/smb-opt/src/blockbuffer/world1.rs BB12`
→ **`0 differing cells over the 176 read columns 16..191`**.

MrWint's `BB12` is byte-correct against our own dump. **No block-map work is needed** — the one gap is that
our dump reads 200 columns and `BB12` is 192, so the warp-zone columns **192..199 must be appended** before
a case can reach the goal pipe.

### 1.3 Enemies — `E_UndergroundArea1` (tools/area_data.py)

| id | what | modelled? |
|---|---|---|
| `$06` | goomba (x5) | **yes** (`CLASS_GOOMBA`) |
| `$38` / `$39` / `$3B` | goomba/koopa **groups** (the `$37..$3f` range) | **yes** (`process_enemy_data`) |
| `$00` | green koopa | **yes** (`CLASS_KOOPA`) |
| `$0d` | piranha plant | **yes** (`CLASS_PLANT`; and F136 — every vertical pipe bears one) |
| `$03` | **red koopa** — `InitRedKoopa` sets state 1 (does not walk off ledges) | **NO — new class** |
| `$27` | lift/platform (x2, page 9) | partial — 4-2's lift is a bespoke hook (`w42_lift_u16`), not a class |
| `$26` | **large platform** (`RunLargePlatform`) (x2, page 10) | **NO — new class, and Mario stands on it** |
| `$34` | **`WarpZoneObject`** — sets `WarpZoneControl` when Mario enters the zone | **NO — and it is the goal condition** |
| `$2e` | `PowerUpObject` — spawned, never collected (PlayerStatus stays 0 all level) | refuse the bump, as 4-2 does |

**All of them are actually met by the WR** (first-seen rows: `$00` 2725, `$03` 3358, `$06` 2469, `$0d` 3062,
`$26` 3410, `$27` 3315, `$2e` 2565, `$34` 3470), so none can be waved away as out of range.

### 1.4 Model gaps and shape

- **Small Mario the whole level** (`PlayerStatus` = 0 at every sampled row) — so `Small` + `NoPowerups`, and
  the `$2e` mushroom is an item-bump refusal exactly like 4-2's (H34 pattern).
- **No RNG.** Nothing here is LFSR-driven (contrast 8-3's Hammer Bros).
- **x is at ~93% of the 2.5 px/frame cap for the whole level** (2903 px in 1250 frames), so the deficit is
  **not** in running faster. It is in the route: the ceiling run, the warp-zone entry, and the pipe timing.
  That is exactly the shape a whole-level search finds and a human does not.
- Port cost is comparable to the 8-4 room-2 port done this session (one new walker class + two platform
  classes + the warp-zone goal object), and the enemy engine is now area- and terrain-parameterised, so it
  is reuse rather than a rewrite.

### 1.5 Next steps for 1-2, in order

1. Append `BB12` columns 192..199 from the dump (`--rust BB12`) so the map covers the warp zone.
2. Case `W12Warp`: control frame = row 2469 (x 40, Y 32, AreaPointer `$c0`), goal = the world-4 pipe entry
   at x 2860; `Small`, `WithRunningTimer`, `WithScrollPos` + block states (the enemy hook needs both).
3. `--check-path 1297` reference audit: WR line exact, 0 bound violations, before any search.
4. Enemy port: red koopa `$03`, large platform `$26`, the `$27` lift as a class, `WarpZoneObject` `$34`.
   Difftest to 0 over the WR's 1297 frames + a random battery (TMPDIR on /home, capped — runbook §1).
5. Segment the level at standable surfaces (runbook §3) and ladder each segment — the 4-2 pattern.

## 2. 8-3 and 4-1 — not yet scanned

Deferred until 1-2's case is exact on the WR. Known going in: **8-3 has Hammer Bros, which are RNG-driven
and the model has no RNG** (that is the reason it is not first despite the 7-frame deficit with FPG+242);
**4-1 (deficit 9)** has no known blocker and is the fallback if 1-2's port turns out bigger than budgeted.

---

# Part 2 — the `W12Warp` case is built and tracks the WR for 919 of 1280 frames

## 3. What shipped this session

**`BB12W` — 1-2's map including the warp zone (200 columns).** MrWint's `BB12` stops at 192, eight columns
short of the warp pipes. Built as `BB12` (cols 0..191) **plus** the dump's cols 192..199, because the
reconstruction can only read columns the parser had not already passed — it read 16..191 and agreed with
`BB12` on **every one of those 176 columns**, so the merge is `BB12` + the tail, not a re-derivation. (First
attempt used the raw reconstruction and Mario fell through the floor at x 40: cols 0..15 were never read and
came out as zeros. Worth remembering — the tool's "0 differing cells over the **176 read columns**" is a
statement about what it read, not about the whole map.)

**`W12Warp`** (`case/w12.rs`): control -> the world-4 warp pipe, `Small`, `WithRunningTimer`, `WithScrollPos`,
`WithBlockStates`, `EnterVerticalPipe<U178, U8>`, goal gated on `screen_left_abs >= 2816`, bound =
`build_overshoot_bound(2928, 2860)`.

**Start frame: fceux row 2486, not the GES-7 control frame at 2469.** The GES 7 -> 8 handoff **duplicates a
frame** — rows 2485 and 2486 are byte-identical — so a case rooted at 2469 runs one frame ahead of the core
from there on. Mario is mid-fall at 2486, so the start carries `y_pos 0x13068` and `y_spd 0x258`. Span =
3766 - 2486 = **1280 frames**. (`iterate_entrance` is wrong here: 1-2's entrance animation is not 4-2's and
iterating it lands 5 frames into the fall.)

**Block states:** 1-2 has **496 plain bricks** (bounce only) and exactly **11** cells that change metatile —
the five question blocks of the opening row at (10..14, 7) plus six item bricks — so the 16-bit `used` mask
fits with room to spare.

**Enemy hook `w12_enemies`:** the same engine as 4-2 and 8-4, with 1-2's `E_UndergroundArea1` bytes, six
pipes enumerated per F136 (cols 103, 109, 115 and the three warp pipes 178/182/186), and `BB12W` terrain.
Start ext = the two goombas already walking in at x 247 / 263 plus the loader at `EnemyDataOffset` 4.

## 4. Difftest progress against the core, measured

| build | frames exact | stopped by |
|---|---|---|
| case, no enemies, raw reconstruction map | 1 | cols 0..15 unread -> Mario falls through the floor |
| + `BB12` merged in | 33 | the GES 7->8 duplicated frame (rerooted 2469 -> 2486) |
| + rerooted at 2486 | 90 | **a goomba stomp** (core `Enemy_State` 0 -> 4, Mario's y speed +4 -> -4) |
| **+ the enemy hook** | **919** | **a `$27` LIFT at x 2236** — Mario rides it |

**919 of 1280 frames (72%) exact, with 5 stomps reproduced**, on a level that has never been searched by
anyone. The remaining 361 frames are blocked on one thing.

## 5. The next blocker, precisely

At fceux row 3405 Mario **lands on a moving platform**: the core carries two `$27` objects in slots 2 and 3,
both at x 2236, one descending through y 195 and one at y 35 — 1-2's **elevator pair**. `class_of_id` has no
`$27` class (4-2's single lift is a bespoke hook, `w42_lift_u16`, not a class), so it falls through to the
inert `CLASS_ITEM`: right for slot occupancy, wrong for motion, and Mario cannot stand on it.

So the port order for 1-2 is: **`$27` small platform (elevators) -> `$26` large platform -> `$03` red koopa**.
The `$34` `WarpZoneObject` needs no class — its effect is the case's `screen_left_abs >= 2816` goal
condition.

## 6. The number that makes this worth finishing

`build_overshoot_bound(2928, 2860)` reports **30,720 end classes with 7 distinct return costs, [39..45]**.
The WR's own return — from the frame the scroll arms the warp zone (row 3720, x 2944) to the pipe entry
(row 3766) — is **46 frames**. The cheapest end class returns in **39**.

That gap is 7 frames, against a deficit of **5** for the framerule (Maru's route) or 8 (the WR's). It is the
same shape as F133's 8-4 room-3 question — *is a cheap end class reachable at the right step?* — except that
here the answer has never been looked for, and the bound's floor is 7 below the WR rather than 1.

**Caveat, stated up front:** that 39 is an **x-only** return cost — it ignores the vertical part of the
turnaround, exactly the coupling that cost 14 frames of slack in 8-4 room 2 (F137). So it is an upper bound
on the prize, not a promise. The honest reading is "worth the port", not "7 frames are there".

---

# Part 3 — the case is exact on all 1280 frames, and two real engine bugs fell out of it

## 7. Finishing the port

| object | what it needed |
|---|---|
| `$27` / `$26` | **`CLASS_LIFTPLAT`** — `LargePlatformSubroutines` sends both to `MoveLargeLiftPlat`, so one class covers them. Motion is `YMF_Dummy += Y_MoveForce; Y_Position += Y_Speed + carry` (10982) with Y wrapping in the byte and `Enemy_Y_HighPos` untouched. Init: `PlatLiftDown` ($27) = force `$f0` / speed `$00`; `PlatLiftUp` ($26) = `$10` / `$ff`; both then `PosPlatform` with Y = 1, which adds **12 px to the X**, not the Y. Mario meets it through `LargePlatformCollision` + `PositionPlayerOnVPlat`, never `PlayerEnemyCollision` — the collision flag uses the platform's **pre-move** position and the placement its **post-move** one. **4-2's lift is the same object**, so its validated collision code was reused verbatim. |
| `$03` | **`CLASS_REDKOOPA`** — `InitRedKoopa` = `InitNormalEnemy` + `Enemy_State = 1`, and `ChkForRedKoopa` (12550) turns it around instead of walking off a ledge, then `rts`es (no state update, no side check). |
| `$34` | no class: its effect is the case's `screen_left_abs >= 2816` goal condition. |

## 8. Two engine bugs the new level exposed

**(a) `CheckpointEnemyID`'s +8 is for ids < `$15` only.** The loader places an object at `row * 16`; the
`adc #$08` and `EnemyOffscrBitsMasked = 1` sit *after* a `cmp #$15 / bcs InitEnemyRoutines`, and the ROM's
own comment says "($00-$14 only)". The model applied both unconditionally, which put 1-2's elevators **8 px
below** the core's. Platforms, powerups and vines are all affected.

**(b) Bounding boxes were never clamped at the screen edges — and this one was live in 4-2 and 8-4 too.**
Boxes are screen-**relative** bytes, so an object near an edge has a box that wraps. `CheckRightScreenBBox`
/ `CheckLeftScreenBBox` (12891) push such a box fully offscreen (`$ff` right, `$00` left) so it cannot
collide with something at the opposite edge. Without it, **two goombas 250 px apart turned each other
around** — 18 of 150 trials in the first 1-2 battery, every one of them a phantom stomp. Fixed for enemy
boxes and the player's.

## 9. Validation

| control | result |
|---|---|
| WR's 1280 records vs the core | **1280/1280 exact**, 6 stomps, `StateChangeVerticalPipe(178,8)` = core GES 3, same frame and Y |
| 1-2 random battery (`--n 150 --len 1280 --arun 14 --require-event`) | **150 trials / 96,424 frames, 0 differences**, 203 stomps, 24 kicks, 102 matching deaths |
| reference-path bound audit (`--check-path 1280`) | **GOAL at step 1280, 0 bound violations**, root bound 1213 vs 1280 |
| 4-2 control gate | exact (6/16/34/70/134/673/3472/16472/69489/257001) |
| 4-2 enemy chain | 596/596, 0 mismatches, 2 stomps, pipe entry at record 7179 |
| 8-4 room 2 (WR + 400-trial battery) | 401 trials, **0 differences**, 241 stomps, 16 kicks |

## 10. The ladder — 1-2's last 60 frames are proof-grade optimal

The bound's slack along the WR, from the `--check-path` profile (`runs/P2.3e/wr_hprofile.txt`):

| WR step | x | h | true | slack |
|---|---|---|---|---|
| 1200 | 2884 | 58 | 80 | 22 |
| 1220 | 2933 | 39 | 60 | 20 |
| 1230 | 2943 | 46 | 50 | 4 |
| **1234** | **2944** | **46** | **46** | **0** |
| 1240..1280 | | | | **0** |

**The bound is TIGHT for the last 46 frames** (step 1234 = x 2944, the overshoot apex where the scroll arms
the warp zone). An admissible bound that equals the true remaining is a proof: **no path from the WR's
step-1234 state reaches the pipe in fewer than 46 frames**, with no search at all.

Then the exhaustive rungs, rooted on the WR's own state (a goal would be a genuine improvement on it):

| rung | horizon | result |
|---|---|---|
| prefix 1230, **d49** | 49 | **DRY**, frontier extinct at layer 4, 10.6 s |
| prefix 1220, **d59** | 59 | **DRY**, extinct at layer 14, 12.5 s |
| prefix 1210, d69 | 69 | abandoned — 8.9M states at layer 14 and growing |
| prefix 1200, d79 | 79 | abandoned — **101M states at layer 25**, 28 GB of layers |

**So the WR's last 60 frames of 1-2 cannot be improved by one frame.** The wall is terrain, not slack: at
step 1220 Mario is already inside the narrow warp-zone corridor (x 2933) and the frontier suffocates; at
1200 (x 2884) he is in open ground and it explodes.

## 11. Where this converges with 8-4

Both levels now stall for the *same* reason: an **x-only bound that cannot price a turnaround**. 8-4 room 2
loses 14 frames to "must land before jumping"; 1-2 loses ~22 to the vertical part of the warp-zone
overshoot. The **coupled end-game term** queued as `P2.2f-bound` is therefore not a one-level fix — it is
the single piece of machinery that would push both ladders deeper, and it is the highest-value engineering
item on the board.

## 12. The wall clip — where 1-2's bound slack actually goes, and a first-arrival-gate near-miss

The slack profile has one sharp feature. Slack is **55 from step 40 to ~500** and **54** until ~1080 — i.e.
the bound and the truth advance in lockstep for a thousand frames, because Mario is at the **speed cap
(x speed 40) for essentially the whole level**. Then between step 1080 and 1120 the slack collapses
**54 -> 23**: the WR spends 40 frames where the bound expects 9.

That stretch is 1-2's **wall clip**. At step ~1087 Mario runs into the exit-pipe column (`BB12W` cols
168/169 are pipe from row 0 to 6, cols 170..176 are floor-to-ceiling brick), his x speed drops 40 -> 0, and
his x then creeps 2677 -> 2700 over ~36 frames while the speed ramps back up. The model reproduces it
exactly — it is inside the 1280/1280.

### The near-miss, recorded because it is the trap this project keeps documenting

A position-goal probe from the WR's step-1080 state, `--goal-x 2700 --goal-y 64`, found a goal at layer
**33** where the WR needs **42**. Reconstructed and **core-verified: 33/33 frames, 0 mismatches**. Nine
frames, apparently, on an unsearched level.

It is worth nothing. The arrival state is `x 2700, Y 61, x speed ~0, JUMPING, moving LEFT, side-collision
timer 15` against the WR's `x 2700, Y 64, x speed 21, STANDING`. Continuing from it to the next milestone
(`x >= 2784 && Y <= 64`) is **pruned at layer 1** — the admissible bound alone proves it needs **>= 47**
frames, which is exactly what the WR pays from *its* step-1113 state. The nine frames are paid straight
back.

This is F115 / H39 again, at the finest possible granularity: **a first-arrival gate on a scalar is not a
seam.** The gate said "9 frames ahead"; the physics said "same cost, worse state". Every position-goal
result has to be continued before it means anything.

### What the same probe does establish

From the WR's own step-1113 state, that milestone is **dry at deadline 46** (exhaustive, frontier extinct
at layer 16) and **reached at 47** — and the WR pays 47. **So the WR is optimal on that stretch too.**

*(Correction worth recording: the first reading of this called it "47 vs the WR's 48" and therefore a frame.
That came from an off-by-one in the fceux-row -> model-step mapping — model step k is fceux row **2486 + k**,
so the WR's arrival is step 1160, 47 frames from 1113, not 48. Always take the reference costs from the
model's own `--check-path` step numbering, never from a hand-mapped RAM row.)*

---

# Part 4 — the other two candidates, scanned

## 13. 8-3 (deficit 10, or 7 with FPG+242)

| | |
|---|---|
| span | GES 7 at fceux row **13121** (x 40, Y 176) -> flagpole grab (GES 4) at **14489** (x 3417, **Y 17** — a high grab) = **1368 frames** |
| ends at | a **flagpole**, not a pipe — goal is `StateChangeFlag`, the 1-1 pole machinery (P2.1b), not `EnterVerticalPipe` |
| enemies the WR actually meets | `$00` green koopa **(modelled)**, `$0d` piranha plant **(modelled)**, **`$0e` jumping paratroopa — modelled THIS session for 8-4**, `$2e` powerup (inert), `$30`/`$33` `NoRunCode` (inert), `$31` `RunStarFlagObj` (the flagpole itself) |
| **the blocker** | **`$05` Hammer Bro**, first met at row 13450 (x 1008). Its behaviour is LFSR-driven and the model has no RNG. This is why 8-3 is not first despite the 7-frame deficit. |

Note the 8-4 port already paid part of 8-3's bill: the `$0e` paratroopa class it needed is exactly the one
built for room 2 (F135). What remains is the Hammer Bro, and F130's lesson for the cheep frenzy applies —
before assuming RNG makes it impossible, check at code level how the randomness actually enters, because
for the cheeps it turned out to be a **3-valued class**, not a branch.

## 14. 4-1 (deficit 9)

| | |
|---|---|
| span | GES 7 at fceux row **3980** (x 40, Y 176) -> flagpole grab (GES 5) at **5424** (x 3593) = **1444 frames** |
| ends at | a **flagpole** |
| existing assets | **none at all** — there is no `w41.rs` and no `BB41` in MrWint's tree. The block map has to be reconstructed from the dump (`tools/blockmap_from_dump.py`), which is exactly what worked for 1-2's warp-zone tail. |
| blockers | none known — no RNG-driven object on the route |

4-1 is the largest build of the three (no case, no map) but the only one with **no known modelling
blocker**. It is the fallback if the Hammer Bro proves expensive.

**Its block map extracts cleanly, and the recipe matters:** start the window at the **area load row (3816)**,
not at the GES-7 control frame (3980). The reconstruction can only read a column the first time it is valid
with no parser task running, and by 3980 the parser has long since passed columns 0..15 — starting there
leaves them zero, which is exactly the bug that made Mario fall through the floor in 1-2 (§7). From 3816 the
floor comes out solid from column 0. `tools/blockmap_from_dump.py data/wr/fceux_wr.ram 3816 5424 --rust BB41`
is the command; the map is 232 columns and the staircase and flagpole are visible at cols ~200-226.

## 15. Scan verdict

| level | deficit | span | ends at | ever searched | blocker |
|---|---|---|---|---|---|
| **1-2** | **5** (Maru) / 8 | 1280 | warp pipe | **no** (only its 71-frame opening) | **none — DONE this session, exact end to end** |
| 8-3 | 7 (FPG+242) / 10 | 1368 | flagpole | no | Hammer Bro `$05` (RNG) |
| 4-1 | 9 | 1444 | flagpole | no | none known; needs a map and a case from scratch |

Order confirmed: **1-2 first** (done), then **4-1** (no blocker) unless the Hammer Bro turns out cheap at
code level, in which case 8-3's smaller deficit wins.

---

# Part 5 — three frames exist in 1-2's wall clip, and the scroll takes them back

## 16. The clip stretch is NOT optimal — 77 vs the WR's 80, core-verified

`bfscx W12Warp … 1080 79 --goal-x 2784 --goal-y 64` (exhaustive, no beam; the WR pays **80**):

> **goal reached at layer 77** (2,016,915 goal transitions), 469.7 s, ~19 GB of layers

And unlike F143's near-miss, the arrival is not a worse state — it **dominates** the WR's:

| field | ours, step 1157 | WR, step 1160 |
|---|---|---|
| x_pos | **0xae130** (x 2785.19) | 0xae0f0 (x 2784.94) |
| y_pos | 0x140b0 (Y 64) | 0x140b0 (Y 64) |
| x_spd | **0x28fc** (40 = cap, frac fc) | 0x28d8 (40, frac d8) |
| player_state | STANDING | STANDING |
| running_speed / running_timer | true / 10 | true / 10 |

Reconstructed and **core-verified: 77/77 frames, 0 mismatches**. So **three frames genuinely exist in 1-2's
wall clip** — on a level nobody has ever searched, against a deficit of 5 (Maru's route) or 8 (the WR's).

## 17. And they do not carry — because the warp zone arms on the SCROLL, not on Mario

Continuing from that state, the WR's own remaining inputs do not work verbatim (the trajectory drifts and
overshoots the entry window by 1 px), so the tail was re-searched:

1. From step 1157, reaching the WR's step-1220 milestone (`x >= 2933 && Y <= 128`) takes **exactly 60**
   frames — and `h(root) = 60 = the deadline`, so 60 is provably the minimum. That puts us at step **1217**,
   still **3 ahead**. Core-verified over the whole 137-frame segment: **0 mismatches**.
2. From step 1217, reaching the warp pipe in 60 frames — the cost the WR pays from *its* step-1220 state —
   is **DRY at layer 9**.

The three frames are refunded. The mechanism is structural and worth stating plainly:

> **`ScrollLockObject_Warp` arms `WarpZoneControl = 4` when SCREEN LEFT reaches its locked maximum 2816
> — a property of where Mario has BEEN, not where he is.**

At step 1217 we are at the same x as the WR is at 1220 (both x 2935) with far more speed (40 vs 22) and more
height (Y 107 vs 124) — but our ScreenLeft is **2806** against the WR's **2809**. Arriving three frames
early at the same place means the scroll is three pixels behind, and the scroll only advances when Mario
pushes it. **In 1-2, a frame banked before the warp zone is a frame lent to the scroll.**

## 18. The census WAS run, and the refund is not a pick artifact

Runbook §3.2 says never trust the auto-pick, and the binding quantity here is the scroll, not x speed — so
`smb-opt offset-census` gained a **`--sl`** flag that ranks a layer by **absolute ScreenLeft, descending**
instead of by scroll offset. (The two are different questions: offset is what 4-2's mint work cared about;
where a warp zone arms on the scroll, the state you want is the one whose scroll is furthest along, i.e. the
*lowest* offset at a given x.)

Censusing the carry search's layer 59 (step 1216, **639,657 records**) by ScreenLeft:

> max ScreenLeft16 = **2803**, at x 2932, offset 129

Our auto-picked continuation is at ScreenLeft **2806** one frame later — i.e. it came through the
scroll-maximal parent. **No state in that layer has a better scroll**, so "the three frames are refunded"
survives the check it was flagged for. It remains a statement about continuations of *this* clip path; one
of the 2,016,915 goal transitions at layer 77 might reach the milestone with a better scroll, and that is a
deeper search than has been run.

## 19. What is still open

Also measured and abandoned: the apex probe (`from step 1157, reach x >= 2944 in <= 74`, i.e. arm the scroll
three frames early) is exhaustively infeasible — **80M states at layer 21, 37 GB**, killed. Slack 10 in open
ground is past the wall.

## 20. The isolated frame at step 487 is a bound artifact, not an inefficiency

The loss map (§12) shows exactly one lost frame between step 34 and step 1084: **step 487**, x 1183, Y 129,
x speed **38** instead of the cap 40, mid-jump over the pit at columns 80..82. That looked like the cheapest
possible target — a single frame in an otherwise perfect thousand-frame stretch, in a region small enough to
search exhaustively.

Window chosen by the runbook's rule of cutting at standable surfaces: root = the WR's step **470**
(STANDING, x 1142, at the cap), goal = its step **516** (STANDING, x 1255, Y 112, at the cap), which the WR
covers in **46** frames.

> `bfscx W12Warp … 470 45 --goal-x 1255 --goal-y 112` -> `at root: Some(45)`, **no goal within 45**, 20.9 s

The root bound equals the deadline, so 45 was the floor and it is unreachable: **the WR's 46 is optimal
over the pit jump.** The frame the loss map showed is the x table's speed-class model being unable to credit
an airborne frame at speed 38, not a frame the route leaves on the table.

**1-2's openings are now down to two**, both listed in STATUS: a better-scroll clip path among the 2.0M
goal transitions at clip-layer 77, and the **intro area** (AreaNum 1, fceux rows 2109-2443), which nothing
has ever modelled.

## 21. 1-2's verdict

Sweeping the level end to end:

| stretch | frames | status |
|---|---|---|
| **intro area** (AreaNum 1, fceux 2109-2443) | 335 | **no frames exist** — `GameEngineSubroutine` is **7 for all 335 frames** and the WR's input records are **all zero**. It is a fully automatic entrance walk; input cannot change its length. |
| main-area opening -> x >= 145 | ~71 | MrWint's own `W12Speedup`, and F66 records the WR at **gap 0** against it |
| steps 34 -> 1083 | ~1050 | **x-optimal**: `h` drops by exactly 1 per frame the whole way (§12's loss map), i.e. the WR gains the maximum x the speed-class table allows on every single frame |
| the pit jump (steps 470-516) | 46 | **optimal** — d45 dry with the root bound equal to the deadline (§20) |
| **the wall clip** (steps 1080-1160) | 80 | **77 is achievable, core-verified** — but the scroll refunds the 3 frames (§16-18) |
| steps 1113-1160 | 47 | **optimal** — d46 dry (F143) |
| steps 1220-1280 | 60 | **optimal** — d59 dry, and the bound is tight for the last 46 (F142) |

**1-2 is closed for practical purposes.** The level that had the cheapest deficit on the board (5 frames on
Maru's route, 8 on the WR's) and had never been searched by anyone now has: an exact model of all 1280
frames, four proof-grade optimality results, one genuine 3-frame find, and a structural reason that find
cannot be banked.

**The single residue** is a scroll-aware search of the clip: 2,016,915 goal transitions reach the post-clip
milestone at layer 77, and only `bfscx`'s auto-pick (best by x speed) and its scroll-maximal parent were
followed. A search that *optimises the scroll* through the clip — rather than reaching a position gate and
checking the scroll afterwards — is the one experiment that could still bank those three frames. That is a
goal-function change, not more compute.

**And it re-prices Maru's 3 frames.** H22/F52 record Maru370's 2024 1-2 as 3 frames faster than the WR's.
Those frames are not in the intro (fixed), not in the opening (MrWint-solved, gap 0), not in the
speed-capped body (x-optimal every frame), not at the pit, and not after step 1113. **The only place left
for them is the wall clip — which is exactly where this session found 3.** That is a strong hint the two are
the same three frames, and that Maru's route pays the scroll back somewhere we have not modelled, or does
not pay it at all because the framerule absorbs it.
