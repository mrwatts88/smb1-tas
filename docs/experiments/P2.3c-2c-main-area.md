# P2.3c-2c — 4-2 main area: where the x-only bound is loose, and a top-route reference path

Date: 2026-08-22 (session 8, alongside run 4 — everything here is 2-thread, cgroup-capped, laptop-light).
Unit: P2.3c-2c (STATUS). Engine: `smb-opt bfscx` (P2.3c-1) + the `--goal-x/--goal-y` override added here.
Tools: `tools/bfscx_ladder.sh`, `tools/chain_inputs.py`, `tools/replay_check.py`.

## Question
`W42Main`'s bound is MrWint's x-only table (`XPosHeuristic`): 540 steps at the root (record 6584) against the WR's
588 and the framerule target 575 (= 588 − 13). A proof run with 35 frames of slack is hopeless (the frontier doubles
per layer, see below), so the main-area search needs a staged bound like the warp zone's (P2.3c-2). To design it:
(1) where along the WR is the x-only bound loose? (2) what does a *top* route cost in the model — HappyLee says 577
(F36), i.e. 37 above the x-bound; is that physics coupling the bound must learn, or something outside the model?

## Geometry (exact, `data/blockmaps/w42_main.txt`; cols → x = 16·col; standing on row r ⇔ Player_Y = 16r)
| feature | cells | notes |
|---|---|---|
| ceiling | row 0: cols 0, 6–56, 67–97 | gaps: cols 1–5 (entrance shaft), 57–66 (above the lift pit) |
| hanging brick mass | rows 1–3, cols 20–47; (43,2) coin brick | bottom at screen 96: under it Mario's Y ≥ 78 |
| pillar | rows 7–9, cols 22–26 (+ (27,7), (28,7)=mushroom brick) | **row 10 below it is open**: a 16-px floor-level tunnel, Y 176 |
| coin shaft | cols 27–29 rows 8–10 open, coins (27..29,10); (29,7) open | the bottom route's entry to the run |
| the run | rows 7–10, cols 30–47 (solid) | top surface Y 112 = the "top floor"; at floor level only a wall walk (F80) |
| question blocks | (50,3) (51,3); (50,7) (51,7) (54,7) (55,7)=mushroom (56,7) | row-7 blocks continue the Y-112 level with 2-block gaps at 48–49, 52–53 |
| lift pit | floor missing cols 57–62 (x 912–1007); lift `$27` at page 3 col 10 | ceiling gap above; lift spawns x 940 y 80 when Mario is at x ≈ 737 (F73, x-driven) |
| hidden blocks, vine ledge | (63,6) (64,5) (65,6) (66,7) `$5f`; (64,3)=vine brick, (65,3) (66,3) | hidden blocks are ignored by side/foot checks until bumped |
| pipe A | cols 72–73, rows 8–10 (top Y 128) | |
| bricks | (76,7) (77,7)=coin brick; (80,7) (81,7)=star brick | top Y 112, flanking pipe B |
| pipe B | cols 78–79, rows 4–10 (top Y 64) | a wall from the floor to row 4: every route crosses x ∈ [1235, 1277] with Y ≤ 71 |
| wrong-warp pipe | cols 84–85, rows 8–10, `$10 $11` top | goal X ∈ [1348, 1356] standing on it (Y 128); pipe C at 89–90 |
| floor pits (entrance) | cols 11–12, 15–16, 18–20 | single floor blocks at 17 and 21 |
Small Mario's collision points (`BlockBufferAdderData[2] = $0e`, the crouching set): head (x+8, Y+18), feet (x+3, Y+32)
(x+12, Y+32), sides (x+2, Y+24) and (x+13, Y+24) — both side "halves" sample the same row; the left point is checked
first and a hit there ends the side check (`check_side_mtiles`: the left point impedes only leftward motion), which is
the wall-walk law (F80). Model code: `emu.rs::player_bg_collision` (exact on 4-2, F87).

## The WR's slack profile under the x-only bound (`runs/P2.3c/main_xonly_p0_d587_checkpath.log`)
`bfscx W42Main data/wr/wr_inputs.bin 6584 0 587 --lift 0 --check-path 588 --stop-step 3` (2 threads, 2 GB cap).
slack(step) = 588 − step − h(state after step):
| steps | x | what happens | slack |
|---|---|---|---|
| 1 → 30 | 40 → 57 | the entrance fall (Y 48 → 176, 34 frames): a falling Mario accelerates slower than the table's runner | 46 → 36 |
| 30 → 167 | 57 → 385 | floor run + pit jumps at speed 40; into the tunnel under the pillar | 36 → 35 |
| 167 → 201 | 385 → 468 | tunnel, coin shaft, a hop that bumps (28,7); at step 200 (row 6784) the right side point (x+13, row 8) hits **col 30** while falling → `ImpedePlayerMove`: speed 40 → 0 | 35 → 22 |
| 201 → 221 | 468 → 482 | the entry trick: one Left press (record 6785) sets moving dir LEFT so the *foot-check* impede (feet inside (30,9)/(30,10), low nybble ≥ 5) pushes him **+1 px** while the kept sub-speed ($f4) drifts him +0.95/frame; two hops avoid the standing side impede; at step 217 x+2 ≥ 480 is inside col 30 and the side check never impedes again (F80) | 22 → 4 |
| 221 → 583 | 482 → 1343 | re-acceleration from 0 (the table knows it), run, lift ride, pipes: tight | 4 → 2 |
| 583 → 588 | → 1348 | braking before the pipe (F88's 3 frames) | 2 → 0 |
So the bottom route's 48 frames are: **10 forced (the entrance fall) + 31 in the col-30 wall entry + 4 at the end + 3 (F88)**.
The mushroom spawned by the (28,7) bump (slot 5, `Enemy_ID $2E` from row 6780, rising 1 px per 4 frames) plays no role
in the stop — the stop is the wall. Nothing in the WR's run after step 221 is loose to the x-table.

## Tool change: `bfscx --goal-x PX [--goal-y PY]` (position goals for segment searches)
`SearchGoal::steps_until_x_at_least` (new trait method; `W42Main` returns its x-table value) replaces the case's bound,
and "x_pos ≥ PX (and Player_Y ≤ PY) after a Success step" replaces the goal. Among the goal layer's goals the printed
one is the best by (x speed, x) so chained segments start from the strongest arrival. Reconstruction (`bfscx-path`)
is unchanged (parent record + input). Regression: the 4-2 WR-suffix control (`… 6584 575 587 --lift 0 --check-path 12`)
reproduces its counts exactly (6, 16, 34, 70, 134, 673, 3472, 16472, 69489, 257001; goal at layer 10, 16,194
transitions, 20.8 s; `runs/P2.3c/regr_w42main_p575_d587.log`). `tools/smb-opt-modes.patch` regenerated.
**Frontier law for position goals**: with the deadline 28 frames above the x-bound (S1, deadline 165, root 137) the
frontier doubled every layer (2.8M at layer 40, `runs/P2.3c/top_s1_x339_y112_d165.log`, killed) — a position-goal
BFS is only cheap when the deadline is at the segment optimum, hence `tools/bfscx_ladder.sh` (deadlines tight-first).

## Top-route reference path (chained segments)
Segments (goal = first arrival; the bound is x-only so each segment's deadline is found by the ladder):
- S1: root → x ≥ 339 (x+13 at the pillar's face) with Y ≤ 112 (on/over the pillar top). Needs the floor → 64-px climb
  from the single block col 17 (x ≤ 284) or earlier; col 21 (x 336–351) leaves no horizontal room.
- S2: → x ≥ 755 with Y ≤ 112 (the end of the run's top surface, past the col-29 hop under the hanging mass).
- S3: → x ≥ 1005 (left foot over the floor after the lift pit).
- S4: → the wrong-warp pipe entry (the case's own goal).
(results below)

## Enemies on the top route (not in the model; `tools/area_data.py E_UndergroundArea2`, `smbdis.asm` 8755–8800)
`E_UndergroundArea2` before the wrong-warp pipe: page 2 col 14 id `$3A`, page 3 col 10 id `$27` (the lift), page 4
col 13 id `$00` (green koopa, x 1232, at the bricks before pipe B), page 5 col 3 id `$02` (buzzy beetle, x 1328, at
the wrong-warp pipe). `HandleGroupEnemies`: `$3A` − `$37` = 3 → goomba (buzzy beetle only in `PrimaryHardMode`), d1 set
→ y `$70` = **Y 112, the top floor**, d0 set → **3 goombas 24 px apart**, placed at `ScreenRight` when the enemy parser
reaches col 46. With the spawn law (F65: ScreenLeft + 303 ≥ 736 → ScreenLeft ≥ 433, Mario at x ≈ 545 if he drives the
scroll) they appear at x ≈ 689/713/737 on the run's top surface walking left, i.e. they meet a top-route Mario under
the hanging mass (32 px of headroom: no jumping over a 72-px-wide group) — **three forced stomps** (descending only,
F59) or a detour. The bottom route never meets them (they walk on the platform above the tunnel). This is the likely
source of HappyLee's extra frames on the top route; the enemy-free reference path below cannot be replayed on the core
without them being handled (P2.5-style goomba module for 4-2 needed; the 1-1 goomba model, F56–F59, is the template).

## S1 result (2026-08-22 late): the x-only bound cannot carry even a 150-frame segment
`tools/bfscx_ladder.sh … 147 2 161 20000000 … -- W42Main data/wr/wr_inputs.bin 6584 0 --lift 0 --threads 2 --goal-x 339 --goal-y 112`
(root x-bound 137; logs `runs/P2.3c/top_s1_x339_y112_d{147,149,165}.log`, `top_s1_ladder.log`):
| deadline | outcome |
|---|---|
| 147 | no path — dies at layer 29 (the entrance fall alone costs ≥ 10 over the x-table, F94) |
| 149 | 13M states/layer from layer 75, **23M at layer 100 growing ×1.2/layer, max x ≈ 215 px** → killed by the 20M watchdog after 2.5 h (9 GB of layers) |
| 165 | ×2 per layer from the start (2.8M at layer 40) → killed |
Reading: at 2 frames of slack the survivors are the height variants of a handful of x-classes — everything the x-only
bound cannot see. `pruned` ≈ 15 % of generated, the rest is legitimate under the bound. The S1 optimum (149–155?) is
still unknown; the reference-path chain is parked until the bound sees y.
**Next (the actual P2.3c-2c work):** a sound y-coupled term for `W42Main`, keyed on the next gate the state must pass:
- gate 1 (pillar top): Y ≤ 112 with x+13 ≥ 352 — from (x, Y, y speed, state) the frames to *gain* (Y − 112) px are bounded
  below by jump physics: an airborne Mario cannot rise more than his current jump allows (max rise ≈ 73 px at speed 40
  from the ground, less in the air once descending: 0), so a state that is airborne with Y − remaining_rise > 112 must
  first **land** (fall at ≤ 4.5 px/frame to the floor Y 176 — or to a platform, hence the gate must use the lowest
  landing under it), then jump (64 px in ≥ ~16 frames). h_y = fall + rise; h = max(h_x, h_y_gate1 + C_gate1→goal).
- gate 2 (pipe B): Y ≤ 71 with x+13 ≥ 1248; gate 0 (the entrance): the fall Y → 176 at ≤ 4.5 px/frame with air
  acceleration (≤ ~0.6 px/frame²) for the x part.
  Validate each with `--check-path 588` on the WR (0 violations) and `tools/heuristic_audit.py`-style random trials
  (F68), then rerun the S1 ladder: the frontier should collapse by orders of magnitude.
- In parallel (bigger, decisive for 4-2): the **goomba module for 4-2** (P2.5c, new unit): the 3-goomba group at col 46
  (y 112), the koopa at x 1232, the beetle at x 1328 — the 1-1 goomba model (F56–F59) is the template (spawn at
  ScreenRight on the parser frame, walk 1 px / 2 frames?, stomp/injury, offscreen erase); difftest from record 6584.

## The y-coupled bound (2026-08-23, session 9): `heuristics::ygate` + `bfscx --goal-x GX --goal-y GY`
**What it is.** A sound lower bound on the steps until `x_pos >= GX && Player_Y <= GY` hold *at the same step*, built
from two relaxations and joined per step count k:
- x: MrWint's x table (max gain in exactly k steps for the state's x class) — unchanged;
- y: the model's vertical rules exactly (`emu.rs`: jump start at −5/−4 px with the jump gravity kept only while A is
  held and y speed < 0, A release irreversible, `y += y_spd; y_spd += v_force` with the MAX_Y_SPD clamp, a head bump
  sets `y_spd = 0x100 | frac`, a landing needs `y_spd >= 0` and Y ∈ [Ys, Ys+5) and snaps Y to Ys), with x dropped
  except through **surfaces**: every standable cell top of BB42 (cell ≠ 0, not coin/climb; hidden blocks included)
  as (Ys = 16·row, the x_pos intervals where one foot point x+3/x+12 is inside a cell of that row). This is a
  superset of the model's landings on purpose: a one-frame landing inside a wall face is SMB1's wall jump, and the
  floor-level wall walk (F80) makes every row of a solid run standable — so the faces of the pillar (Y 128/144 at
  x ≥ 340), the rows inside the brick run and the pit walls are all surfaces. Head bumps are allowed on any frame
  with y speed < 0 (the model also needs Y mod 16 ≥ 4 and a block above — superset).
- per state: the hold-A trajectory is pointwise the highest relaxed trajectory (a release or bump only lowers it),
  so "Y ≤ GY at step k without landing" ⇔ k in its window; the earliest frame a landing on surface s can happen is
  max(first hold-A frame with Y ≤ Ys+4 (s above) or fastest-descent frame with Y ≥ Ys (s at/below: bump-at-1 or
  release, whichever lands first), x-table steps to bring x into one of s's intervals); after a landing the
  y-only T(Ys) (min steps from standing at Ys to Y ≤ GY over jump/landing chains through higher surfaces, precomputed
  top-down) applies; standing states may wait, so their feasibility is "k ≥ t". The bound = the first k at which the
  x table allows x ≥ GX **and** Y ≤ GY is y-feasible (window or re-landing). Code: `src/heuristics/ygate.rs`
  (doc comment = the argument), `W42Main::set_position_goal/steps_until_xy`, `xy_h` in `bfscx`, trace mode `traceh`.
**Known looseness (sound, listed for the next tightening).** T is computed with landings every 16 px on wall faces
(T(176 → 112) = 12 via four one-frame face landings, vs 18 for a single running jump); in the model each face landing
is followed by the side-check push-out (−1 px, speed zeroed), so two consecutive face landings need the feet back
inside the column, which the re-acceleration (< 1 px/frame) cannot do — proving that would make T(176) = 18 and the
face surfaces single-use. Inside the brick run (F80) hops land at Y 160 but every hop bumps the brick above, so the
relaxed chain 160 → 144 → … is also unrealizable there. Neither matters for S1 (those surfaces are at x ≥ 340).
**Validation.** (1) The WR-suffix regression control is byte-identical (same counts 6, 16, …, 257001; the position
goal is not used there): `runs/P2.3c-2c/regr_w42main_p575_d587_ygate.log`. (2) `tools/ygate_audit.py` — random
right-biased trajectories from the root (A held in random runs, B mostly held) traced by `smb-opt traceh`, asserting
at every step with a later goal step t' that t' − t ≥ h: **300 trials × 230 steps, 8 reach the goal, 1,485 checks,
0 violations, min slack 0** (`--gx 339 --gy 112 --lift 0 --n 300 --len 230 --seed 1`). (3) On the WR
(`traceh … 588`, `runs/P2.3c-2c/traceh_wr_s1.log`): h 137 at the root (the fall is not y-binding), 12 inside the
run (the face-chain T(176)).
**Tooling note.** `systemd-run --user --scope -p Nice=10` is rejected on this host ("Unknown assignment: Nice=10" —
scope units take no Nice= property), which silently aborted the first ladder attempt; `tools/bfscx_ladder.sh` now runs
`nice -n 10 smb-opt` inside the capped scope. The x-table build (~22 s) dominates each short ladder attempt.

## S1 solved (2026-08-23, session 10): the enemy-free S1 optimum is exactly 149 steps
The detached d149 search (the y-coupled bound, 2 threads, 24-byte records — launched before the P2.5c-2 record
growth) ran to its verdict: **goal reached at layer 149 (45,545,904 goal transitions), no goal at any earlier
layer** — and a deadline-149 run cannot prune a shorter goal path (a state on a ≤ 148-step goal path has
g + h ≤ 148), so with the d147 attempt (dead at layer 29) this is proof-grade: **S1 (root → first x ≥ 339 with
Y ≤ 112) = 149 steps, and 147 is impossible; 148 is excluded by this run itself**. The frontier peaked ~40M
(layer 105), fell to 14.4M by layer 120, and collapsed at the end (layer 130: 14.3M parents → 1.7M unique,
210M pruned; the last layers ran in ~20–35 s) — the y-term's endgame bite, as designed. Log:
`runs/P2.3c-2c/top_s1_x339_y112_d149.log` (layer files deleted after the reconstruction).
Path: `bfscx-path … 148 "[33, 21, …]" 0x1 --lift 0 --stride 24 --threads 8` (the new `--stride`: the run predates
the 96-byte records; old 24-byte layer records are zero-padded on read) → `runs/P2.3c-2c/s1_path.bin` (149
records), chained with the record-0 prefix into `runs/P2.3c-2c/chain_s1.bin` (6,733 records) and **replayed on
the QuickNES core: 149/149 frames identical** (`model_difftest.py --wr-file … --wr --root-record 6584 --len 149
--n 0 --enemies 0`, `runs/P2.3c-2c/s1_path_reconstruct.log`). S1 is enemy-free in fact as well as in the model:
the group spawn needs ScreenLeft ≥ 433 and the path ends at x 339.
**Next segment (S2, the first enemy-aware one): x ≥ 755 ∧ Y ≤ 112 from chain_s1.bin (prefix 149), `--enemies 0
--goal-x 755 --goal-y 112`, ladder from the x-bound up; 8 threads now that run 4 has the nights.**

## S2 solved (2026-08-23, session 11): the enemy-aware optimum equals the movement bound — 166 steps
Goal: first x ≥ 755 ∧ Y ≤ 112 (end of the top surface, past the col-29 hop) from `chain_s1.bin`
(FIRST 6584, prefix 149 = the proven S1), `--enemies 0` (the full P2.5c-2 module; bounce band armed in the
y-gate). Probe: deadline 150 → root bound **166** (`top_s2_probe_d150.log`). Ladder
(`MEMCAP=4G tools/bfscx_ladder.sh … 166 2 206 20000000 …`, 8 threads, run 4 SIGSTOPped for the window):
**the first rung d166 reached the goal — at layer 166, 70,609,064 goal transitions, peak frontier 5,537,985
(layer 165, still growing when the goal ended it), 1,996 s wall** (`top_s2_x755_y112_en_d166.log`).
Proof-grade with no lower rung needed: the deadline equals the bound, and at deadline 166 any state on a
≤ 165-step goal path has g + h ≤ 165 ≤ 166 and cannot be pruned, so earliest-goal-layer 166 is the optimum.
**Consequence for H36: across S1+S2 — the whole goomba-group section — the enemies cost 0 frames over the
enemy-free y-coupled movement bound. Root → x ≥ 755 ∧ Y ≤ 112 = 149 + 166 = 315.**
Tool bug found (and fixed, engine rebuilt): bfscx printed "goal parent record" truncated to klen+2 = 22
bytes (correct in the 24-byte-record era; under `--enemies` the 64-byte ext is nonzero from the root), so
the zero-padded record matched no layer-164 parent and the first reconstruction FAILED
(`s2_path_reconstruct_fail_truncated_rec.log`). The full 96-byte record was recovered from
`layer_165.bin` by its 22-byte prefix — **3 records matched: same player key, the goomba group in three
walk phases** — and the reconstruction rerun with the first.
Replay: **core-verified — 315/315 frames identical, 0 differences, exactly 1 stomp** (chain step 288 =
S2 step 139, x ≈ 690: the group's leftmost goomba, taken in stride mid-jump with the −4 bounce;
`trace_s2_events.log`, `s2_path_reconstruct.log`, the difftest line in the session log). `chain_s2.bin` =
6,899 records. The 30 G of S2 layer files were deleted after the verified reconstruction (d149 practice).
The path's shape (RLE in `s2_path_reconstruct.log`): the sub-pixel R/L speed-weave along the top surface,
one early hop, then a long jump into a 34-frame fall through the goomba region with the single stomp.
**S3 (→ x ≥ 1005, the lift pit; no Y goal): probe root bound = 100** from the S2 arrival
(`top_s3_probe_d80.log`, deadline 80, new full-record binary). Ladder `100 2 140 20000000` launched —
`top_s3_ladder.log`; ride-the-lift vs jump-the-pit is the question this rung answers.

## S3 first attempt (2026-08-23): dead root — the greedy chain boundary bites
The S3 ladder (x ≥ 1005 from `chain_s2.bin`, root bound 100) died structurally: rungs d100–d118 lost all
states by layer ≤ 19 with dead = 0 — the wavefront froze at x ≈ 790 — and d120 exploded past 20M records
(killed by the ladder). Diagnosis from the d118 log: the S2 goal pick (best by (x speed, x) among the
70.6M layer-166 goal transitions) is an airborne arrival at Y ≈ 83, x ≈ 756, descending at max speed. It
cannot reach the (47,7) ledge (29 px of fall needed within 12 px of travel), so every continuation either
hits the col-50 question-block face below top level (`ImpedePlayerMove`, speed → 0, h snaps back to ~100 →
pruned out) or falls through the 48–49 row-7 gap to the bottom floor, whose re-climb costs more than the
rungs' slack. d120's 21M-record blowup is the bottom-detour variant space opening up, not progress.
Lesson (structural): **a first-arrival position goal chained at an airborne boundary can select an arc
that cannot survive the next segment** — chain boundaries must force a grounded-or-landing arrival.
Fix: **S2′** = first x ≥ 800 ∧ Y ≤ 112 (at x ≥ 800, Y ≤ 112 means at/over the (50,7)–(51,7) tops — a
32-px landing zone, not a wall face), then **S3′** = → x ≥ 1005 ∧ Y ≤ 112 with the y-gate armed for the
pit crossing. F112 (S2 = 166 via x 755, the goombas cost 0) stands — S2′ only moves the chain boundary.
S3 rung logs kept (`top_s3_x1005_d1*.log`); the dead layers deleted.

## S2′ solved (2026-08-23, session 11 cont.): 184 = the movement bound; the chain-safe boundary works
S2′ (first x ≥ 800 ∧ Y ≤ 112 from `chain_s1.bin`, `--enemies 0`): probe root bound **184**
(`top_s2p_probe_d160.log`), and again the **first rung d184 reached the goal at layer 184** (77,448,522
goal transitions, peak frontier ~7.5M around layer 167, 3,021 s at 8 threads) — proof-grade, deadline =
bound. The goal print is STILL a truncated 22-byte record (the widened print slices a value that is
already short — the goal bookkeeping at `main.rs:987` hands over a 22-byte rec; proper fix queued):
recovery-by-prefix from `layer_183.bin` is the working procedure (**10 matches** this time — same player
key, richer goomba/slot phase variety; record #1 reconstructed clean to layer 0). `chain_s2p.bin` = 6,917
records, **core-verified 333/333 frames, 2 stomps** (chain steps 280/296, x ≈ 671/711, both in stride
mid-descent). The arrival is chain-safe by inspection as designed: Y ≈ 38 rising at max speed — an arc
that clears the (50,3)–(51,3) question-block row and lands on the (54,7)–(56,7) group at x ≈ 865, grounded
at full speed 47 px before the pit lip. **Chain: root → x ≥ 800 ∧ Y ≤ 112 = 149 + 184 = 333; enemies
still cost 0 frames over the y-coupled movement bound.** S2′ layers (verified) deleted.
**S3′** (→ x ≥ 1005 ∧ Y ≤ 112, the pit crossing, y-gate armed): probe root bound **82**
(`top_s3p_probe_d70.log`); ladder `82 2 122 20000000` launched (`top_s3p_ladder.log`).
