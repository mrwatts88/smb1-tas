# P2.2a — 8-4 turnaround room (H25, Maru's stop): forensics and search spec

Date: 2026-08-24 (session 14, evening — the 8-4 campaign opener per decisions.md).
Claim under test (H25/F37/F11, Maru 2024): in the 8-4 turnaround room the runner can "stop on the floor one
frame earlier while still scrolling far enough for the wrong warp; depends on subspeed ($705)" — a 1:1 frame
in the unquantized 8-4 = the record.

## The mechanism (dump forensics, rows 16350–16560)
The room-3 wrong warp: the pipe at x ~3404 must lead to the water section, i.e. the enemy-data area-change
command at (page 14, col 4) → $02 p0 must be PARSED before the entry. Parse threshold (the F40 law):
command x = 3648, look-ahead ScreenRight + 48 masked ⇒ **SL ≥ 3345**. The WR:

| row | event |
|---|---|
| 16394 | the (13,4) → $65 p1 command parses (the non-warp default: back to page 1!) |
| 16492 | brake begins (x 3428, speed 40; L+R decel ~2/frame, with a re-accel blip 21→23 at 16503 — a hand-tuned scroll landing) |
| 16516 | **SL reaches 3345 — the threshold EXACTLY — and $02 p0 parses the same row**; Mario x 3457, rel = 112 (pinned, no minted offset) |
| 16517 | speed crosses 0 at max x 3457; the turnaround begins (leftward accel to −40) |
| 16550 | pipe entry (GES 3) at x 3404; the F66-optimal 74-frame clip follows |

**Margin over the threshold: ZERO pixels.** HappyLee stops the scroll at 3345 dead-on (the same knife's edge
as his 4-2 entry at SL 1216 exactly). A 1-frame-earlier stop with his subpixel phase would miss the parse and
route-die — so the H25 frame, if real, is a SUBPIXEL-PHASE effect: a different brake pattern (or approach
fraction $705) that lands SL = 3345 with the reversal beginning one frame earlier, or returns to the pipe one
frame earlier from the same stop. The WR's approach+stop+return = rows 16355 → 16550 = **195 frames**; the
segment is enemy-light, land-only (no swimming), and the structure is the warp-zone overshoot-and-return
(F83/F89) plus the parse threshold (F40) — every piece already modeled.

## The search spec (step 2)
- Case `W84Room3` (new, w84.rs): room-3 control state (dump row 16356, table below) → the pipe entry, with
  `WithScrollPos` + `WithRunningTimer` (+ block bounce; block states if the room has bumpable cells — check),
  block buffer from the dump (`tools/blockmap_from_dump.py` rows 16355–16600) or MrWint's BB84 part.
- Goal: the pipe entry **with SL ≥ 3345 at the entry** (else refuse, the inverted F40 pattern — a goal_refused
  variant requiring the parse instead of forbidding it).
- Bound: x-table + the F89 overshoot-return machinery (`build_overshoot_bound` at the scroll-target x).
- Ladder: deadline 194 (WR − 1) — a goal = THE RECORD candidate (→ core replay → splice → two-emulator);
  dry at 194 with the d195 `--check-path` control = H25 refuted for this room.
- Enemy check first: `tools/area_data.py E_CastleArea6` for the room-3 pages (podoboos? none observed in the
  WR trace region); if enemies exist they are LFSR-frame-indexed (lag-free route) — extend the module only if
  they can touch the corridor.

## Root state (dump row 16356 = record 16354; alignment F69)
(see the ram_trace table in the session log / STATUS checkpoint; folded into the case constants when built)

## Step 2 build (2026-08-24, session 14 late): the case is in the engine
- **Root (record 16354 = dump row 16355):** x_pos 0xc3800 (3128.0), y_pos 0x19000 (Y 144), speeds 0, STANDING,
  all fractions 0 ($0705/$0433/YMF 0), SL 3072 (sl8 0x00), PlayerSize small (the warps WR never powers up —
  NB MrWint's W84 cases are `Big`, his own route). Phases (unused — the segment is enemy-free and stateless
  beyond player+scroll): FC 120, ITC 6, GT 24, parser col 216, eoff 34.
- **`W84Room3`** (case/w84.rs): Small + WithRunningTimer + WithScrollPos, key [u8; 12]; goal =
  `StateChangeVerticalPipe(212,5)` **with `screen_left_abs ≥ 3345`** (the warp condition, no hook needed —
  the scroll is in the state; `screen_left_abs` disambiguates the sl8 page exactly, so no [0x11,0x40) window
  needed). Bound = the F89 overshoot machinery: `build_overshoot_bound(0xd8100, pipe_xmax, 0)`; two-phase on
  the state's own SL (pre-3345: coupled overshoot to x ≥ 3457 — necessary since rel ≤ 112 while scrolling and
  minted offset only raises the x needed; post: leftward table to the pipe window).
- **Independent confirmation:** MrWint's `W84Part3VertPipeEntry` (the clip case, 74 = WR, F66) encodes the
  same condition as `left_screen_edge_pos ∈ [0x11, 0x40)` = SL 3345 on page 13 — the threshold cross-checks.
  His clip segment being optimal at 74 localizes any H25 frame to the approach + the SEAM into the clip state
  (the WR's row-16476 handoff) — which the whole-segment search subsumes.
- Dispatch arms (tracec/bfscx/bfscx-path, hook None). Validations running: WR line (`--wr --len 196 --n 0`,
  entry expected at step 195) + 100 random trials.

## Validation results
- **WR line: 195/195 frames, 0 differences, pipe entry at the identical frame+Y** (`--wr --len 196 --n 0`) —
  the root state (incl. AREA_INIT v-forces) is exact; **WR = 195 steps** (control → entry).
- Random trials (100 × ≤210): (running — folds in below).

## The rungs
- **d194 = the record rung** (a goal ⇒ an 8-4 frame ⇒ THE RECORD): launched, 3G cap / 4 threads
  (`runs/P2.5b/../w84r3_d194.log` → `runs/P2.2a/`). Dry ⇒ the d195 `--check-path 195` positive control, then
  H25 is answered for this room.
