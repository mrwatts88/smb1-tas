# Hypotheses ledger

Every idea for saving time lives here. Statuses: **untested → in-progress → confirmed /
refuted / parked**. *Refuted requires a proof artifact* (search record or code-level argument
with a reproducing script). "Nobody has done it" is never evidence. Parked = deprioritized,
say why. Link each status change to the experiment file or script.

| ID | Hypothesis | Track | Status | Evidence / notes |
|---|---|---|---|---|
| H1 | **Ending-input coast.** The movie ends at last input; a final jump from farther away (or an enemy/fireball bounce) could make the last input earlier while Mario still reaches the axe. | A | in-progress (baseline measured) | WR already coasts: last input = A press on frame 17848 (0-based), axe reached within the next 19 frames (F16). Question now: can the final jump start earlier / the axe be reached earlier — P2.2 |
| H2 | **Lag frames.** #1715M contains lag frames that could be removed via object/scroll management; each is a frame in 8-4 or a possible framerule flip elsewhere. | A | **reopened, sharpened (E1/F224)**: the 22 in-movie lag frames are exactly one per AREA LOAD, and **four of them are inside 8-4** (dump 15797/16234/16599/17469), which is unquantized -- so each is worth exactly one frame if removable. The load frame renders 12 column sets, so the NMI overrun is probably structural rather than marginal, but the margin has never been measured. Prior: low. | P0.4: 24 lag frames, all boot/Start/area-load frames, none in-level (F30). Only a load-frame removal would count; revisit in P0.5 (is the load lag avoidable at all?). |
| H3 | **Framerule-phase manipulation.** Some action (pause, Select, area transition, pipe, vine, death, soft reset, lag) shifts IntervalTimerControl's phase relative to the level timeline, letting a level with slack k be re-aligned. | A/B | untested | P0.5 timing model; then search P0.7 (F62): pause freezes ITC, FrameCounter and all game logic but steps the LFSR (≥ 44 frames per pause); TimerControl halts the timers only. So pause cannot shift the framerule phase relative to the level, but it shifts the RNG relative to everything else — relevant for RNG-dependent objects (8-4 Bowser, Hammer Bros), not for framerules. **CLOSED 2026-08-25 (F255):** `TimerControl` ($0747) DOES shift the ITC grid relative to the level (non-zero skips `IntervalTimerControl` while `FrameCounter`, the LFSR and the game logic all keep running) — so the lever is real — but the arithmetic is against every freeze the ROM can produce. For a freeze of k frames costing c frames of progress, with v0 the level's slack, the exit moves by Delta = c + ((v0 + k - c) mod 21) - v0, so a gain needs c + ((k-c) mod 21) < 21, and the freeze must fall between the level's load and its T_set. `TimerControl` has exactly one writer (`SetPRout` 11419, `ldy #$ff`); the only freeze during which Mario still moves is `PlayerInjuryBlink` (k = 55, c = 16, d = 18) -> Delta = +13 frames at best, before the cost of being big; `PlayerChangeSize`/`PlayerFireFlower` have c = k. Kept only as a price for an arbitrary write to $0747 (a 1-frame freeze at c = 0 with v0 = 20 is worth 20 frames). `docs/experiments/E10-rom-read.md` §3. |
| H4 | **Time-bonus countdown.** (Confirmed 1 frame per unit, F27.) If the end-of-level countdown burns ~1 frame per remaining timer unit before framerule rounding, the remaining-timer value (and the 24-frame tick phase at flagpole touch) is a lever; also check whether any glitch lowers/zeroes the timer. | A | untested | P0.5 |
| H5 | **Warp table indexing.** WarpZoneControl × pipe-index into WarpZoneNumbers: which values are reachable beyond the normal {2..8, 36}? | B | **refuted** (P0.6) | Proof: `docs/warp-model.md` §5.4 + `tools/warp_tables.py`. WZC is cleared per area load (F38), written 4/5/6 by the text object and +1 by the single self-erasing `WarpZoneObject`; the text cannot be parsed while the scroll is locked, so the +1 always precedes it → WZC ∈ {0,1,4,5,6} at any pipe; index rows 2/3 (WZC 2,3,7 → worlds 255/38) are unreachable. Reachable destinations: 1-2 {4,3,2}/{−1,5,−1}, 4-2 ceiling 5, 4-2 $2F {8,7,6}. Only a memory-corruption write to $06D6 (H7-class) could change this. |
| H6 | **NES Minus World is not a dead end.** With WorldNumber = 35 many table reads are OOB (area lists, hard-mode tables, Bowser replacement); the exit pipe, vines, death/continue, or a Game Over continue may lead somewhere. | B | **refuted at table level** (P0.6) | `docs/warp-model.md` §6, `tools/warp_tables.py`: world 35 → AreaPointer $01 (Water2) via `AreaAddrOffsets[51]`; all three commands in Water2 need world 2/3/7 → never match 35; the exit pipe reloads $01 p0; death → `ContinueGame` → `FindAreaPointer` → $01; game over → `ContinueWorld` 35 → same; no flagpole, no vine. Every path recomputes the same pointer. Remaining opening = an in-game write to WorldNumber/AreaPointer (H7). |
| H7 | **WorldNumber write-reachability.** Some in-game indexed write (enemy slot overflow, object ID overflow, buffer overrun) can set WorldNumber ($075F) / LevelNumber to reach world 8 or flag the ending in an earlier castle. P0.6 sharpened the targets (F43): (a) WorldNumber ≥ 7 before any castle's axe ends the game (`PlayerEndWorld`); (b) AreaPointer/EntrancePage ($0750/$0751) = $65/16 before any down-pipe enters the Bowser room; (c) WarpZoneControl ($06D6) ∈ {2,6} in 1-2 warps to 8-1 from 1-2. | B | untested (**narrowed**, session 17) | P3.1–P3.3 (RAM oracle finds the jackpot cells; audit finds the writes). **Session 17 narrowing — not a refutation.** (a) **F201**: target (a) has no target on the warp route — the route contains exactly one castle, 8-4, where WorldNumber is already 7, so there is no earlier axe for a WorldNumber write to convert into an ending; making it pay needs an *earlier castle*, i.e. a routing change (see D2), not a RAM write. (b) **F202/F203**: target (c) — `$06D6` WarpZoneControl in 1-2, the biggest prize on the route — is **provably unreachable by the only OOB writer P3.1 found**: the block-buffer `sta ($06),y` mechanism has an arithmetic ceiling of `$06CF` ($07 always $05, $06 <= $DF, y <= 240), missing $06D6 by 7 bytes; `$075F` WorldNumber is 144 bytes above it. **What is still open:** P3.1 §4's own uncovered paths — stack overflow/underflow, non-indexed writes, and `VRAM_Buffer` writes indexed by `VRAM_Buffer1_Offset` ("a classic corruption vector in other games… not audited line by line"). H7 needs a writer, and the writer hunt is now the whole hypothesis. |
| H8 | **Cart-swap-free ACE.** Object ID $C9 (or any OOB object/state value) can be produced in worlds 1–8 without cart swap, e.g., via another OOB index into the Bowser-replacement or enemy tables, or via uninitialized-RAM reads at power-on. | B | untested | P3.1, P3.4 |
| H9 | **Cross-level DP.** A slower framerule in an earlier level changes RNG / framerule phase enough to save more later (8-3 Hammer Bros, 8-4 Bowser). | A | **refuted** (E1/F224) | Proof artifact: `tools/entry_state_scan.py` over the WR dump. `RandomBits(f) = LFSR^(f - L(f))(seed)` with `L(f)` = area-load count; both terms are route-fixed, and `InitializeArea` clears everything below $074c, so no physics-relevant cell crosses a boundary carrying path information. Spending slack changes nothing at any later entry. `docs/experiments/P4E-finder.md` §E1 |
| H10 | **8-4 full-state exhaustive search** finds ≥ 1 frame (wall clip, turnaround room, water, Bowser pattern, L+R usage). | A | untested | P2.2 |
| H11 | **Per-level threshold search** reaches framerule N−1 in at least one of the 7 framerule levels (priority = smallest slack first). | A | untested | P0.4 then P2.3 |
| H12 | **L+R / U+D semantics** contain effects beyond the known 1-frame reversal (e.g., interactions with walls, vines, pipes, flagpole, swimming) that enable new clips. | B | untested | WR uses L+R on 85 frames in ~20 clusters (list via `tools/fm2_info.py --list-lr`), U+D never. P0.4 maps each cluster to its level/purpose; P0.7 catalogs the code paths |
| H13 | **Alternate pipe glitch** (entry point variable loads per screen): a pipe in 1-2, 4-2, or 8-4 can be made to lead somewhere useful — e.g., an 8-4 pipe into the Bowser room. | B | **refuted at table level** (P0.6) | F40 / `docs/warp-model.md` §3.3: every pipe destination is the last matching row-$0E command; the complete table has 58 commands and the Bowser-room page (16) is written only by the 8-4 water section's command, which is already the route. The 4-2 wrong warp (already in the WR) is the only parse-order shortcut in the tables. Non-table destinations need a write to $0750/$0751 (folded into H7). |
| H14 | **Vine teleport / screen-edge tricks** skip a section of 8-4 or 4-2. | B | untested | P0.6, P2.x |
| H15 | **Soft reset exploitation.** If TASVideos rules allow mid-movie resets: resetting during a specific write leaves the warm-boot/continue path ($07FD/$07FF) in a state that starts later than 1-1 faster than playing there. Cost: title screen again. P0.6 (F44): warm boot needs top-score digits < 10 and $07FF = $A5; `ContinueWorld` $07FD is written only by a game over, so a continue costs a game over first. | B | untested (low prior) | Check rules; would need a reset-timing search in P3.4 |
| H16 | **Sprite-0 / PPU timing.** Lag frames can be prevented (or the NMI-overrun threshold moved) by controlling what the game renders. | A | untested | P0.5 |
| H17 | **Object-slot spawn suppression.** 5+ active sprites prevent spawns (documented for 4-4 Bowser). In 8-4, suppressing Bowser (or hammers/fire) may give a faster axe. | A/B | untested | P0.6, P2.2 |
| H18 | **Power-up usage.** A power-up somewhere (fire, super) nets frames despite pickup cost (e.g., fireballs vs Bowser, big-Mario clip geometry). Low prior. | A | untested | P2.x |
| H19 | (P0.5: confirmed for this boot — Start rows 34–43 all give control on row 197, F31; only the boot ITC phase could change it.) **Start-press / power-on alignment.** The first Start press frame and emulator initial-RAM convention are fixed by rules; confirm #1715M presses Start on the first accepted frame and that nothing in the boot path can be shortened. | A | untested | WR presses Start on frame 41 (0-based) after a soft-reset command on frame 0 (F16). P0.5: find the first frame the title screen polls Start; P0.4: confirm nothing earlier is accepted |
| H20 | **Uninitialized-RAM reads.** The game reads RAM the boot routine does not clear (the ACE lands at $1181 for this reason). Under the emulator's defined initial RAM, any such read that affects gameplay is a lever. | B | untested | P3.1 |
| H21 | **1-1 is 1 frame from the next framerule** (F29): T_set = grab + T + 159 (F32), WR: 1289 + 370. Objective for the 1-1 search: a glitch grab with grab + T ≤ 1658 (e.g. grab on row 1288 with T still 370, or any earlier grab; note T only drops every 24 control frames, so a 1-frame-earlier grab normally keeps T). Fireworks must stay off (last digit ∉ {1,3,6}). Community (F35): believed impossible since 2009, but no proof. P2.1a structure (F47/F48): the third room is a fixed puzzle from X = 2616, speed 0; the WR reaches the pole region at frame 1280 (QuickNES) and spends 5 frames stopped for the FPG grab at 1285; sub-questions: (a) earliest physical arrival at x ≥ 3158 (flat-world frontier, sound relaxation), (b) the FPG trigger condition in x/subpixel/y terms (from `HandleClimbing`/`FlagpoleRoutine`), (c) whether rooms 1–2 can deliver the third-room load earlier (its entry state is fixed, so any room-1/2 frame counts 1:1). P0.8: MrWint's smb-opt solved the third room only as two hand-chained segments (174-frame speed-up to the stairs + 59-frame 'half FPG' from a chosen stairs state, F52) — never as one search from the fixed entry; its XPosHeuristic is the sound tight bound to reuse. | A | **refuted (F116 + F124)** | P2.1a done; P2.1b-m/m2: validated model incl. goombas (F56); pole approach from the WR's stairs-top state exhaustively optimal (F57); WR on the x-frontier at the stairs top (F60). Residual = H28 (different stairs-top handoff, P2.1b-m3 running) and H29 (bounce-assisted room 1); rooms 1–2 equal the enemy-free optima (F63), the bonus room (no enemies) is therefore proven optimal. **CLOSED 2026-08-24 (session 15).** Both residuals are now refuted: H28 by F116 (whole-room search from the fixed room-3 entry, no FPG grab ≤ 238) and H29 by F124 (enemy-aware room-1 rung dry at 367, WR's 368 optimal). Rooms 1–2 already equalled the enemy-free optima (F63) and the bonus room has no enemies. **1-1 cannot deliver its 1 missing frame on any modeled route** — the level is closed end to end; the residual is the glitch track (Track B), not the search track. |
| H22 | **1-2 is 8 frames from the next framerule** (F28/F29): entering the warp pipe 8 frames earlier (or shortening the 48-frame ChangeAreaTimer pipe animation, or gains in the intro/main area) saves 21 frames. P0.8 (F52): Maru370's 2024 'perfect 1-2' is 3 frames faster than the WR's and 5 frames from the next framerule; zdoroviy_antony: +7 subpixels, 'not enough'. Deficit to beat on the best-known route: 5. | A | untested | Third target after 1-1 and 8-4. The ~499-row auto-walk intro is fixed — unless it isn't (check AltEntranceControl paths in P0.5/P0.6). |
| H23 | **Remaining framerule deficits**: 4-1 9 frames, 8-3 10 (of which 3 are known: FPG with timer 242, F37 → 7 left), 4-2 13 on the WR route but **2 on HappyLee's top-floor route** (F36), 8-1 18, 8-2 19 (F29). Search order by remaining deficit: 4-2 (2), 8-3 (7), 4-1 (9), 8-1, 8-2. | A | untested | P2.3; `docs/community-claims.md`. 2026-08-22 (F66/F67): no exhaustive segment optimum exists for 4-1/4-2/8-1/8-2/8-3 (MrWint solved none of them), while every solved segment (1-1, 1-2 opening, 8-4) has the WR at the bound — so these five levels are the only route segments where a player-only search has unexplored room; 4-2 top route first (P2.3a). 2026-08-22 (session 6, F73): the WR's bottom route rides the 4-2 lift and jumps off it 44 px above the floor; the top route crosses the same pit while the lift descends through it → the 4-2 main-area bound needs the lift in the model (P2.3b); the warp-zone model is exact (F75) and its enemy-free bound is sound. P2.3b (same day): lift modeled and validated (F81); the WR's own pipe entry is 1 frame late (F79, verified on the core) → bottom-route deficit 12, top route presumably 1 if HappyLee entered the same way. |
| H24 | **8-4 is unquantized**: every frame saved in 8-4 (incl. the 19-frame ending coast, H1, and the 4 sub-area loads which are not framerule-bound) counts 1:1. | A | confirmed (mechanism) | F27/F28: OperMode→2 at the axe; room loads have no ScreenTimer wait. Gains still to be found (P2.2) — but F67 (2026-08-22): every running/swimming section is at the speed cap on every frame except the two fixed accelerations, and the three solved segments equal MrWint's optima (F66); remaining 8-4 levers are transitions, the water-room end alignment, Bowser/RNG, glitches. |
| H25 | **Maru's 8-4 idea** (stop on the turnaround-room floor one frame earlier, subspeed $705-dependent, F37) is not in the L+R WR and would be a 1:1 frame in 8-4. | A | untested | Needs Maru's movie file (TASVideos 4:57.54 page has only the time) to diff against the WR; else reproduce from the description in P2.2. |
| H26 | **8-2 framerule via end-of-level Koopa FPG** (KnucklesMaster368 2017) or the RTA plant-despawn (3 Bullet Bills one frame — HappyLee: "RTA only"). 19-frame deficit makes this the least likely flag level; "better spawn proven not possible by using cheat" (zdoroviy_antony) is the only search-like evidence and covers spawn outcomes only. | A | untested (low prior) | `docs/community-claims.md` |
| H27 | **The WR's 5-frame stop before the 1-1 pole is avoidable.** The WR arrives at x = 3157.3 at frame 1280 with speed 40, stops (speed 0) and creeps to X = 3161 for the grab at 1285 (F47). A trajectory arriving with a grab-compatible (x, subpixel, y) would grab on arrival — up to 5 frames earlier, 1 is enough. Needs the exact FPG trigger condition (x & 15 ∈ [6,10) at the pole column via the head/side collision points, Y ≥ $A2 at the grab frame) and the y-trajectory from the stairs. | A | **refuted for the WR's stairs-top state** (2026-08-21, F57): exhaustive model BFS from the frame-1229 state (all 16 inputs/frame, sound dedup + bounds) finds no glitch grab ≤ 1284; lowest grab by 1284 is Y 161.875 (one pixel short, F55: such a grab waits ~47 frames for the flag). The model is exact on 82k random frames after the stairs (F56). Still open from earlier roots / the whole room (P2.1b-m2). | proof: `runs/P2.1b-model/root1229_d55_v3.log`; `docs/experiments/P2.1b-pole-search.md` |
| H28 | **A different staircase-top handoff saves the 1-1 frame.** The WR is x-optimal at the stairs top (step 183, x 3033) and its pole approach from that exact state is optimal (F57); but the final descent must end on the ground at X ∈ [3155, 3162] (F60) and its timing depends on the y fraction (persists across landings), the speed fraction, timers and facing at the handoff. If some reachable handoff state at steps 183–186 lets the landing happen one frame earlier, the grab moves to ≤ step 238 and 1-1 gains 21 frames. Test = the whole-room search (P2.1b-m3), or a two-phase version: enumerate reachable stairs-top states (x ≥ 3024 by step ~190 with all fractions), then the pole search from each. | A | **refuted** (2026-08-24, F116: run 4 whole-room search, relaxed model + goombas, no FPG grab ≤ step 238 — proof-grade in the over-approximating model; the 407M-state stairs-top seam held no better handoff) | F60/F61; `docs/experiments/P2.1b-pole-search.md` |
| H29 | **A bounce-assisted path beats the enemy-free optimum in 1-1 room 1 (368) or the bonus room (111).** The WR is at MrWint's relaxed-model optimum in both (F63); the relaxed model is a superset of the real game except that it has no enemies, so the only real-game options it misses are stomp bounces (free mid-air hops) off the room-1 goombas/koopa (`E_GroundArea6`: pairs at pages 1–2, 5, 6, 7, 8; a green koopa at page 6). A bounce could save a frame only where the model had to slow down or jump from the ground (the four pipes). Test: extend the enemy model to room 1 and run `bfs11` from the room-1 entry with deadline 367 (the bonus room has no enemies → already proven optimal). Only goombas can spawn before the warp pipe (the koopa is at page 6, F64). | A | **refuted (F124)** | F63; P2.5a (2026-08-22): the room-1 goomba rules are read from the code and validated by a reference simulation (F64/F65, `tools/room1_enemy_sim.py`); the WR already takes two forced bounces there and still equals 368. Next: port into the smb-opt model with scroll tracking + difftest, then the deadline-367 search (P2.5b). **REFUTED 2026-08-24 (session 15), proof-grade (F124).** The port landed and was validated (WR 368/368 0 diffs; 500 trials / 82,234 frames / 0 diffs / 53 matching deaths / 169 stomps), then `bfscx W11Room1E … 196 0 367 --enemies 0` went **dry in 4.7 s** — root bound exactly 367, frontier 1 → 36 over layers 1–20, all 576 children pruned at step 21, `no goal found within 367 steps` (`runs/P2.5b/w11_d367.log`). Sound because the bound is admissible with enemies: pipe entry forces `x_pos ≥ 0x39400` via the right-foot `cv 0x11` check (emu.rs:485, adder 0x0c00) and the case bound is the x table to exactly that x; and `src/w11enemies.rs` never touches `x_pos`/`x_spd` — its only player write is the stomp bounce `s.y_spd = -4<<8` (line 315), so a bounce cannot beat the x machine. Positive control (runs before the search, main.rs:938–950): the WR's own line replays enemy-aware to `StateChangeVerticalPipe(57,7)` GOAL at step 368 with **0 bound violations over 368 steps**. Forensics: `k+h` = 367 for steps 1–20 and 368 from step 21 on — the one unavoidable frame is an airborne frame of the opening jump; the WR then rides the bound exactly to the earliest admitting pixel (x 0x39410). Exclusion: the (21,7) powerup bump is refused (mushroom outside the model; growth freeze ≫ 1 frame). The d368 *exhaustive* rung was abandoned as infeasible (94 GB at layer 90/368, ≈1.6 TB projected) and was never needed — its reference-path audit is the control. **1-1 is now closed end to end** (with F116). `docs/experiments/P2.5b-room1-search.md` §"The verdict". |
| H30 | **Vine/block-buffer OOB write → EnemyFrenzyBuffer → out-of-range JumpEngine (ACE candidate).** Block-buffer writes use y = ((Y+adder)&$F0) − $20, which wraps to $E0/$F0 for objects above the status bar; from Block_Buffer_2 (odd pages) they land in $06B0–$06CF. `VineObjectHandler` writes the vine metatile $26 at the vine top as it grows past the top of the screen: in an odd page at column 11 that is `EnemyFrenzyBuffer` ($06CB), consumed by `InitEnemyFrenzy` as ID $26 → `sbc #$12` = 20 → `JumpEngine` with 6 entries → jump through the bytes after the table; column 12 sets `SecondaryHardMode`. Vines on the route: none in the WR (4-2 vine = the non-warp top route; 1-2/4-2/8-x vines exist). Test on the core: grow a vine at an odd-page column 11 (which levels have one?), dump RAM/PC; then whether any jump target does anything useful (world counter, axe). | B | **refuted for vines** (2026-08-22): the only vine blocks are GroundArea4 p5 c1, GroundArea5 p8 c3, GroundArea9 p5 c3, GroundArea18 p5 c5, UndergroundArea2 p4 c0 (`tools/area_data.py L_*`) — odd-page columns 1/3/5 write $26 into unnamed RAM $06B1–$06C5, even pages into Block_Buffer_2. Open only for some other object above Y $20 that triggers a block-buffer write at an odd-page column 11/12 (no such writer identified: Mario skips BG collision above the screen, enemies that bump blocks stay below Y $20). | `docs/oob-audit.md` §2 |
| H31 | **A crack hides in an unmodeled mechanic rather than in player movement**: every solved segment has the WR on the player-only bound (F66/F67), so a new frame most plausibly comes from something the player model omits — platform/lift carry (4-2, 8-x), shell interactions, block bumps, or the x-bound itself being wrong. | A/B | untested | STATUS "Model omissions" table; P1.3 done 2026-08-22: the x-bound itself is clean in room 3 (F68) → the crack, if any, is in a mechanic, not the bound; P2.3a (4-2 lift) next. |
| H32 | **The 4-2 lift is a launch pad, not just a bridge**: a jump from the descending lift (the WR: Y 132 at x 986) replaces a floor jump from Y 176; with the lift spawning on the scroll (F73) a different approach to the pit (top route, earlier/later arrival) meets it at a different height — the 2 missing frames of the top route may sit in the lift timing, which no player-only search can see. | A | untested | F73; needs P2.3b (lift + scroll in the model), then `bfsc`/IDA on `W42Main` with deadline 586 (WR−2) |
| H33 | **REFUTED AS STATED (F241/F242/F243, E9a): the entry list is wrong and the census over all nine route areas is empty.** "Non-solid-but-non-empty" is the wrong test — `CheckForSolidMTiles` is a head-bump classifier and the side check blocks on *anything* non-empty (F241). Of H33's proposed entry tiles, **coins cannot work at all** (the right probe collects them 11 px before the left probe needs them — F242, and this is why F93 costs 31 frames), **`$1c`/`$6b` pipe tops are re-probed by `BHalf`** so they give no pass, **climbable diverts to `HandleClimbing`**, and **a bump-created gap makes it worse** (a bumped block becomes `$c4`, a destroyed brick `$00`, an erased coin `$00` — no bump anywhere in SMB1 creates a non-empty non-blocking tile). Only `$5f`/`$60` qualify and **all nine on the route are isolated** (F243). What survives is the *vertical* entry (F244), which is H46's dynamic half. Original text: **Wall-entry trick (F80) as a route tool elsewhere.** `DoPlayerSideCheck` quits after the first side point that touches any tile, so once Mario's x+2 point is inside a brick run he walks through the whole run (moving right; the x+13 point is never checked). Entry needs a non-solid-but-non-empty tile (coin $c2, hidden $5f/$60, climbable, pipe-top $1c/$6b) or a bump-created gap at the side row next to the run; a `$23` bouncing block inside the run stops him. Scan every level on the route (block maps from the dump) for brick/solid runs at Mario's side row (Y+24) adjacent to such a tile, and for runs that would shortcut the route (walls that force a climb or detour). | B | **refuted as stated 2026-08-25 (E9a)** | F241/F242/F243; `runs/E9a/census.txt`; `docs/experiments/P4E-census.md` §1, §3 |
| H34 | **Item-assisted 4-2 routes are outside the player model.** Bumping (28,7)/(55,7) spawns a mushroom, (81,7) a star, (64,3) a vine; the WR itself bumps (28,7) and (64,3) without touching the items (F87). A route that collects the mushroom (big Mario: breaks bricks, different hitbox; the power-up freeze costs frames) or climbs the vine (leads only to the world-5 warp, F41) is not searched by `W42Main`. Prior: low for the mushroom (the growth animation alone costs more than the 2-frame deficit unless big Mario opens a shortcut through the brick mass); the vine route is a different destination. Status: untested; parked until a player-only 4-2 bound exists (P2.3c). **2026-08-23 (P2.5c-2):** `--enemies` searches now *refuse* any transition that bumps an item cell (`LiftEvent::ItemBump`), so their results are exact for item-free routes only. Note the new angle from F100: a bumped item *occupies an enemy slot* — the WR's vine (slot 1 from row 7040) is why its wrong-warp pipe has no plant; a vine-free route gets a plant in the pipe it must enter (every vine-free prefix of the P2.5c-2 difftests had it). So "bump the vine (or the mushroom brick) to fill a slot" is a route property with a real payoff that the refusal excludes — if the enemy-aware search finds the pipe's plant to be the binding constraint, model the items' slot usage (vine: `$2F`, rises then stays; mushroom: `$2E`, ~110 frames) instead of refusing. | **CONFIRMED (2026-08-24 session 12): the vine unlock delivered the record route — main area 553 (F118), the bump free and forced at the bound (F119); mushroom/star remain refused (no need arose)** |
| H35 | **The 4-2 warp zone holds ≤ 15 frames, probably far fewer, and none before the drop.** The staged bound (F89) leaves 15 frames between the x-optimal two-phase plan and the WR, all in the final approach/return; the return itself is optimal from the WR's drop state (F90). Any gain must come from a different drop state (arrival speed / height at x ≥ 957) and is bounded by 15 − (fall + landing coupling ≈ 5) ≈ 10. Decides nothing alone: 4-2's framerule deficit is 13 (10 after F88), so a warp-zone gain only matters together with main-area frames. Test: an exhaustive Phase-B search from *all* drop states (x ∈ [957, 979], roof level, all speeds) — a multi-root `bfscx` run, cloud-sized — or an end-game (x,y) bound + the full run. | untested |
| H36 | **The 4-2 top route may already be a framerule gain.** HappyLee's claim (F36): the top route is 2 frames short. The pipe-entry frame (F79's late Down, 1 frame) is a property of the pipe, not the route, so it plausibly transfers → 1 frame short. The other 2 of the 3 frames found at the end of his bottom route (F88) come from his braking approach from the floor and do NOT transfer automatically; the top route's own ending (arriving from the upper floor) is unexamined and may hold its own slack — his endings are now known to be loose. No top-route movie exists to check; the test is the main-area search with the top route allowed (P2.3c-2c bound + a cloud run), or constructing a top-route reference with IDA\*/`bfscx` from the upper floor and checking its last ~20 frames exhaustively as for F88. Prior: medium — this is the most hopeful concrete lead in the project. **2026-08-22 (P2.3c-2c):** the enemy-free model should put the top route near 550–555 (the WR's only big losses are the entrance fall and the bottom route's wall entry, F94), but the top floor carries a 3-goomba group (`$3A` at col 46, y 112) that cannot be jumped under the hanging mass — the real top route has three forced stomps, which is where HappyLee's 577 presumably comes from. The test therefore needs the 4-2 enemy module (P2.5c) + the y-coupled bound (F95), not just the player model.  **2026-08-23 (P2.5c-1, F99/F100):** the real obstacle course of the top route is now known from the dump: the goomba group (x 690/713/737 at Y 112, walking left), the lift, **pipe A's piranha plant** (rises as soon as spawned, 47 frames up, 64 idle, 47 down — Mario must cross pipe A in a down window or from above its hitbox), the koopa at the bricks (x 1232 → 1183), **pipe B's plant (up when the WR passes; the jump over pipe B must clear y 72..96 + its box)**, the beetle at the wrong-warp pipe, and — route-dependent — **a plant in the wrong-warp pipe itself if an enemy slot is free when the parser renders col 84** (the WR had all five slots full; a top route that leaves one free gets a plant in the pipe it must enter). The enemy-free 550–555 estimate is therefore an underestimate for reasons beyond the stomps; the module must carry slot occupancy. **2026-08-23 (P2.5c-2, F101/F102):** the module is validated on the core (0 differences over 877k frames incl. 3,686 stomps / 949 kicks) and wired into `bfscx` with death pruning; the test of H36 is now a tooling question (the y-bound must admit stomp bounces, design §7) plus the run. **2026-08-23 (later): the bounce relaxation is implemented and audited (0 violations, §step 4), and the first top-route number exists — S1 (root → x ≥ 339 ∧ Y ≤ 112) = 149 exactly, core-verified.** The remaining chain: S2 (x ≥ 755 ∧ Y ≤ 112, enemy-aware, from `chain_s1.bin`), S3, S4 → the top-route total vs 575 (= 588 − 13, one framerule). Two facts from the validation bear on the route: a stomp needs Mario *descending* on an even frame (a landing frame is a death, F101's ordering) — the three goomba stomps under the hanging mass are timing-constrained, not free; and a vine-free route faces a plant in the wrong-warp pipe (see H34). | **REOPENED 2026-08-24 (session 16, F127): the "structural dead end" was a search artifact.** Movement confirmed beyond the claim: the top route reaches the pipe in 553 (F118), 1 under the two-framerule line. The warp was declared structurally infeasible (F122) because five searches held the scroll offset at 112 — but a diversity beam (P2.3c-8/H39) on the *same* root and deadline carries it to **126**, core-verified on QuickNES, via 19 frames of held Left that every earlier beam deleted on sight. The top route needs **132** and now reaches **126: a 6 px gap, not a mint-free route.** **Session 16 continued:** at deadline 90 the diversity beam reaches offset **132** and reports a real, core-verified pipe entry at 596 frames (509 + 87) — x 1348 / ScreenLeft 1216 / AreaPointer $2F, byte-identical to the WR's entry. **It still warps wrong (F129):** the sct-freeze mint is transient, so the scroll catches up during the descent, crosses 1217 two frames in, and the destination flips to $42. The warp needs `Player_X_Scroll` = 0 at entry — a second condition the case never modelled. Status: **open**, target sharpened from "can it mint?" (yes) to "can it enter *stopped* at x 1348, and what does that cost?" — the F123 speed-pricing question, now for the top route. 596 is a beam upper bound, not an optimum; the 575 line needs 21 more frames. The valid wrong warp needs +20 px of scroll offset the top route cannot mint (minting = the bottom route's col-30 floor wall walk; the top route can't reach floor past pipe A, F117; offset frozen at 112 across 5 beam+exhaustive searches). So the 553 is a movement result that CANNOT become a record via the top route — not "2 frames short," structurally warp-infeasible (strong evidence, not formal proof). The cloud fork is moot. Remaining 4-2 threads: H35 (warp zone) + bottom-route deficit 10 (F88), neither cheap. |
| H37 | **REFUTED (F228): cols 78-79 is a `VerticalPipe(decoration)`, whose top metatiles are `$13/$12` (`VerticalPipeData`, smbdis 3837), and `HandlePipeEntry` requires `$11`/`$10` — it can never be entered however Mario arrives.** Original text: **Pipe B (4-2 main, cols 78–79, rows 4–10) is not a provable "Y ≤ 71" gate: the wall-entry mechanics may let Mario in at floor level.** The y-coupled bound was first specified (session 8) with a hard gate "Y ≤ 71 at x+13 ≥ 1248" — but the WR's own col-30 entry (F80/F93: foot-check impede with `Player_MovingDir` LEFT pushes +1 px into a wall while the side check is skipped; once x+2 is inside, the side check never impedes rightward motion) and the one-frame landing inside a wall face (the foot check lands Mario on any solid cell's top window before the side check pushes him out — SMB1's wall jump) are general mechanics, and nothing in the geometry of pipe B obviously forbids them: a Mario hopping against the pipe at Y ∈ [165,175] has his feet inside (78,10) with nybble ≥ 5. If a floor-level entry into pipe B exists it is a new route (HappyLee jumps over the pipe), not just a bound question. Test: a short `bfscx --goal-x 1262 --goal-y 176` segment from a state in front of pipe B (a WR state at x ≈ 1200, prefix from the WR inputs) with a small deadline ladder — either a path (then replay on the core, F80-style) or a dead frontier (then a proof that the col-78 face cannot be entered, and gate 2 becomes sound). Until then the main-area bound uses no pipe-B gate (`ygate` is used only for position goals, where the goal itself is the condition). | untested — **still untested after the 2026-08-24 floor-clip ladder** (plan step (i), d62–d82 from the 465 chain): no rooted path reached pipe B at all, every one slams pipe A's face at step 469 (F117); the test still needs a root on the floor in front of pipe B (a grounded pick at x ≥ 1200 from a chain-safe 415, or a WR-derived state) |
| H38 | **A speed-preserving 4-2 offset mint exists** (something that raises Mario's screen-relative x without sacrificing his speed to an impede — an enemy push, a lift/platform carry (`Platform_X_Scroll` feeds the scroll!), a pipe-exit placement, or an unmodeled displacement). If one exists with ≤ ~2 frames of cost, the bottom route's ~584 floor drops toward 575 and F123's closure fails. The known mechanisms all price the mint at ~30 frames (F123). Test: enumerate every writer of Player_X_Position outside the movement/impede path in the disassembly (enemy collisions, platforms, `PositionPlayerOnVPlat`, vine placement) and check which feed `Player_X_Scroll`/the scroll handler; then a targeted search per candidate. | B | **refuted for 4-2** (2026-08-24, P2.3c-7, code-level): the enumeration is complete — the vine snap (the prime suspect, up to +14 px) is an irreversible autoclimb warp commitment (side-point-only grabs × rows-≤2 cells ⇒ Y < 32 ⇒ GES 1 ⇒ forced Up ⇒ area change to $2F) and would net ≤ +14 at wall-mint rates even if leave-able (~578–581 > 575); flagpole/platform-carry/enemy-push/jumpspring/edge-clamp all absent or x-inert in 4-2 main. Open for OTHER levels: horizontal platforms feed `Platform_X_Scroll` (anti-mint but proves the machinery), springs/flagpole snaps exist elsewhere. | `docs/experiments/P2.3c-2c-main-area.md` §P2.3c-7 (labels: PutPlayerOnVine 12206, VineCollision, Vine_AutoClimb 5666, ClimbingSub 5966, WrCMTile 6702); F120/F122/F123 |
| H39 | **Every beam we have run is a per-layer first-arrival gate, so it is structurally blind to exactly the states a seam hides — and so are the segment cuts.** `bfscx --beam N` keeps the N lowest-h records per layer (`--beam-offset`: the N highest-offset, then h). Both are *global single-key* orders, i.e. greedy: any maneuver that must **pay first and gain later** has worsening h (or a non-monotone offset) on the very layers it needs to survive, so it is deleted before it can pay. This is the same lossy operation as a first-arrival segment gate, applied every frame instead of every seam — a beam does not escape segment-greedy search, it *is* segment-greedy search at 1-frame granularity. The engine already half-knew this: the `--beam-offset` comment says "a minting maneuver costs (slows, backs off x) before it pays, so h-first prunes it the instant it slows" — the fix applied was a *second global key*, which merely moves the blindness to a different axis (F122's offset then froze at 112 across all five runs). The seam protocol's step 3 ("reconstruct TWO candidates differing in vertical situation") is the same insight applied by hand at width 2. **Fix: bucketed (diversity) beam** — keep the best N *per bucket* over coarse physical dimensions (scroll offset, y band, x-speed band, x subpixel phase — F11/F37 subspeed dependence; runbook §3 notes every seam failure so far was vertical), never the best N overall. **Corollary that reprioritises live work:** F125 closed 8-4 room 3's return leg *from the WR's apex* (h(apex) = 33 = WR, the 32-rung dry at layer 1) and concluded H25's frame is "in the approach **or in reaching a different apex state**". The planned next step — segment the approach with a goal at the WR's apex, then chain — can therefore only find "reach the WR's apex sooner", and the WR's apex is already proven not to yield the frame. **That plan is provably incapable of finding H25's frame**; the approach search must instead emit a *set* of apex states each carrying its own return cost (multi-goal seam / multi-root return). | infra/A | **CONFIRMED (F128), with F127 as the casualty** | **Both tests done 2026-08-24 (session 16).** (a) `--beam-buckets` shipped; beam-off byte-identical on the control gate (F128). (b) The F122 re-test **moved the offset**: 112 → **126** on the same root/deadline/budget, core-verified on QuickNES (F127). The minting maneuver is **19 frames of held Left** — motion away from the goal — which the lowest-h beam deletes on its first frame, exactly as predicted. So F122's search evidence WAS an artifact of the pruning order; 4-2's top route is 6 px short of the warp threshold, not structurally mint-free. Open follow-on: **every first-arrival segment gate in the project carries the same defect** — the seam protocol's width-2 hand-picking is a partial mitigation, not a fix (see the P2.2a apex corollary, which is now unblocked). `docs/experiments/P2.3c-8-beam-diversity.md` |
| H40 | **Bucket keys should be derived from the downstream code's branch variables, not from generic state diversity — but only once the mechanism is known.** A bucketed beam's cost is the *product* of its dimensions' cardinalities, so generic axes buy diversity across variables the game may never read. The cheaper key is exactly what the downstream routine branches on. Worked and verified example (F130): the 8-4 cheep frenzy reads `Player_X_Speed` only as a 3-valued class and places spawns *relative* to Mario, so its key is ≈ (`FrenzyEnemyTimer` phase, speed-class ∈ 3, slot occupancy) — about 3×, and absolute X is not a dimension at all. **Scope limit (this is the important half):** a code-derived key requires already knowing which routine matters. F122 is the counter-case — nobody knew the mint was reachable, so there was no 'downstream routine' to key on, and it took *generic* diversity (offset × y × speed × subpixel) to find it (F127/F128). So the two are **complementary, not competing**: generic diversity for **discovery** (when the mechanism is unknown), code-derived buckets for **exploitation** (when it is). Using a code-derived key during discovery re-creates the very blindness H39 describes, just with a better-chosen blind spot. Cost note: `--beam-max` already bounds the product (it shrinks per-bucket width first and logs `WARNING capped` before dropping any bucket), so the multiplicative cost is a tuning question, not a correctness one. | infra | **confirmed for exploitation, scoped** | F130 (the worked example, verified in the disassembly); F127/F128 (the counter-case where generic diversity was necessary). Apply: use code-derived keys for the `w84enemies` port and any modelled-mechanism search; keep generic keys for exploratory rungs |
| H41 | **Inside a framerule-flat region the beam's OBJECTIVE is wrong, not just its diversity.** Below a rule boundary a saved frame is worth zero (F27/F28), so an h-ordered beam ranks candidates on a quantity with no marginal value and pays for it at the seam by discarding exit-state quality. Where the slack to the next boundary is known, the beam should score **exit-state quality** and treat time as a **hard constraint** (h ≤ remaining budget) rather than as the objective. **Scope: this does NOT apply to 8-4**, which is unquantized — every frame there counts 1:1 (H24, F27/F28), so time *is* the objective and an h-ordered beam is correctly aimed. It applies to 4-2, 1-1, 1-2, 8-3 and any framerule-bound level, and to the seams inside them (the 553 G-line's seven cuts). | A/infra | untested | Origin: session 16 review notes. Test cheaply on the 4-2 chain revalidation (P2.3c-11): re-run the seven seams with time as a constraint and an exit-quality score, and compare against 553 |
| H42 | **The record hides at MrWint's segment seams, and the WR inherits every loss in his decomposition.** The argument is tight and rests on F66 rather than on speculation: F66 shows the WR **equals** MrWint's enemy-free optimum on every segment he solved, gap 0 — which means **the WR is provably no better than a segment-decomposed optimum on those stretches.** Wherever that decomposition is lossy, the WR is lossy in the same place, and nobody has ever searched across the boundaries. **Three pieces of direct evidence that the decomposition IS lossy:** (1) `docs/experiments/P2.5-segment-scan.md` records that the two 8-4 pipe-clip segments start from **hand-picked mid-air states** in MrWint's files (different y from the WR's) — a hand-chosen boundary state is the lossiest seam there is, and only the frame count *from the same x* was ever compared; (2) the one row in F66's table where a cross-seam comparison exists, 1-1 stairs → glitch grab, has MrWint's segmented solution at 59 (+8) = **67** and the WR at **66** — **gap −1, the decomposition lost a frame**; (3) our own 553 was built the same way and STATUS still carries "552-vs-553, at most one frame open". Add F115 (a maneuver first-arrival gates provably cannot cut) and F127/F128 (the same defect, at 1-frame granularity, cost us a whole verdict today). **The seams are enumerable** from F66's table. 8-4 (unquantized — one frame IS the record, H24): room 1 `W84Speedup` → x 187, then a 445-frame middle; room 2 `W84Part2Speedup` → x 1981, **157-frame middle at the speed cap**, `W84Part2VertPipeEntry` from **x 2373**; room 3 `W84Part3Speedup` → x 3260, 51-frame middle, `W84Part3VertPipeEntry` from **x 3388**. **Method:** dissolve one seam by searching the two adjacent segments as ONE span with a diversity beam (H39/F128), deadline = their known combined cost, then core-replay any improvement. Short spans, so the beam can be wide enough to matter. **Status:** room 3's two seams are being dissolved right now by P2.2a′ (root → 162 unsegmented, which spans both x 3260 and x 3388) — and note the earlier P2.2a checkpoint independently guessed this, calling the candidate "the seam into the F66-optimal 74-frame clip". Room 2's x-1981/x-2373 pair is untouched and is the next target. | A | **in-progress — room 2's x-2373 seam TESTED and NOT LEAKING (F139, s17); its x-1981 seam still open; room 3 via P2.2a′** | Origin: the user, session 16. F66 + `P2.5-segment-scan.md` (hand-picked seam states; the 1-1 −1 row); F115; F127/F128; F133. **Sub-question CHECKED 2026-08-24 s16 (the user's follow-on: are the seams placed where the ENEMIES are?) — partially confirmed, and it raises P2.2f's prior.** Direct evidence: MrWint's own source comments a segment as *"segment is enemy-free (E_CastleArea6: nothing between the pages 8-9 lifts, erased by control, and page 18)"* (`w84.rs:485`) — he explicitly reasoned about enemy placement to choose the boundary. Decoding `E_CastleArea6` (`tools/area_data.py`) against the seam x values: room 2's 157-frame middle (x 1981 → 2373) **contains four enemy objects** — two Buzzy Beetles ($02) at x 2048 and 2080, and two id-$0e objects at x 2224 and 2256 — and the clip seam at **x 2373 sits exactly between that page-8 group and the next pair at x 2480/2512**. **But it is not the universal explanation:** pages 10–13 carry no enemy objects at all (page-skip 9 → 13, then the area-change command), so **room 3's seams (x 3260, x 3388) are geometric — the turnaround — not enemy-driven.** Net: enemy avoidance drove at least part of the decomposition, so the enemy-adjacent seams are exactly where generate-and-test buys coverage nobody has ever had — and room 2's x-2373 clip seam is one of them. **FIRST SEAM ANSWERED, 2026-08-24 s17 (F139).** The seam positions were measured in the WR rather than
| H45 | **1-2's scroll lock costs the WR a frame, and the cost may be avoidable — the search can now see it.** F150: when the area parser reaches column 178 it sets `ScrollLock`, and the **next** frame's `ScrollHandler` takes `InitScrlAmt` — the screen does not scroll at all. On the WR that frame would have scrolled 2-3 px, and since 1-2's warp zone arms on `ScreenLeft` reaching 2816, **those pixels are frames**. But the freeze only costs something if Mario was going to scroll on that frame: `ScrollHandler` already bails when `Player_Pos_ForScroll < 0x50`, when `SideCollisionTimer` is running, or when `Player_X_Scroll` is 0. And **the frame on which column 178 is parsed is itself scroll-driven** (bursts fire per 32 px), so a route has some control over where the freeze lands. Two shapes to test: (a) land the freeze on a frame Mario is not pushing the scroll anyway; (b) note that the `WarpZoneObject` only clears the lock when Mario's Y is **even** — an odd Y holds it, which is a pure loss, so any path must be checked for it. **Prior: moderate.** The freeze is one frame, and 1-2's deficit is 5; but this is a mechanism the model was blind to until now, so it has never been searched. **Test:** it needs no new machinery — the corrected `W12Warp` prices it automatically; watch whether a goal path's `ScrollLock` frame differs from the WR's step 1057, and whether the arming step beats 1233. | OPEN (raised s17, 2026-08-25) | `docs/facts.md` F150; `docs/experiments/P2.3e-framerule-scan.md` §29 |
inferred: MrWint's boundaries are at **step 70** (x 1981) and **step 227** (x 2373). Rooting on the WR's own
state and searching the tail **exhaustively** (no beam, so a dry is a verdict and a goal would be a real
improvement on the WR), prefix 220 / deadline 46 spans steps 220 → 267 and therefore **crosses the x-2373
seam** — and it is **DRY**, with a passing positive control at the WR's own deadline (prefix 240 d27 finds
goals; d26 dry). **So that seam is not leaking a frame, and the WR's last 47 frames of room 2 are optimal.**
The x-1981 seam is ~197 frames from the goal and the exhaustive ladder's cost wall is ~50 frames (F98 at 14
frames of bound slack), so it cannot be reached by extending the horizon; a bucketed beam over it drops the
WR's own path at layer 41 (F138). It is gated behind the coupled end-game term (§13 of the experiment file).
**Scope note:** this answers the seam for room 2 only, from the WR's own arrival state; it says nothing
about room 1, room 3, or the other levels' seams. Unit: P2.2f |
| H43 | **Cart-swap-free ACE via the enemy frenzy buffer — BLOCKED at value-minting (F215), and all three previously-unaudited writer classes now audited (F218-F221) without producing an arbitrary write.** F206 shows a complete chain: a block-buffer write to odd-page **column 11**, with the writing object above Y $20 so the row index wraps to $F0, lands on `EnemyFrenzyBuffer` ($06CB) — inside the writable window whose $06CF ceiling F203 proved tight; `CheckFrenzyBuffer` copies it unchecked into `Enemy_ID`; and both enemy JumpEngines (55-entry init, 34-entry run) accept indices far past their tables, so `jmp ($06)` takes PC from the bytes after the table. Two gaps remain, and they are the hypothesis: **(a)** enumerate which *values* the block-buffer writers can actually place (they write metatile numbers, not arbitrary bytes) and whether any of them, as an `Enemy_ID`, lands on a useful pointer; **(b)** exhibit a route position where Mario can bump a block with Y+adder < $20 on **odd-page column 11** — F207 identified the writer as `PlayerHeadCollision` + `BlockObjMT_Updater` (the bump latches the wrapped address and a later frame writes `Block_Metatile` to it), and note the vine is *explicitly* guarded (`cpy #$d0 / bcs ExitVH`) so it can never write OOB at all, which is stronger than H30's level-data argument; **(c)** compute where the out-of-table pointer actually points (index $c4 -> 27-28 bytes past the table) and whether it can be steered to an ending rather than a crash — the sweep shows $c4/$58 die in ~165 frames and $5d diverges for the whole movie, so it jumps *somewhere*, but nowhere useful yet. | B | **CLOSED 2026-08-25 (F262): the arbitrary jump exists and can never be armed.** F207/F208 confirmed it fires when `$06CB` is poked to `$c4`, but `$c4` is not in the cell's writable set and neither is any value >= $37 — the complete writer enumeration is in F262, and the two classes P3.1 §4 left uncovered (`VRAM_Buffer`, stack) are page-bounded away from the target. Residuals are named in F262 (the `MetatileBuffer` start-row overrun, a misaligned `AreaDataOffset`, power-on state). Previously: **(b) REFUTED at code level 2026-08-25 (F252) — there is no such route position, because there is no such Y.** `PlayerBGCollision` is entered only past `ChkOnScr` (11919-11926), which requires `Player_Y_HighPos` = 1 **and** `Player_Y_Position` < $CF; every Y in F210's and F216's windows is >= $CF, and "above the top of the screen" is `Player_Y_HighPos` = 0. The reachable head row is $00-$C0 for every size/crouch/swim combination, and the feet and both side probes close the same way — **no player-driven block-buffer access can leave the buffer.** With F215 (values) and F203 (address ceiling) the block-buffer mechanism is now closed on BOTH axes. What remains of H43 is only P3.1 §4's unaudited write classes (stack over/underflow, non-indexed writes, `VRAM_Buffer` overflow) — see `docs/experiments/E10-rom-read.md` §6. Note F253: the *enemy* path (`HandleEToBGCollision`, enemy Y in {6,7}) IS unguarded and does reach row $F0, but writes only `$00`, which F215 already shows is inert as an `Enemy_ID`. | P3.3. Relevant: this is the cart-swap-free version of the existing ACE branch (PLAN.md: OnehundredthCoin 4:52.65 needs an SMB3->SMB1 hot-swap to seed $07FD), and D2 already makes a category-creating glitch under 17,868 frames a success. Note F205's caveat: the band-A sweep poked one frame per level, which undersamples $06CB badly, so its silence is weak evidence here. |
| H44 | **An off-WR-route destination may be faster than the WR route.** Raised by the user, session 17, while reviewing the P3.2 steering map. The frenzy-buffer jump (H43/F206-F208) can redirect area loading, and a redirect need not land on the WR's own itinerary to be worth frames: e.g. a jump from 1-2 into some world-5 or world-6 level whose remaining path to 8-4's axe is shorter than 1-2 -> 4-1 -> 4-2 -> 8-1 -> 8-2 -> 8-3 -> 8-4. **Why our current measurement is blind to this:** P3.2's route-progress predicate scores a run by whether it reaches a `(WorldNumber, AreaPointer)` pair **the WR itself visits**, earlier than the WR did. Any destination off that itinerary scores zero, however good it is. A route-agnostic `world_ahead` column (first frame each `WorldNumber` is reached vs baseline) was added the same session as a partial fix — "got to world 8 sooner" is progress by any path — but it still cannot price a *lateral* move (a different world-5 level). **What a real answer needs:** a cost model for "frames from an arbitrary `(world, level, area, entry state)` to the ending", which the project does not have — the per-level frame table (Key numbers) covers only the 8 levels on the WR route. Cheapest first step would be measuring load-to-load frames for the off-route levels, which requires driving the core through them rather than replaying the WR. **Priority: parked, explicitly not refuted.** The user's own framing: worth knowing what the jump can do, but the WR path is the priority. Revisit if H43 produces a controllable redirect, at which point "where can it send us" becomes the operative question. | B | parked (raised + scoped, session 17) | Depends on H43 having a real trigger (F210's entrance). See `docs/experiments/P3.2-ram-oracle.md` §12 |
| H46 | **STATIC HALF CLOSED, EMPTY AT BOTH HITBOXES (F243, E9a 2026-08-25); the DYNAMIC half is open and is E9b.** 708 faces across the 14 controllable route areas, 703 with an empty left neighbour, 5 coins killed by F242, all nine hidden blocks isolated, and big Mario's two-row left probe *reduces* rather than resolves (F243). The primitive that is still live is **vertical**: F244's sink-into-the-top with `Player_MovingDir` LEFT, which skips `DoPlayerSideCheck` entirely and is invisible to a static map. E9a also produced the value filter H46 never had: the route's whole geometric loss is **290 frames** and where it sits (F245). Original text: **The wall-face census: SMB1's two collision-entry primitives have never been applied route-wide, and at least one unexploited face exists.** The primitives are general mechanics, not site trivia: **F80/H33 the walk-through** (`DoPlayerSideCheck` returns after the *first* side point that touches a metatile, so once standing Mario's x+2 point is inside a solid run the x+13 point is never tested and he walks through **at full speed** — F239 shows the WR doing exactly this for 250 px in 4-2 with `Player_X_Speed` pinned at 40), and **F93 the foot-check entry** (a Left press makes `Player_MovingDir` LEFT so the foot impede pushes +1 px *into* the wall; costs the speed and therefore mints scroll offset, F120). Tested to date: 1-2's clip (F143/F144), 8-4's pipe clips (F66), one failed attempt at 4-2's pipe B (H37, refuted at data level by F228). **Why it stalled: a rooting problem, not a clip problem** — every tool here searches from a fixed root over a long horizon, so testing a face at x 1200 means *searching your way there* first, and that search fails for reasons unrelated to the face (F117: every ladder slams pipe A). **The fix is already in the repo (F50):** savestate = template + patched RAM, so root directly at each face and sweep the small local box (x subpixel, y over the standing/jumping/falling band, x-speed, `Player_MovingDir`) with a short local-exhaustive search. Run it at **both hitboxes** (F238: big-Mario side-point geometry is untested route-wide; this folds in H18 nearly free). Verdict per face: admits-walk-through / admits-foot-entry / refuses. | A/B | **static half closed / dynamic half untested** | `docs/experiments/P4E-census.md`; `runs/E9a/`; raised in the Mac session 2026-08-25; prior raised by E7, which found the same lever by algebra on one level (`docs/experiments/P4E-finder.md` §E7). Enumerate faces with `tools/area_data.py` over the nine route areas already decoded for F233. Falsification: a completed census marking every face "refuses" at both hitboxes — a much stronger statement about clips than anything currently on the board. |
| H47 | **An over-cap forward displacement mechanism exists** — something that moves Mario forward faster than the running cap (40 subpixels/frame): a collision push chain, a platform/lift carry, an enemy interaction, a coordinate wrap. | A | untested, **prior low — but this is the unstated premise of every closure on the board** | Every x-table lower bound prices per-frame progress at the cap, so **F124 (1-1 closed), F225 (4-1/8-1/8-3 cannot deliver a framerule from movement), F122/F123 (4-2 closed both routes), F232's movement half, and F152's 1-2 loss map all become inadmissible simultaneously if H47 holds.** The bounds are loose in the *right* direction only while nothing beats the cap (F153: the x bound ignores walls, so it under-prices time). Why the prior is low: F227 enumerates every scroll freeze and finds only `SideCollisionTimer`, whose sole writer zeroes `Player_X_Speed` on the same path; F120 shows collision-push displacement moves the player without feeding the scroll (displacement, not speed); F231's coordinate wrap in 8-4 room 1 is level-design looping, not a gain. **Action: no new search — state the premise once so that if a mechanism ever turns up it is immediately obvious what it invalidates.** Raised in the Mac session 2026-08-25. |
| H48 | **8-2's col-206/207 wall can be jumped outright from the col-203 pillar, saving most of the WR's 114-frame wall-jump climb (F245).** The wall's right-probe window is `x ∈ [3283, 3314]` and it clears iff the side row is <= 2, i.e. **`Y <= 55`**; from the pillar top (`Y` = 96) that needs **41 px of rise before x = 3283**, and the WR's own measured full-speed A-held arc gives 42 px at `Δx` 25 — so the jump must be issued at **`x <= 3259`**. He is grounded on the pillar for `x ∈ [3236, 3260]` (`DoFootCheck` takes either foot) but lands at **x 3258**, and the first issuable jump frame is `x ≈ 3260`. **It misses by 1-2 px.** Land 3-10 px earlier and the same arc clears with 10-25 px of margin. The cost of landing earlier is a flatter approach jump out of x 3161 that must still clear the col-199/201 staircase (top row 8, `Y` = 128; the WR passes it at y 126, 2 px of margin) — the two constraints fight, and a local search rooted at dump ~12160 with a **subpixel cell key** settles it. Falsification: an exhaustive sweep of pillar-landing states shows none with `x <= 3252` and speed >= 39, or shows the jump still short. | A | untested | F245; `docs/experiments/P4E-census.md` §5; the running `runs/E8-w82/climb.log` is rooted too late (12240) and has **no `--subcell`**, so it cannot see a 1-2 px question |
| H49 | **REFUTED the same session by direct core measurement (F251): the switch works and the arithmetic is against us.** Poking `$06D6` mid-descent collapses `PlayerEntrance` from **96 frames to 1** with the pipe destination unchanged — exactly as F249 predicted — but `AltEntranceControl` = 0 is the *only* value that can show the intermission card, and `DisplayIntermediate` shows it **unconditionally on castle levels** (`cpy #$03 / beq PlayerInter`, bypassing `DisableIntermediate`). Card = 137 frames against 96 saved: **42 frames worse**, measured. Mode 0 also spawns from `HalfwayPage` rather than `EntrancePage`, i.e. at the room's start. **Residual (the only live piece): the water-room transition (load 16598) has a non-castle destination, so `DisplayIntermediate` there DOES consult `DisableIntermediate` ($0769) — `$06D6` non-zero AND `$0769` non-zero during that one descent is ~96 frames.** Both bytes are above F203's `$06CF` ceiling, so this is now a target for E10's write-primitive audit, not a hypothesis that can be tested today. Original text: **Collect 8-4's pipe-exit animation: make `WarpZoneControl` non-zero mid-descent and turn a 122-frame entrance into a 43-frame one, three times (F249/F250).** 8-4 is the only unquantized level, so this is worth **79 frames per firing** at face value. The chain is complete at code level and every link but one is already proven: `$06CB = $34` -> `CheckFrenzyBuffer` copies it into `Enemy_ID` unchecked (F206) -> enemy routine `$34` = `WarpZoneObject` -> `inc WarpZoneControl` given `ScrollLock != 0` and Mario's Y even -> `VerticalPipeEntry` picks mode 0 on the commit frame -> `PlayerEntrance` skips `EntrMode2`'s 96-frame rise. **`$06CB` is inside F203's proven `$06CF` OOB ceiling**, which `$06D6` is not. **Three open sub-questions, in order:** (a) **is mode 0's starting position usable in those rooms?** — `Entrance_GameTimerSetup` puts Mario at `PlayerStarting_X_Pos[0]` = `$28` with a header-derived Y, not at the pipe mouth, and the naive poke test dies on the WR's stale inputs; measure where he actually lands in each room before anything else, because if he lands in a pit the whole line is dead. (b) **can `ScrollLock` be non-zero in 8-4 at a pipe descent?** — enumerate 8-4's `ScrollLockObject`s with `tools/area_data.py`. (c) **H43(b) unchanged** — a head bump at `Y + adder < $20` on an odd-page column 11 (F207's latch) somewhere in 8-4. **Cheapest first step is (a) and it needs no exploit at all**: poke `$06D6` and read Mario's spawn position, which is ~20 minutes. | B | **refuted 2026-08-25 (F251); residual folded into E10** | F249/F250/F251; raised 2026-08-25 session 19 from the user's question about transition screens; `build/ram_oracle --at 15773 --addr-lo 0x6d6` |
| H50 | **REFUTED AS A ROUTE 2026-08-25 (F262/F263): the mechanism is real and measured, the injector does not exist and cannot be made to exist.** The writable value set of the two frenzy cells is `$06CB` in {$00,$12,$14,$15,$16,$17} and `$06CD` in {$00,$14,$17,$18} — `$31` is in neither; `CastleObject`, the only `$31` writer in the ROM, fires once per non-page-0 castle and **no area in the game has two** (F263); and the two write classes that were still unread cannot reach page 6 at all (`VRAM_Buffer` tops out at $0444, the stack at $01FF). Kept in the ledger as a **measured price on any future write primitive**, not as an open lead. Original: **CONSEQUENCE MEASURED: 857 FRAMES AT N = 2 (F258), 1,329 WITH THE WIN MUSIC ALSO DROPPED (F259) — the mechanism is confirmed on the core across all five flag levels; what remains is solely the injector(s).** Two sessions measured this independently the same day and agree on the 857 (`tools/starflag_probe.py` and `tools/starflag_poke.py`). The extension (F259): **N > 2 buys nothing** — the exit frame is identical at N = 3/4/5 because `DelayToAreaEnd`'s second condition, `EventMusicBuffer == 0`, becomes the floor (which is also why 8-1 gains only 78 and its wait *grows* 109 → 132). `PlayerEndLevel` queues that music only if `ScrollLock` is still set when Mario passes Y >= $AE, so clearing `$0723` one frame earlier removes it: **`StarFlagTaskControl` then goes 3 → 5 in a SINGLE frame** and the gains become 312/289/210/278/240 = **1,329**. Run health-checked past the early exit (1-2 loads, timer reloads to 400, lives intact). **Second injector question (F260):** none of the five flag levels contains a `ScrollLockObject`, so the music half needs its own write. So H50 banks 857 on one write and 1,329 on two — and it is the *payoff* attached to H43's missing primitive rather than a separate lead. A second `Enemy_ID = $31` (StarFlagObject) at a flagpole deletes the framerule and divides the timer countdown.** `RunStarFlagObj` is dispatched once per frame **per enemy slot** holding $31, but the state it drives is global — except the one byte the framerule waits on, which is per slot (F254). Consequences with N objects: task 2 `AwardGameTimerPoints` subtracts N units per frame (countdown ⌈T/N⌉+1 instead of T+1), and task 4 `DelayToAreaEnd` reads the *second* object's never-written `EnemyIntervalTimer` = 0 and advances to task 5 in the same frame — **so F27's (v+1)+105 wait, which IS the framerule, becomes 1 frame.** Priced at N = 2 on the WR: **≈ 1,319 frames** (1-1 310, 4-1 287, 8-1 208, 8-2 276, 8-3 238), *and* all five flag levels become unquantized like 8-4, so `docs/open-threads.md`'s "a level only pays if it saves its whole deficit" stops holding. Blocked today by `CastleObject`'s `lda CurrentPageLoc / beq ExitCastle` (which suppresses the page-0 castles 4-1/8-1/8-2/8-3 all carry) and by the unconditional left-side offscreen erase; the only injectors for a second $31 are the frenzy cells (F206), whose write geometry F252 has just closed. **But it can be priced without the exploit, and that is the next step:** `build/harness --poke` (added for F251) — set `Enemy_ID+k = $31` and `Enemy_Flag+k = 1` for a spare slot a few frames before 1-1's grab and read the next area-load frame. One ~20-minute run confirms or kills both halves. **This is the highest-value untested claim on the board and the cheapest to settle.** | B | untested — **measure first (poke), then ask about reachability** | F254, F27, F206, F252; `docs/experiments/E10-rom-read.md` §2 |
