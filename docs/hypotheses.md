# Hypotheses ledger

Every idea for saving time lives here. Statuses: **untested → in-progress → confirmed /
refuted / parked**. *Refuted requires a proof artifact* (search record or code-level argument
with a reproducing script). "Nobody has done it" is never evidence. Parked = deprioritized,
say why. Link each status change to the experiment file or script.

| ID | Hypothesis | Track | Status | Evidence / notes |
|---|---|---|---|---|
| H1 | **Ending-input coast.** The movie ends at last input; a final jump from farther away (or an enemy/fireball bounce) could make the last input earlier while Mario still reaches the axe. | A | in-progress (baseline measured) | WR already coasts: last input = A press on frame 17848 (0-based), axe reached within the next 19 frames (F16). Question now: can the final jump start earlier / the axe be reached earlier — P2.2 |
| H2 | **Lag frames.** #1715M contains lag frames that could be removed via object/scroll management; each is a frame in 8-4 or a possible framerule flip elsewhere. | A | parked | P0.4: 24 lag frames, all boot/Start/area-load frames, none in-level (F30). Only a load-frame removal would count; revisit in P0.5 (is the load lag avoidable at all?). |
| H3 | **Framerule-phase manipulation.** Some action (pause, Select, area transition, pipe, vine, death, soft reset, lag) shifts IntervalTimerControl's phase relative to the level timeline, letting a level with slack k be re-aligned. | A/B | untested | P0.5 timing model; then search |
| H4 | **Time-bonus countdown.** (Confirmed 1 frame per unit, F27.) If the end-of-level countdown burns ~1 frame per remaining timer unit before framerule rounding, the remaining-timer value (and the 24-frame tick phase at flagpole touch) is a lever; also check whether any glitch lowers/zeroes the timer. | A | untested | P0.5 |
| H5 | **Warp table indexing.** WarpZoneControl × pipe-index into WarpZoneNumbers: which values are reachable beyond the normal {2..8, 36}? | B | **refuted** (P0.6) | Proof: `docs/warp-model.md` §5.4 + `tools/warp_tables.py`. WZC is cleared per area load (F38), written 4/5/6 by the text object and +1 by the single self-erasing `WarpZoneObject`; the text cannot be parsed while the scroll is locked, so the +1 always precedes it → WZC ∈ {0,1,4,5,6} at any pipe; index rows 2/3 (WZC 2,3,7 → worlds 255/38) are unreachable. Reachable destinations: 1-2 {4,3,2}/{−1,5,−1}, 4-2 ceiling 5, 4-2 $2F {8,7,6}. Only a memory-corruption write to $06D6 (H7-class) could change this. |
| H6 | **NES Minus World is not a dead end.** With WorldNumber = 35 many table reads are OOB (area lists, hard-mode tables, Bowser replacement); the exit pipe, vines, death/continue, or a Game Over continue may lead somewhere. | B | **refuted at table level** (P0.6) | `docs/warp-model.md` §6, `tools/warp_tables.py`: world 35 → AreaPointer $01 (Water2) via `AreaAddrOffsets[51]`; all three commands in Water2 need world 2/3/7 → never match 35; the exit pipe reloads $01 p0; death → `ContinueGame` → `FindAreaPointer` → $01; game over → `ContinueWorld` 35 → same; no flagpole, no vine. Every path recomputes the same pointer. Remaining opening = an in-game write to WorldNumber/AreaPointer (H7). |
| H7 | **WorldNumber write-reachability.** Some in-game indexed write (enemy slot overflow, object ID overflow, buffer overrun) can set WorldNumber ($075F) / LevelNumber to reach world 8 or flag the ending in an earlier castle. P0.6 sharpened the targets (F43): (a) WorldNumber ≥ 7 before any castle's axe ends the game (`PlayerEndWorld`); (b) AreaPointer/EntrancePage ($0750/$0751) = $65/16 before any down-pipe enters the Bowser room; (c) WarpZoneControl ($06D6) ∈ {2,6} in 1-2 warps to 8-1 from 1-2. | B | untested | P3.1–P3.3 (RAM oracle finds the jackpot cells; audit finds the writes) |
| H8 | **Cart-swap-free ACE.** Object ID $C9 (or any OOB object/state value) can be produced in worlds 1–8 without cart swap, e.g., via another OOB index into the Bowser-replacement or enemy tables, or via uninitialized-RAM reads at power-on. | B | untested | P3.1, P3.4 |
| H9 | **Cross-level DP.** A slower framerule in an earlier level changes RNG / framerule phase enough to save more later (8-3 Hammer Bros, 8-4 Bowser). | A | untested | P2.4 |
| H10 | **8-4 full-state exhaustive search** finds ≥ 1 frame (wall clip, turnaround room, water, Bowser pattern, L+R usage). | A | untested | P2.2 |
| H11 | **Per-level threshold search** reaches framerule N−1 in at least one of the 7 framerule levels (priority = smallest slack first). | A | untested | P0.4 then P2.3 |
| H12 | **L+R / U+D semantics** contain effects beyond the known 1-frame reversal (e.g., interactions with walls, vines, pipes, flagpole, swimming) that enable new clips. | B | untested | WR uses L+R on 85 frames in ~20 clusters (list via `tools/fm2_info.py --list-lr`), U+D never. P0.4 maps each cluster to its level/purpose; P0.7 catalogs the code paths |
| H13 | **Alternate pipe glitch** (entry point variable loads per screen): a pipe in 1-2, 4-2, or 8-4 can be made to lead somewhere useful — e.g., an 8-4 pipe into the Bowser room. | B | **refuted at table level** (P0.6) | F40 / `docs/warp-model.md` §3.3: every pipe destination is the last matching row-$0E command; the complete table has 58 commands and the Bowser-room page (16) is written only by the 8-4 water section's command, which is already the route. The 4-2 wrong warp (already in the WR) is the only parse-order shortcut in the tables. Non-table destinations need a write to $0750/$0751 (folded into H7). |
| H14 | **Vine teleport / screen-edge tricks** skip a section of 8-4 or 4-2. | B | untested | P0.6, P2.x |
| H15 | **Soft reset exploitation.** If TASVideos rules allow mid-movie resets: resetting during a specific write leaves the warm-boot/continue path ($07FD/$07FF) in a state that starts later than 1-1 faster than playing there. Cost: title screen again. P0.6 (F44): warm boot needs top-score digits < 10 and $07FF = $A5; `ContinueWorld` $07FD is written only by a game over, so a continue costs a game over first. | B | untested (low prior) | Check rules; would need a reset-timing search in P3.4 |
| H16 | **Sprite-0 / PPU timing.** Lag frames can be prevented (or the NMI-overrun threshold moved) by controlling what the game renders. | A | untested | P0.5 |
| H17 | **Object-slot spawn suppression.** 5+ active sprites prevent spawns (documented for 4-4 Bowser). In 8-4, suppressing Bowser (or hammers/fire) may give a faster axe. | A/B | untested | P0.6, P2.2 |
| H18 | **Power-up usage.** A power-up somewhere (fire, super) nets frames despite pickup cost (e.g., fireballs vs Bowser, big-Mario clip geometry). Low prior. | A | untested | P2.x |
| H19 | (P0.5: confirmed for this boot — Start rows 34–43 all give control on row 197, F31; only the boot ITC phase could change it.) **Start-press / power-on alignment.** The first Start press frame and emulator initial-RAM convention are fixed by rules; confirm #1715M presses Start on the first accepted frame and that nothing in the boot path can be shortened. | A | untested | WR presses Start on frame 41 (0-based) after a soft-reset command on frame 0 (F16). P0.5: find the first frame the title screen polls Start; P0.4: confirm nothing earlier is accepted |
| H20 | **Uninitialized-RAM reads.** The game reads RAM the boot routine does not clear (the ACE lands at $1181 for this reason). Under the emulator's defined initial RAM, any such read that affects gameplay is a lever. | B | untested | P3.1 |
| H21 | **1-1 is 1 frame from the next framerule** (F29): T_set = grab + T + 159 (F32), WR: 1289 + 370. Objective for the 1-1 search: a glitch grab with grab + T ≤ 1658 (e.g. grab on row 1288 with T still 370, or any earlier grab; note T only drops every 24 control frames, so a 1-frame-earlier grab normally keeps T). Fireworks must stay off (last digit ∉ {1,3,6}). Community (F35): believed impossible since 2009, but no proof — only a naive brute-force size estimate; the WR/2019 rewrite deliberately lose time before the 1-1 pole to get an FPG-feasible x-subpixel, so the search must cover the FPG subpixel condition. | A | untested (no community proof) | First target for P2.1 (search engine on 1-1). P0.9 must check whether the community has a proof that 1-1 is optimal at this framerule. |
| H22 | **1-2 is 8 frames from the next framerule** (F28/F29): entering the warp pipe 8 frames earlier (or shortening the 48-frame ChangeAreaTimer pipe animation, or gains in the intro/main area) saves 21 frames. | A | untested | Third target after 1-1 and 8-4. The ~499-row auto-walk intro is fixed — unless it isn't (check AltEntranceControl paths in P0.5/P0.6). |
| H23 | **Remaining framerule deficits**: 4-1 9 frames, 8-3 10 (of which 3 are known: FPG with timer 242, F37 → 7 left), 4-2 13 on the WR route but **2 on HappyLee's top-floor route** (F36), 8-1 18, 8-2 19 (F29). Search order by remaining deficit: 4-2 (2), 8-3 (7), 4-1 (9), 8-1, 8-2. | A | untested | P2.3; `docs/community-claims.md` |
| H24 | **8-4 is unquantized**: every frame saved in 8-4 (incl. the 19-frame ending coast, H1, and the 4 sub-area loads which are not framerule-bound) counts 1:1. | A | confirmed (mechanism) | F27/F28: OperMode→2 at the axe; room loads have no ScreenTimer wait. Gains still to be found (P2.2). |
| H25 | **Maru's 8-4 idea** (stop on the turnaround-room floor one frame earlier, subspeed $705-dependent, F37) is not in the L+R WR and would be a 1:1 frame in 8-4. | A | untested | Needs Maru's movie file (TASVideos 4:57.54 page has only the time) to diff against the WR; else reproduce from the description in P2.2. |
| H26 | **8-2 framerule via end-of-level Koopa FPG** (KnucklesMaster368 2017) or the RTA plant-despawn (3 Bullet Bills one frame — HappyLee: "RTA only"). 19-frame deficit makes this the least likely flag level; "better spawn proven not possible by using cheat" (zdoroviy_antony) is the only search-like evidence and covers spawn outcomes only. | A | untested (low prior) | `docs/community-claims.md` |
