# Facts ledger

Status: **V** = verified by us (cite script/experiment). **S** = sourced from the community
(cite URL) — a claim to verify, not a fact we own. Add a row for every number we rely on.

| ID | Fact | Status | Source / verification |
|---|---|---|---|
| F1 | The TASVideos "warps" record is HappyLee's movie #1715M: **17,868 frames** = 4:57.31 (movie length at 60.0988 fps), published 2011-01-06, on `Super Mario Bros. (W) [!].nes` (fm2 says romFilename `Super Mario Bros. (JU) [!]`, emuVersion 9828, rerecordCount 9739). It obsoleted klmz's 4:57.33 by exactly 1 frame. | V (frame count) | https://tasvideos.org/1715M ; file `data/wr/happylee-supermariobros,warped.fm2`; `tools/fm2_info.py` |
| F2 | RTA-timing equivalent of #1715M is 4:54.032. | S | https://en.wikipedia.org/wiki/Super_Mario_Bros._speedrunning |
| F3 | Maru's "warps, RTA rules" (no Left+Right) TAS is 4:54.265 (4:57.54 TAS timing) — 14 frames slower than #1715M. | S | https://tasvideos.org/HomePages/Maru |
| F4 | Human any% WR: averge11 4:54.415 (2025-12-18). Humans match the TAS framerule in every level except 8-4 since Niftski's "Lightning 4-2" (Sept 2023). | S | https://gamerant.com/super-mario-bros-new-world-record-speedrun-january-2025/ ; Wikipedia |
| F5 | Level completion is quantized to a global 21-frame framerule; the time from flagpole to next level rounds up to the next boundary. 8-4 ends on the axe touch with no rounding. | S | https://tasvideos.org/GameResources/NES/SuperMarioBros |
| F6 | Fireworks fire when the timer's last digit is 1, 3, or 6 at the flagpole; they cost frames. This is why the TAS avoids the flagpole glitch in 8-3. | S | Game Resources page |
| F7 | TASVideos also publishes 12 other SMB1 branches, incl. warpless 18:36.78 (HappyLee & Mars608, 2018), warps (Europe/PAL) 4:55.16, walkathon variants, minimum presses, max score/coins, FDS "-3 stage ending" 2:44.61, FDS "game end glitch" 5:29.957, and "arbitrary code execution" 4:52.65. | S | https://tasvideos.org/1G |
| F8 | The ACE movie (OnehundredthCoin, 4:52.65) requires a cartridge hot-swap from SMB3 to seed RAM $07FD so SMB1 boots into "World N" (world index 22). Mechanism: world index ≥ 8 → Bowser-replacement table OOB read → object ID $C9 → behavior jump table → $D007 → state machine value 4 → jump to unmapped $53AE → open bus executes SRE ($0A),Y → RTI → PC lands at uninitialized $1181 → controller-driven payload. | S | https://tasvideos.org/8991S |
| F9 | Community belief: the NES Minus World (-1, world index 36) is a water level whose exit pipe loops back to itself; on FDS the minus worlds differ and allow a -3 ending and ACE. | S (belief, not proof) | TASVideos publications; re-examine under H6 |
| F10 | Pressing Left+Right for one frame makes Mario walk backward in the opposite direction, giving instant deceleration. Enemy hitboxes affect Mario only every other frame. 16 subpixels per pixel. | S | Game Resources page |
| F11 | A known 8-4 micro-improvement (RTA-rules TAS): in the turnaround room, stop on the floor one frame earlier while still scrolling far enough for the wrong warp; depends on subspeed (RAM $705). | S | https://www.speedrun.com/smb1/forums/1wd01 |
| F12 | zdoroviy_antony's 2019 "perfect subpixel optimization" .fm3 rewrite ends on exactly frame 17868 — no improvement. flamexx's 2025 rewrite from 4-2 and cpt32's 2025 warps TAS are both slower than #1715M. | S | https://tasvideos.org/Forum/Topics/1337?CurrentPage=59 ; https://tasvideos.org/userfiles/game/1 |
| F13 | The TASVideos SMB1 forum thread has 63 pages (1,558 posts); the last posts (Oct–Nov 2025) are about warpless 5-1 manipulation; no active warps-improvement effort is visible on pp.59–63. | S | https://tasvideos.org/Forum/Topics/1337?CurrentPage=63 |
| F14 | Known community bots were narrow: DaSmileKat's enemy-mechanics brute forcer, a vertical-physics model + brute forcer for "minimum A presses", a Cheep-spawn optimizer. No public full-state exhaustive level search found yet (P0.8 verifies). | S | https://tasvideos.org/7094S ; https://tasvideos.org/GameResources/NES/SuperMarioBros |
| F15 | ROM identity, verified: TASVideos lists `Super Mario Bros. (W) [!].nes` as MD5 `811b027eaf99c2def7b933c5208636de` / SHA1 `ea343f4e445a9050d4b4fbac2c77d0693b1d0922` (whole file with the classic iNES header `4e45531a 02 01 01 00…`). The movie's `romChecksum base64:jjYwGG411HcjG/j9UOVM3Q==` is the MD5 of PRG+CHR only = `8e3630186e35d477231bf8fd50e54cdd` (headerless SHA1 `facee9c577a5262dbe33ac4930bb0b58c8c037f7`, CRC32 `d445f698`). Matt's dump (No-Intro name `Super Mario Bros. (World).nes`, NES 2.0 header) has byte-identical PRG+CHR; a classic-header copy byte-identical to TASVideos' file is in `roms/` (gitignored). | V | https://tasvideos.org/Games/1 ; `tools/verify_rom.py` |
| F16 | Structure of the WR input file: soft-reset command on frame 0; first Start press on frame 41 (0-based); last input on frame 17848 (0-based) = 17,849 (1-based), an A press; 19 input-free frames follow (the axe is reached during the coast). Left+Right is pressed on 85 frames; Up+Down on none. Hold counts: R 2718, L 405, D 7, U 0, Start 1, Select 0, B 1680, A 2596. | V | `tools/fm2_info.py data/wr/happylee-supermariobros,warped.fm2 --list-lr` |
| F17 | The published length (17,868) counts the 19 trailing input-free frames. So the bar for a new movie is: game completes AND last input earlier than frame 17848 (0-based); a shorter file alone is not an improvement. | V (from F16) + rule reading to confirm in P4 | TASVideos movie rules |
| F18 | Primary host `laptop`: Fedora 43 Workstation, kernel 6.18.6, Intel i5-1335U (2 P-cores w/ HT + 8 E-cores = 12 threads, up to 4.6 GHz, 12 MiB L3), 15 GiB RAM, NVMe with 217 GB free, Wayland desktop. Host has gcc 15.2.1 and Python 3.14.2; `sudo` needs a password, so root installs are done in a rootless `toolbox` container (`smb1`) instead. | V | `docs/experiments/P0.1-tooling.md`; `lscpu`, `free -h`, `/etc/os-release` |
| F19 | Emulator versions: FCEUX 2.6.6 (RPM Fusion `fceux-2.6.6-11.fc43`, Qt5/SDL2, Lua 5.1) in the `smb1` toolbox; Mesen 2.1.1 (Linux x64 self-contained build) native in `~/opt/mesen2`; BizHawk 2.11.1 (linux-x64 tarball) on mono 6.12.0.199 in the toolbox, NES core pinned to NesHawk. Reproduce with `tools/toolbox_setup.sh`; run with `tools/fceux_run.sh`, `tools/mesen2_run.sh`, `tools/bizhawk_run.sh`. | V | `docs/experiments/P0.1-tooling.md` (versions printed by `rpm -q`, `mono --version`, release tags) |
| F20 | Headless recipes that work: FCEUX under `xvfb-run` with `--loadlua` (+`emu.speedmode("nothrottle")`; `emu.exit()` segfaults after finishing — ignore exit 139); Mesen2 `--testrunner --enablestdout --timeout=S script.lua rom.nes` with `DOTNET_SYSTEM_GLOBALIZATION_INVARIANT=1` and a pre-seeded `~/.config/Mesen2/settings.json` (Lua must write files; `emu.log` is not on stdout); BizHawk `EmuHawkMono.sh --lua=… rom` under `xvfb-run` with `config.ini` {SoundOutputMethod Dummy, LastWrittenFrom 2.11.1} (otherwise invisible modal dialogs hang it). | V | `tools/lua/smoke_*.lua` outputs, `docs/experiments/P0.1-tooling.md` |
| F21 | Reference-emulator throughput on one core with a Lua frame loop (title screen): FCEUX ≈ 1,400 fps; Mesen2 testrunner ≈ 400 fps (debugger active under Lua); BizHawk NesHawk ≈ 100 fps (renders every frame via llvmpipe). All far below what search needs (P1.1). | V | `tools/lua/bench_*.lua`; numbers in `docs/experiments/P0.1-tooling.md` |
| F22 | From power-on with no input, `FrameCounter` ($09) advances exactly once per emulated frame in FCEUX, Mesen2, NesHawk and QuickNes, but its value at a given script frame differs by emulator (at loop iteration 60: FCEUX 51, Mesen2 53, NesHawk 53, QuickNes 54; boot lag frames counted: FCEUX 7, NesHawk 6, QuickNes 5). So fm2-frame ↔ emulator-frame alignment must be established explicitly in P0.3, not assumed. | V (observation) | `tools/lua/smoke_*.lua`; `docs/experiments/P0.1-tooling.md` |
| F23 | **The WR movie syncs** in FCEUX 2.6.6 and BizHawk 2.11.1/NesHawk on our ROM, and the per-frame game state of the two emulators is identical for all 18,267 compared frames (23 key addresses incl. RNG, timers, positions; all 24 lag frames coincide). | V | `docs/experiments/P0.3-wr-sync.md`; `tools/compare_dumps.py data/wr/fceux_wr.csv data/wr/bizhawk_wr.csv --offset 1` |
| F24 | **Axe frame of the WR**: `OperMode` ($0770) becomes 2 (set only by `HandleAxeMetatile`) on fm2 frame **17867 (0-based)** = 1-based frame 17868 = the published movie length; 19 frames after the last input (frame 17848). Timer shows 317 at the axe. Lag frames before the axe: **24** (7 boot, 1 after Start, 16 area/pipe loads). | V | `tools/check_sync.py` on both dumps; `docs/experiments/P0.3-wr-sync.md` |
| F25 | Frame-index conventions: FCEUX Lua dump row i (after the i-th `emu.frameadvance()`) ↔ fm2 frame i−1 (verified by matching FCEUX's reported joypad byte to the fm2 on 17,800 rows, 0 mismatches); BizHawk row i ↔ fm2 frame i (one ahead; its `joypad.get` reads 0 during playback, alignment from lag rows). Boot RAM-init differs (FCEUX $FF, NesHawk $00) but the game overwrites it before it matters. (P1.1 clarification: the row↔movie-frame mapping is the csv `movie_frame` column; the **input record** j is applied in FCEUX row j+2 — Start record 41 acts on row 43 — see F45.) | V | `docs/experiments/P0.3-wr-sync.md` |
| F26 | WR level-entry frames (FCEUX rows = fm2 frame + 1): 1-1 @43, 1-2 @1945 (intro area; main area @2444), 4-1 @3767, 4-2 @6043 (main @6542), 8-1 @7724, 8-2 @10814, 8-3 @12957, 8-4 @15058, axe @17868. The 1-2/4-2 pipe-intro areas last exactly 499 rows each. | V | `tools/check_sync.py data/wr/fceux_wr.csv …`; full per-frame RAM in `data/wr/fceux_wr.ram` (regenerable) |
| F27 | **Framerule mechanism (code + dump).** `DecTimers` (NMI, before game logic) decrements IntervalTimerControl ($077F) each frame; on underflow it resets to 20 and the interval timers $0796–$07A3 decrement → 21-frame period. A flag level ends when StarFlagTaskControl ($0746) reaches 5: task 3 sets the star-flag object's EnemyIntervalTimer = 6 on frame T_set, task 4 waits for it (and EventMusicBuffer = 0, never binding in the WR). With post-frame ITC = v at T_set the area change happens at T_set + (v+1) + 105 → slack = v, deficit = 21 − v. The time-bonus countdown costs **1 frame per timer unit**. Verified: predicted = observed expiry in all 5 flag levels. | V | `tools/slack_table.py`; `docs/experiments/P0.4-slack-table.md`; smbdis.asm `DecTimers`, `RunStarFlagObj`, `DelayToAreaEnd`, `PlayerEndLevel` |
| F28 | **Level-start law.** For main-level entries (AltEntranceControl = 0) the intermission card's ScreenTimer ($07A0) = 7 is written on frame load+8 (the load's lag frame is load+2); with post-frame ITC = w there, player control starts at **load + 154 + w** (= 8 + (w+1) + 6×21 + 19); exact for all six such loads in the WR. Flag levels load at u = 19 → w = 12 → 166 frames. Pipe levels' framerule is therefore set at the next load: slack = w, deficit = 21 − w: 1-2 (w=13 at the 4-1 load), 4-2 (w=8 at the 8-1 load). Sub-area loads (AltEntranceControl ≠ 0: 1-1 bonus room, 4-2 wrong warp, 8-4 rooms) have no card and are **not** quantized (control at load+43 / +122). | V | `tools/slack_table.py` area-load table |
| F29 | **WR slack table** (slack / deficit in frames): 1-1 20/**1**, 1-2 13/8, 4-1 12/9, 4-2 8/13, 8-1 3/18, 8-2 2/19, 8-3 11/10, 8-4 unquantized (axe). Per-level frames (load→next load): 1902, 1870, 2228, 1729, 3042, 2143, 2101, 2810 (+43 to first load). Flag glitch used in 1-1, 4-1, 8-1, 8-2; 8-3 slides. Timer at the end event: 370, 347, 340, 356, 200, 338, 244, 317. | V | `tools/slack_table.py` |
| F30 | All 24 lag frames of the WR are boot (7), the Start press (1) and one per area load (16); there is no in-level lag. Sub-area loads lag too. | V | `tools/check_sync.py`, `tools/slack_table.py` |
| F31 | **Boot/Start law.** The title menu (`GameMenuRoutine`, OperMode_Task 3) accepts Start from row 34 (fm2 frame 33); Start sets OperMode = 1 the same frame and that row is the 1-1 load row. With the level-start law (F28) every Start row 34–43 yields control on row 197; the WR presses on row 43. 1-1's control start is therefore fixed by the boot ITC phase (7 boot lag frames), not by the Start frame. | V | `docs/timing-model.md` §3; dump rows 1–46 |
| F32 | **Flag-level end sequence constants.** Flagpole glitch = grab with Mario Y ≥ $A2 (162): `FlagpoleRoutine` sets GES 5 the same frame (no slide). Glitch grab → castle-door trigger = 126 frames in 1-1/4-1/8-1 (8-2: 1 frame, the glitch happens at the door; 8-3 slides: 173 incl. walk). Task 1 completes instantly; countdown = T + 1 frames (T = timer at the grab); flag raise = 32 frames; then T_set. So **T_set = grab + T + 159** (glitch levels; 8-2: + T + 34) and the next load = T_set + (v+1) + 106. Fireworks only for timer last digit 1/3/6 (none in the WR). | V | `docs/timing-model.md` §6; dump (all 5 flag levels) |
| F33 | **Pipes and sub-areas.** Every down-pipe entry takes 48 frames (ChangeAreaTimer = $30) from GES 3 to the load; side pipes 160 (page 0) or 52. Sub-area entries: control at load+43 (side entry / intro→main), +122 (vertical pipe exit, 96-frame rise), +27 (4-2 wrong-warp re-entry). 1-2/4-2 intro areas: 499 rows, input overridden. | V | `docs/timing-model.md` §4/§7; dump |
| F34 | **Game timer.** Header loads 401; `RunGameTimer` ticks on the first control frame and then every 24 frames while GES ≥ 8 and Mario is on screen (pipes, loads, sub-area entrances pause it). T at the grab = 400 − floor(eligible control frames / 24). | V | `docs/timing-model.md` §5; dump 1-1 (31 ticks, deltas 24 except across the pipe/bonus room) |
| F35 | Community record on 1-1: in 2009 andrewg wrote "we're 1 frame short of improving level 1-1 by 21 frames. And it's like impossible to improve it"; the only quantitative argument ever given is Derakon's naive 6^20-inputs / "1160 years at 100,000 fps" estimate. No search record or proof exists in any source read. The 2019 subpixel rewrite "lost time in the third room of 1-1 to execute the flagpole glitch". | S | `docs/community-claims.md` (Topics/8414, speedrun.com 0jnl2) |
| F36 | HappyLee (2010): in 4-2 the top-floor brick route is the fastest but "still 2 frames to the next 21 frame boundary"; he used the slower bottom route for entertainment. Humans' "Lightning 4-2" reaches the same framerule as the TAS with "zero frames to spare". | S | `docs/community-claims.md` (#2964S, speedrun.com elaz6/tk6gs/rla1w) |
| F37 | zdoroviy_antony: "8-3 with FPG and '242' is 3 frames faster than without FPG — 1 frame from walking to the castle and 2 frames from timer countdown" (the WR slides with timer 244). Maru's RTA-rules TAS stops "on the floor one frame earlier" in the 8-4 turnaround room (depends on subspeed $705). | S | `docs/community-claims.md` (speedrun.com 0jnl2, 1wd01) |
| F38 | **Area-load memory law.** `InitializeArea` → `InitializeMemory` (Y=$4B) clears $0000–$074B (not the stack $0160–$01FF) plus $0780–$07A1 on every area load incl. death restarts. Survivors: AreaPointer $0750, EntrancePage $0751, AltEntranceControl $0752, HalfwayPage $075B, LevelNumber $075C, WorldNumber $075F, AreaNumber $0760, ITC $077F, $07A2–$07FF. WarpZoneControl $06D6 and ScrollLock $0723 are cleared. | V | `docs/warp-model.md` §1; smbdis.asm `InitializeArea`, `InitializeMemory` |
| F39 | **Area pointer resolution.** `AreaPointer = AreaAddrOffsets[WorldAddrOffsets[WorldNumber] + AreaNumber]` ($9CB4/$9CBC, 8/36 bytes; worlds 1,2,4,7 have 5 areas because the pipe-intro scene $29 is an area). Pointer bits 6–5 = type, low 5 = index (ground $20+n = GroundArea n+1, etc.; bit 7 ignored). Recomputed only by `StartWorld1`, `NextArea` (flag/axe), `PlayerEndWorld`, `ContinueGame` (death / game-over continue). | V | `tools/warp_tables.py`; `docs/warp-model.md` §2–3 |
| F40 | **Pipe-destination law.** A down-pipe goes to `AreaPointer` at `EntrancePage` as last written by a row-$0E enemy command (3 bytes: pointer, world<<5 \| page) parsed when its column came within ScreenRight+48 px **and** its 3-bit world equalled WorldNumber — or to the warp lookup if WZC ≠ 0. All 58 commands are tabulated (`docs/warp-model.md` §3.3); only 8-4's own commands point at a castle and only the 8-4 water section's command writes the Bowser-room page 16. The WR's 4-2 wrong warp = the page-5-col-4 pipe entered before the page-5-col-15 command (→ $42) was parsed, so AreaPointer was still $2F from the page-2-col-1 (vine) command (dump load row 7223, mode 1). | V | `tools/area_data.py`; `tools/ram_trace.py`; `docs/warp-model.md` §3 |
| F41 | **Warp-zone law.** `ScrollLockObject_Warp` writes WZC = 4 (world 1) / 5 / 6 (worlds > 1, ground-type area) and toggles ScrollLock; enemy $34 `WarpZoneObject` does WZC += 1 once (when ScrollLock ≠ 0 and Player_Y & Player_Y_HighPos = 0) and erases itself; `HandlePipeEntry` reads `WarpZoneNumbers[(WZC&3)·4 + pipe]` (pipe 0/1/2 for X < $60 / < $A0 / else), WorldNumber = byte − 1, Area/Level/EntrancePage = 0, mode 0 (quantized, F28). **Reachable WZC at a pipe = {0,1,4,5,6}** (proof: `docs/warp-model.md` §5.4: text unreachable while locked, one self-erasing increment per visit, both cleared per load). Destinations: 1-2 {4,3,2} (WZC 4) or {−1,5,−1} (WZC 1); 4-2 ceiling zone 5; 4-2 $2F zone {8,7,6} (no $34 enemy there). World 8 only from 4-2. Dump: 1-2 WZC 0→1 (row 3546)→4 (3721); $2F 0→6 (7608), pipe row 7724 X 114 → world 8. | V | `tools/warp_tables.py`; `tools/ram_trace.py`; `docs/warp-model.md` §5 |
| F42 | **Bogus worlds.** WorldNumber 35 (NES Minus World, WZC 1 left/right pipe in 1-2) → `WorldAddrOffsets[35]` = $33 → `AreaAddrOffsets[51]` = $01 = Water2 (2-2/7-2 data); its commands need world 2/3/7 → never match → exit pipe reloads $01; death/continue recompute $01; no flagpole → closed loop at table level. WorldNumber 38/255 (WZC 3/7) are unreachable; they would load $00 / $9D (enemy data at $7EA0 = open bus). | V | `tools/warp_tables.py` §"bogus worlds"; `docs/warp-model.md` §6 |
| F43 | **Completion condition.** After the axe, `PlayerEndWorld` ends the game iff WorldNumber ≥ 7 (`cpy #World8; bcs`); otherwise Area/Level = 0, WorldNumber++, `LoadAreaPointer`. WorldNumber writers: `GoContinue` (title: 0, or `ContinueWorld` $07FD with A+Start — set only by a game over), `HandlePipeEntry`, `PlayerEndWorld`. So without memory corruption world 8 is first reachable at 4-2's $2F zone. | V | smbdis.asm `PlayerEndWorld`, `GoContinue`; `docs/warp-model.md` §0/§7 |
| F44 | **Boot.** Warm boot iff `TopScoreDisplay` digits ($07D7–$07DC) all < 10 and `WarmBootValidation` $07FF = $A5 (then memory is cleared only to $07D5). Power-on = cold boot: clears to $07FD, writes $A5. `ContinueWorld` and `WorldSelectEnableFlag` ($07FC) are 0 on power-on. | V | smbdis.asm `Start`; `docs/warp-model.md` §8 |
| F45 | **QuickNES is frame-exact on the WR.** libretro QuickNES (commit 26bb785) driven by `src/fastcore/harness.c` replays #1715M with RAM identical to the FCEUX dump on 17,856 consecutive rows (FCEUX rows 11–17866; one row, 44, differs in 2 OAM-buffer bytes because the post-Start lag frame's NMI spans the frame boundary and is snapshotted at different points), same 24 lag rows, axe on QuickNES row 17865 = FCEUX row 17868. **Alignment law:** FCEUX row r ↔ QuickNES row r−3; fm2 record j is applied by FCEUX in row j+2 (Start: record 41 → row 43) and must be fed to QuickNES frame j−2 (`--input-skip 2`). | V | `docs/experiments/P1.1-fast-core.md`; `tools/compare_ram.py data/wr/fceux_wr.ram runs/qn_wr.ram --offset -3 --from 10 --ignore 160-1ff --all` |
| F46 | **Stage-1 core throughput** (i5-1335U, 12 threads): 15.0k fps per instance on the full WR; savestate 12,792 bytes, serialize+unserialize 2.5 µs; 6 parallel instances 69k fps, 12 → 104k fps aggregate. The libretro build renders every frame (`emulate_skip_frame` unused). | V | `./build/harness … --state-every 1`; `docs/experiments/P1.1-fast-core.md` |
| F47 | **1-1 third room is a fixed puzzle.** The pipe exit puts Mario at X = 2616 (page 10, $38) with subpixel $0705 = 0 and speed 0 (`InitializeArea` clears them); control starts at load+122 (F33). The WR accelerates 0→40 in 33 frames (L+R at control so facing = 3 ≠ moving dir 1, jump: airborne doubled adder to the air cap 24 by frame 1068; land; Left tap → facing 2 ≠ moving 1 → doubled $E4 to 40 by 1079), runs at 40 to x = 3157.3 at frame 1279 (model step 233), is stopped by the pole's base block (X 3158, speed → 0) and lands on the ground beside it (Y 176, step 236), then **jumps** (A+L+R, step 237) and touches the pole two frames later while rising, at X 3161, Y 164 (step 239 = frame 1285; the glitch needs Y ≥ 162, F55). (Session 4 correction from the validated model trace, `smb-opt trace11room`; the earlier "embedded creep" reading of the dump was wrong in mechanism, not in numbers.) Grabs need X & 15 ∈ [6,10) at the pole column → X ∈ [3158, 3161] for column 198. | V | `docs/experiments/P2.1a-bfs-engine.md`; dump `runs/`/`tools/ram_trace.py` rows 1046–1290; smbdis.asm `X_Physics`, `ImposeFriction`, `HandleClimbing`, `BlockBufferCollision` |
| F48 | **Horizontal acceleration mechanics.** Speed ($57) is in 1/16 px/frame; each frame the friction adder (FrictionData $E4 run / $98 walk / $D0 fast-walk) is added to the shared subpixel byte $0705 with carry into speed, **doubled when PlayerFacingDir ≠ Player_MovingDir** (moving dir is refreshed every frame from the speed sign; facing only from ground input); caps: $28 on the ground running (B or RunningTimer, L/R == moving dir) or airborne with speed ≥ 25, else $18 (airborne below 25 cannot exceed 24). Position: $0705 += (speed<<4)&$FF, X += speed>>4 + carry. Sound bound: speed gains ≤ 2/frame; under it the frontier from the third-room entry reaches x = 3158 at frame 1272 (WR: 1280). | V | smbdis.asm lines 6042–6195, 6227–6270, 7536–7575, 5598–5605; `docs/experiments/P2.1a-bfs-engine.md` |
| F49 | **Full-state BFS explosion.** From the third-room load with dedup on RAM ($0000–$00FF, $0300–$07FF): 16 states per layer until control, then 16 → 127 → 936 → 5,402 → 26,247 → 107,757 → >245k in the first 6 control frames (×5/layer, dup ratio 3.5); the sound ≤2/frame bound prunes nothing there (WR is 18.7 px inside it). 12.8 KB savestates cap a layer at ~245k states per 6 GB; expansion costs ~75 µs per child on QuickNES. | V | `src/search/bfs.c`; `docs/experiments/P2.1a-bfs-engine.md` |
| F50 | **Savestate = template + RAM.** In QuickNES's 12,792-byte savestate the 2 KiB system RAM sits at offset 216. Patching a state's RAM into a template savestate taken at another frame (even the title screen, frame 200) and unserializing reproduces the true continuation exactly for 600 frames on the WR (tested from frames 1100, 1229, 1280: through the flagpole sequence and the 1-2 load). In-level the CPU idles between NMIs and the PPU/APU state is a function of RAM, so a search state is the 2 KiB RAM — **except at lag-frame boundaries** (the CPU is mid-NMI, not in RAM): a search must not start at or pass through an area-load lag frame (P2.1b: root 923 collapsed to one state per layer; root ≥ 930 or 1040 is fine). | V | `src/fastcore/state_probe.c` (`build/state_probe … --frame-a 1046 --frame-b 1229 --run 600`) |
| F51 | **Flagpole trigger geometry.** `PlayerBGCollision` → `CheckSideMTiles` → `HandleClimbing`: the grab fires when a side collision point (small Mario: X+$0D at Y+8 / Y+$18) reads a $24/$25 metatile **and** X & 15 ∈ [6, 10) (`HandleClimbing`, $04 = Player_X low nybble). For 1-1's pole (column 198, x $C60–$C6F) that means X ∈ [3158, 3161]. `FlagpoleRoutine` skips the slide (GES 5 the same frame) iff the flag object's Y ≥ $AA or Mario's Y ≥ $A2 at that moment. The WR lands embedded in the pole's base block at X 3157 (speed → 0 at frame 1280) and is pushed to X 3161, Y 164 five frames later (F47). | V | smbdis.asm 11902–12068, 12153–12175, `FlagpoleRoutine`; `docs/timing-model.md` §6 |
| F52 | **Prior exhaustive-search tool.** MrWint/smb-opt (2017–18, Rust nightly, `third_party/smb-opt` commit daa44287) is a player-physics-only model (enemies ignored, 10–12-byte states, IDA* with precomputed exact x/y-gain heuristics) used for segment searches in #5862/#5975 (4-4 wall clip, speed-ups, FPG approaches incl. a 59-frame "stairs to half flagpole glitch" in 1-1 from a hand-picked state). No whole-level or full-state search exists anywhere. Best-known 1-2 (Maru370 2024) is 5 frames from the next framerule. | S | `docs/prior-tools.md` (tasvideos.org/5862S, github.com/MrWint/smb-opt, speedrun.com/smb1/forums/75044) |
| F53 | **smb-opt's player model is exact on the WR's 1-1 third room.** With the goomba bounce at row 1112 injected (y speed := −4, the model has no enemies), MrWint's model reproduces every frame of rows 1047–1286 (x, y, 8.8 speeds incl. the $0705/$0433 fractions, states) and fires its flag collision at step 239 = row 1286, X 3161, Y 164 — the WR's glitch grab. Without the injection it matches the first 64 frames (0→40 acceleration, landing) and then diverges only because of the stomp. | V | `docs/experiments/P1.2-lite-smb-opt.md`; `third_party/smb-opt` `trace11pipe` mode |
| F54 | **Sound bounds in the search engine.** `bfs_par` (v3, compact states) prunes with (a) speed +≤2/frame, (b) MrWint's XPos max-distance table exported from smb-opt (70,688 x-physics classes; every real-game class met so far was in the table), (c) best-case descent to Y ≥ 162. From the WR's frame-1229 state, (b)+(c) cut layer 11 from 489,820 to 292,660 states; from the control start (root 1040) bound (b) cut frames 1051/1052 from 107,757/≥245,300 to 37,815/66,281 (growth ≈ ×1.8/layer remains). v2 (no (b)/(c)) hit 1.52M states at frame 1243 from root 1229 without finding a grab ≤ 1284. | V | `docs/experiments/P2.1b-pole-search.md`; `runs/P2.1b-root1229-par/stdout.log`, `runs/P2.1b-root1229-v4/` |
| F55 | **Only a glitch grab is fast; the grab objective is integer Y ≥ $A2 (162) in the grab frame.** `FlagpoleRoutine` runs later in the same frame as the grab: if the flag's Y ≥ $AA or Mario's Y ≥ $A2 → GES 5 at once (zero slide frames); otherwise the flag drops ~2 px/frame (`Enemy_YMF_Dummy += $FF` with carry) until it reaches $AA. `FlagpoleSlide` forces a climb-down (Y speed 1, force $FF ≈ 2 px/frame) only while Mario's Y < $9E (158), so a grab at Y < 162 can never slide to 162 (it stops at 158–159) and always waits for the flag (≈ 47 frames in 8-3, F34). Therefore grabs at Y 158–161 are as slow as high grabs, and the search goal is exactly `StateChangeFlag` with Y ≥ 162 (the core confirms: GES goes 4 → 5 in the grab frame iff Y ≥ 162, 71/71 cases in `tools/model_difftest.py --mutate`). | V | smbdis.asm `FlagpoleRoutine` (6579–6611), `FlagpoleSlide` (5810–5823), `ProcClimb`/`Climb_Y_SpeedData` (6036–6062); `runs/P2.1b-model/difftest_mutate*.log` |
| F56 | **smb-opt's player model, with two fixes, is exact on random inputs in the 1-1 third room.** As cloned it diverged on 53/200 random trials: (1) `NoRunningTimer` in the 1-1 cases (the game keeps the run cap for 10 frames after B is released, `RunningTimer` $0783); (2) jumps on a held A (the game needs A released the previous frame, `PreviousA_B_Buttons`). With `WithRunningTimer` and a new `a_held` state bit: 0 differences on the player fields (X/subpixel/speed/fraction/Y/y-speed/fraction/state) over 1,600 trials from frames 1150/1230 (random and WR-mutated inputs; 82k frames before a trace-parser fix that had cut comparisons at the first ground L+R frame, and 12,311 full-length frames / 200 grabs after it), 596/596 grabs identical in frame, Y and FPG flag. With the goomba pair added (`room_enemies`, F59): 0 differences on 97,335 full-length frames from the first control frame (700 trials, 119 matching deaths, 37 grabs incl. 35 core-confirmed glitch grabs). The model with goombas is exact for the 1-1 third room as far as tested. | V | `tools/model_difftest.py`; `docs/experiments/P1.2-lite-smb-opt.md`; `tools/smb-opt-modes.patch` |
| F57 | **Model-side BFS of the pole approach (H27).** `smb-opt bfs11`: replay to a root in the model, then a layered BFS over the 16 A/B/L/R inputs with global dedup on the compressed player state (sound: the model has no frame counter and no enemies are ahead of the stairs top) and MrWint's Bounds heuristic (x ≥ 3158 and y ≥ 162 by the deadline) as the bound. From the WR's frame-1229 state with deadline 1284: no glitch grab exists; the earliest grab of any kind is at frame 1279 (Y 152) and the lowest grab by 1284 is Y 161.875 — one pixel short of the glitch. Layer sizes 8 → 6.3M (peak, frame 1275), 41–55M states visited, 3–6 min, 2.2 GB (compact version). The core run v4 (`bfs_par`, same root) was killed by the OOM at layer 18 (1.94M states, 500 s/layer) and is superseded. | V | `runs/P2.1b-model/root1229_d55*.log`; `docs/experiments/P2.1b-pole-search.md` |
| F58 | **1-1 third-room enemies are two goombas on fixed trajectories.** `E_GroundArea6` has one object in the room: page 11 col 1 row 11 `id $37` (goomba pair). In the WR dump both exist from the room load (slots 0/1, Enemy_Flag 1), walk left 1 px every 2 frames at Y 184 — in the harness's 0-based frame numbering (= model step + 1046) x = 2768 − ⌊(f − 1044)/2⌋ after frame f (2767 at 1046, the step happens on even frames; +24 for the second) — and the first is stomped at frame 1111 (model step 65; an earlier write-up said 1112 because the injected bounce was one step late). Their positions are functions of the frame only (no RNG, no Mario dependence) until Mario touches them, so a layered search can treat them as frame-indexed obstacles with one alive bit each. | V | `tools/area_data.py E_GroundArea6`; `tools/ram_trace.py data/wr/fceux_wr.ram 1049 1240 Enemy_ID … --changes` |
| F59 | **Goomba interaction rules (for the model's enemy extension).** `RunNormalEnemies` (per enemy slot, after the player's movement in the frame): … `PlayerEnemyCollision` runs only when `FrameCounter` is even (third room: FC = f − 925 in the harness's 0-based frames, i.e. odd model frames; verified by `tools/model_difftest.py --goombas`), GES = 8, Mario not vertically offscreen, the enemy's masked offscreen bits are 0 and its state d5 clear. Boxes in screen-relative coordinates (`BoundBoxCtrlData`): small Mario (ctrl 1) x+3…x+13, y+20…y+32; goomba (ctrl 9) x+3…x+13, y+14…y+20; overlap per `CollisionCoreLoop` (inclusive). On overlap with `Enemy_CollisionBits` d0 clear (set it; cleared again when no overlap): `Player_Y_Speed > 0` → stomp (`HandleStompedShellE`: enemy state 4, harmless from then on, `StompTimer`++, `EnemyIntervalTimer` $10, enemy X speed 0, `Player_Y_Speed` = −4, Mario's state unchanged); otherwise, unless `StompTimer` ≠ 0 (1-frame chain window) or `InjuryTimer` ≠ 0, small Mario dies (`KillPlayer`). Goombas walk 1 px every 2 frames (x decrements on odd QuickNES frames), Y 184, and are erased 72 px beyond either screen edge (`OffscreenBoundsCheck`). The WR: boxes overlap at frame 1111 (Mario X 178, Y 168 → box 181–191 × 188–200 vs goomba at 175: 178–188 × 198–204), FC 186, y speed +4 → bounce. Implemented as `room_enemies` in the smb-opt patch: exact on 1,100 random/mutated trials from the first control frame incl. 44 deaths (`runs/P2.1b-model/difftest_goombas_*.log`). | V | smbdis.asm `RunNormalEnemies` 9060–9078, `PlayerEnemyCollision`/`HandlePECollisions`/`ChkForPlayerInjury`/`EnemyStomped`/`HandleStompedShellE` 11293–11500, `BoundBoxCtrlData` 12766, `CollisionCoreLoop` 12940, `OffscreenBoundsCheck` 11006; dump rows 1110–1122 (`tools/ram_trace.py`) |
| F60 | **1-1 pole approach mechanics.** On the ground, Mario's lower side point (Y+24) is in the base block's row for X ≥ 3155, so the ground approach is impeded at X 3154; a glitch grab needs the lower side point in the pole's row 9 with Y ≥ 162, i.e. Mario *rising* through Y 162–167 (after a jump from the ground at Y 176) at X 3158–3161. Landing on the block top (Y 160) gives a non-glitch grab. So the only fast finish is: descend from the staircase so that the landing is on the ground beside the block at X ∈ [3155, 3162] (right foot over the block, left foot on the ground), jump with L+R (moving_dir LEFT makes the block's push go right), grab while rising 2–3 frames later. The WR's jump off the staircase top (rising to Y 20) lengthens the 128-px descent from 36 to 53 frames so that it ends at X 3158; running off the edge lands at X 3130 and is stuck at 3154. The WR is on the exact x-frontier at the staircase top (x 3033 at step 183 = 50 frames before the table's earliest x ≥ 3158 at step 233) and its pole approach from that state is exhaustively optimal (F57). | V | model traces `smb-opt trace11room` (WR) and a run-off variant; search controls in `docs/experiments/P2.1b-pole-search.md`; `PlayerBGCollision` side-check order (upper point ignores climbable tiles) |
| F61 | **Whole-room search size.** From the fixed entry with deadline 238 (one step before the WR's grab) the relaxed model with goombas grows ×1.3/layer around frame 1090 (layers 10/20/30/40/45/48: 10.3k/102k/787k/1.68M/11.5M/27.1M unique, 107M visited, 6.4 GB) and the exact model ×1.5/layer (25.4M at frame 1090); both died at the memory cap. The variety is x-slack (≈200 x values) × speed fraction (64) × y fraction (256, persists across landings) × air phase — of order 10⁸ states per layer, ~4×10¹¹ model steps for the room. | V | `runs/P2.1b-model/room_relaxed_d239.log`, `root1045_d239.log`; `docs/experiments/P2.1b-pole-search.md` |
| F62 | **Pause and TimerControl law.** While `GamePauseStatus` d0 is set the NMI skips all timers (frame + interval incl. `IntervalTimerControl`), `FrameCounter` and all game logic, but **still steps the LFSR** (`PauseSkip` → `RotPRandomBit`); a pause lasts ≥ 44 frames (`GamePauseTimer` $2B gates the next Start). `TimerControl` ≠ 0 (injury/death $FF) skips the timers but not `FrameCounter` or the LFSR. Select does nothing in play; Start/Select are edge-triggered in `ReadJoypads`. L+R and U+D are never masked: L+R sets facing 3 (doubled friction, never the run cap on the ground, Right wins for acceleration); U+D while climbing = Up; Down on the ground cancels L/R (crouch only when big). A jump needs a fresh A press; the jump velocity uses `Player_XSpeedAbsolute`, updated only by `ImposeFriction`. | V | `docs/input-semantics.md` (smbdis.asm 780–812, 851–882, 2421–2447, 5558–5590, 5903–5952, 5966–6062, 6064–6130, 6139–6190, 6227–6270) |
| F63 | **The WR equals MrWint's enemy-free segment optima in every segment of 1-1.** Room 1: control (FCEUX row 197, X 40, speed 0) → vertical-pipe entry (GES 3, row 565) = 368 frames = `W11VertPipeEntry` (368). Bonus room: control (row 656, X 24) → side-pipe entry (GES 2, row 767) = 111 = `W11SubSidePipeEntry` (111). Third room: x ≥ 3009.06 (0xbc110) at model step 174 = `W11PipeSpeedup` (174); stairs→grab 66 steps vs his 67 equivalent (59 from a hand-picked state 8 frames ahead). His model as cloned is a relaxation of the real game (F56/F57 argument) except for enemy bounces, which it cannot produce; so rooms 1–2 are optimal in the real game unless a goomba/koopa bounce path beats 368 or 111 (H29). | V | `tools/ram_trace.py data/wr/fceux_wr.ram 190 935 GameEngineSubroutine AreaPointer … --changes`; `third_party/smb-opt/src/case/w11.rs` (solution comments); `docs/experiments/P2.1b-pole-search.md` |
| F64 | **1-1 room-1 enemy mechanics (goombas A x 352, B x 640, pair at the screen edge) are fully determined by Mario's path through the scroll.** Spawn: the next `E_GroundArea6` object loads into the first free slot on the first frame (with `AreaParserTaskNum & 7 ≠ 7`) where ScreenLeft + 303 ≥ its column x (`CheckRightBounds`: ScreenRight ≤ x ≤ (ScreenRight+48)&~15); groups (`HandleGroupEnemies`, id $37) are placed at **ScreenRight of that frame** (+24), not at their data column. `Enemy_X_MoveForce` is not initialised at spawn (half-pixel phase inherited from the slot). Walk 0.5 px/frame (`MoveObjectHorizontally`, force += $80), side check in the moving direction at (x+0/x+16, y+20) turns a goomba at the pipes, enemy–enemy overlap (odd `FrameCounter`, pair bit in `Enemy_CollisionBits`) turns both — a flattened goomba included; a stomped goomba (state 4, `EnemyIntervalTimer` 16, speed kept) is erased when the timer hits 14, i.e. on the 2nd tick of the 21-frame interval clock (22–42 frames); erased offscreen at x < ScreenLeft−73 or x ≥ ScreenRight+72/73. Reference simulation reproduces the WR dump on every enemy-slot field and the parser state for all 369 frames of room 1 and predicts both WR stomps; perturbed rules fail by 178–303 rows. Corrects F59's "enemy X speed 0" at a stomp (the speed is kept; state 4 just does not move). | V | `tools/room1_enemy_sim.py`; `docs/experiments/P2.5a-room1-enemies.md`; smbdis.asm `ProcessEnemyData` 7886, `HandleGroupEnemies` 8755, `MoveObjectHorizontally` 7541, `DoEnemySideCheck` 12592, `EnemiesCollision` 11552, `ChkKillGoomba` 9360, `OffscreenBoundsCheck` 11006 |
| F65 | **Scroll law.** Frame order is player movement → `ScrollHandler` → enemy slots 0..5. The screen scrolls by Mario's x delta of the frame (`Player_X_Scroll`) when the previous frame's `Player_Pos_ForScroll` (x − ScreenLeft) ≥ $50 and `SideCollisionTimer` = 0, minus 1 when the delta ≥ 2 and the position < $70; ScreenRight = ScreenLeft + 255. 0 mismatches on the WR's 369 room-1 frames. MrWint's `scroll_handler` (`WithScrollPos`) tests the current frame's x instead of the previous frame's relative position — a 1-frame difference at the thresholds, to be settled by the difftest in the port. | V | `tools/room1_enemy_sim.py` (`check_scroll`); smbdis.asm `ScrollHandler` 5378, `PlayerCtrlRoutine` 5605, `GameEngine` 5311 |
| F66 | **The WR equals MrWint's enemy-free segment optimum on every route segment he solved**: 1-1 (368/111/174, F63), 1-2 opening speedup 71, 8-4 room-1/2/3 speedups 79/70/70, 8-4 pipe clips 40 (room 2, from x 2373) and 74 (room 3 turnaround, from x 3388) — gap 0 everywhere; his `W84Part4Speedup` (water room) has no recorded solution (WR: 102 frames to x ≥ 185). | V | `docs/experiments/P2.5-segment-scan.md`; `third_party/smb-opt/src/case/{w12,w84}.rs` solution comments; `tools/ram_trace.py` rows 2487–2558, 15224–16550 |
| F67 | **8-4 is at the movement cap everywhere except two fixed accelerations.** `Player_X_Speed` = 40 on every frame of the room-1 middle (445 frames incl. the 4-page loop-back), room-2 middle (157) and room-3 middle (51); water room 696 frames (control 16720 → side-pipe entry 17416, 1020 px): swim cap 24 on 667 frames, 29 accelerating from 0; Bowser room 278 frames (17590 → axe 17868, 653 px): 40 on 246, 32 accelerating. Structure (rows): load 15060, control 15224, pipe 15748 (page 1 after the loop), control 15918 (page 7), clip 16185, control 16355 (page 12), turnaround clip 16550 (page 13), water control 16720 (AreaPointer 2), side pipe 17416, Bowser control 17590 (page 16), axe 17868. | V | `docs/experiments/P2.5-segment-scan.md`; `tools/ram_trace.py data/wr/fceux_wr.ram 15050 17868 AreaPointer GameEngineSubroutine Player_PageLoc --changes` |
| F68 | **MrWint's x-bound is sound and tight against the real game in 1-1 room 3.** 1,906,600 checks (core x gain in k ≤ 40 frames vs the table's max gain from the exact search classification of the same state) over 60,170 random-input steps from the room entry and the stairs, incl. L/R-heavy inputs: 0 violations, 0 unknown classes, the bound attained exactly in up to 38% of checks. Sampling evidence, classification included; other rooms only add obstacles except platforms/lifts (open). | V | `tools/heuristic_audit.py`; `smb-opt trace11bound`; `docs/experiments/P1.3-heuristic-audit.md`; `runs/P1.3-heuristic-audit/*.log` |

| F69 | **Input/dump alignment for any segment**: the FCEUX dump row r carries record r−2 in `SavedJoypadBits`; a model trace from record FIRST has step k ↔ FCEUX row FIRST+2+k ↔ QuickNES frame FIRST−2+k, root state = row FIRST+1. 1-1 room 1 from record 196 is exact for 126 frames until the first stomp; 4-2 main from 6584 (root row 6585), warp zone from 7247 (root row 7248). | V | brute-force of first × row offset in `docs/experiments/P2.3a-w42-model.md` ("Alignment"); `smb-opt tracec`; `tools/model_difftest.py --case … --first …` |
| F70 | **Head bump rule (`BlockBounceTimer` $0784)**: bumping a solid tile, or any block in a water area, or any brick/question block while the timer is nonzero → `Player_Y_Speed` = 1 (`NYSpd`); a fresh bump (timer 0) of a brick/question block → `PlayerHeadCollision` sets the timer to 16 and `BumpBlock` sets the speed to 0 (big Mario on a breakable brick: −2). The fraction is kept; the timer decrements every frame. The WR bumps the 4-2 bottom-corridor ceiling 6 times in a row (rows 6737–6762: timer 16→0→16, speed 1 while it runs). MrWint's model lacked the timer (always 0). | V | `smbdis.asm` 11946–11973, 7263–7264, 7307–7314; dump rows 6735–6762 (`tools/ram_trace.py … BlockBounceTimer Player_Y_Speed`); model exact through the bumps after the fix (`P2.3a-w42-model.md`) |
| F71 | **The joypad is disabled above the status bar / below Y $D0 only in water areas** (`PlayerCtrlRoutine` checks `AreaType` first). In 4-2's warp zone the WR holds A while above the screen (rows 7485–7511) and keeps the slow-gravity jump; MrWint's model dropped the joypad in every area. | V | `smbdis.asm` 5558–5571; WR trace exact over the above-screen frames after the fix |
| F72 | **A coin touch ends `PlayerBGCollision` for that frame** (head, foot and side checks all jump to `HandleCoinMetatile` → rts): no landing, no foot "deep in block" impede (`ChkFootMTile`: foot y low nybble ≥ 5 → `ImpedePlayerMove` by `Player_MovingDir`), no side impede on that frame. A model that ignores coins (`IgnoreCoins`) is wrong on every such frame; 4-2 main has 3 coins (row 10 cols 27–29), the warp zone 19 (`W42WarpCoins`). | V | `smbdis.asm` 11994–11995, 12011–12018, 12027–12075; reproduced on the dump (row 6789) and a mutated warp trial (f7422); 0 differences after the coin handlers |
| F73 | **The WR rides the 4-2 lift.** `E_UndergroundArea2` id $27 (page 3 col 10 row 5) = `LargeLiftDown`: spawns at row 6933 at x 940 (page 3 X 172), y 80, descends 15/16 px per frame (`PlatLiftDown`: Y_MoveForce $f0, Y_Speed 0; `MoveLiftPlatforms`); a rider is placed by `PositionPlayerOnVPlat` (Player_Y = lift Y − 32, y speed/force 0); its side stops Mario horizontally. The WR stands on it rows 7012–7024 (X 954→986, Y 123→132) and jumps off at 7025 from Y 132 (44 px above the floor). `PlatformCollisionFlag` ($03A2) is not involved (balance platforms only). The top route crosses the same pit (cols 57–62) while the lift descends through it. | V | `smbdis.asm` 8944–8970, 10982–10990, 11846–11862; dump rows 6933–7089 (`Enemy_Y_Position+3`), 7010–7040 (`Player_State`/Y); `P2.3a-w42-model.md` |
| F74 | **Vertical pipe entry** (`HandlePipeEntry` 12270): Down held, left foot (X+3) on metatile $10 and right foot (X+12) on $11, same frame → GES 3 (from `ChkFootMTile`, i.e. standing/landing and not walking into it). 4-2 wrong-warp pipe (col 84): X ∈ [1348, 1356]; the WR lands at X 1343 (row 7168) and walks 5 frames to 1348 (7173) — no frame lost. Warp-zone world-8 pipe (col 50): X ∈ [804, 812]; the WR enters on the landing frame at 810 (7724). The model's entry (`StateChangeVerticalPipe`) means "entry possible"; Down must be added when replaying a search result. | V | `smbdis.asm` 12270–12290, `BlockBuffer_X_Adder` 13031; dump rows 7168–7175, 7720–7724; model entry frames = core GES 3 frames in both segments |
| F75 | **4-2 model validation**: `W42Warp` is frame-exact vs QuickNES on the WR's 476 frames and on 40×300 random + 40×480 WR-mutated trials (0 differences; piranha plants, id $0D from row 7480, are the only unmodeled objects and only kill). `W42Main` is exact on the WR's rows 6586–7011 (to the lift) and on 40×250 random + 40×420 mutated trials except one lift side contact. | V | `tools/model_difftest.py --case W42Warp --first 7247 …`, `--case W42Main --first 6584 …` (seeds 7/3/11/5); `P2.3a-w42-model.md` |
| F76 | **MrWint's IDA* vs a full-input BFS**: `smb-opt case W11VertPipeEntry` solves 1-1 room 1 (368 frames) in 22 s / 86 MB / 283,205 states; the same case as a 16-input per-layer BFS with the same x-bound reaches 5.2M unique states by layer 60 (1 frame of slack). The difference is `SmbInputFetcher` (ida.rs:19), a reduced per-state input set (B only on the ground with the moving direction, L+R only when standing, A only when a jump can start/continue, no A|B combos) — a domain reduction, not a proven-lossless one. | V | `runs/P2.3a/ctrl_w11_ida.log`, `runs/P2.3a/ctrl_w11_d368.log` |
| F77 | **Scroll law**: `ScrollHandler` uses `Player_Pos_ForScroll` = the relative x computed at the end of the *previous* frame (x before this frame's movement − ScreenLeft before this frame's scroll): scroll by this frame's dx if that value ≥ $50, SideCollisionTimer = 0 and dx > 0, minus 1 if dx ≥ 2 and the value < $70. MrWint's model used the current frame's relative x (one frame early). | V | `smbdis.asm` 5378–5400; `tools/room1_enemy_sim.py check_scroll` (0 mismatches, room 1); 4-2 main 587/587 rows exact after the fix (`P2.3a-w42-model.md` §P2.3b) |
| F78 | **Area parser timing**: `ScrollThirtyTwo` ≡ ScreenLeft mod 32 (0 mismatches over 4-2 main and 1-1 room 1); when it wraps while `AreaParserTaskNum` is 0 a burst of 8 tasks starts (value 7 after that frame, then −1 per frame); the enemy loader skips frames whose value & 7 == 7 — **the value it sees is the previous frame's** (amended 2026-08-23, P2.5c): `GameEngine` runs the enemy slots (`EnemiesAndLoopsCore`, the only caller of `ProcessEnemyData`) before `RunParser` at its end, so a burst-start frame (post-frame value 7) still loads enemies and the *next* frame is the skipped one; evidence: WR dump row 6733 consumes the page-2 area-change object on a burst-start frame (`tools/w42_enemy_sim.py`: 0 parser mismatches with the pre-frame rule, 2 with the post-frame rule; 1-1 room 1 cannot tell the two apart). The lift hook `w42_lift` was corrected accordingly (it had used the post-frame value; the WR's lift spawn is not on a boundary, so F81 stands). Exceptions: frames with `VRAM_Buffer_AddrCtrl` = 6 (block/coin VRAM writes) pause the parser. | V | `smbdis.asm` 3036–3046, 5360–5370, 7771–7775; dump test in `P2.3a-w42-model.md` §P2.3b |
| F79 | **The WR's 4-2 wrong-warp pipe entry is 1 frame late.** `HandlePipeEntry` needs Down + feet on `$10`/`$11` and runs on every standing frame; HappyLee is at X 1348 with no L/R on row 7172 but presses Down on 7173. Replaying the WR on the QuickNES core with Down added to record 7170 enters at frame 7168 instead of 7169; record 7169 does not (X 66). The other 6 vertical pipe entries of the WR (rows 565, 3767, 7724, 15748, 16185, 16550) are earliest-possible. Absorbed by the framerule on the WR's route (4-2 deficit 13 → 12). | V | `tools/pipe_entry_scan.py` → `runs/P2.3a/pipe_entry_scan.log` |
| F80 | **The 4-2 bottom route walks through the solid block at floor level.** `DoPlayerSideCheck` returns after the first side point (x+2, then x+13) that touches any metatile; `ImpedePlayerMove` for the x+2 point does nothing while moving right. Standing Mario's side points are in row 10; the coin at (29,10) admits the x+2 point into the brick run cols 30–47 and the x+13 point is never checked again, so the WR walks inside the bricks from x 481 to 824 at Y 176 (rows 6804–6960). A bumped brick in `$23` state inside that run stops him (difftest trials). The model reproduces this (same early return). | V | `smbdis.asm` 12027–12061, 12318–12345; WR dump rows 6804–6960 + block buffer; `P2.3a-w42-model.md` §P2.3b |
| F81 | **4-2 lift model validated**: `W42Main --lift` is exact on the WR's 587 frames (13 ride frames) and on 120 mutated trials from roots 6990/7000 (every trial touching the lift; 667 contact frames in 60) — the only differences are koopa deaths and bumped blocks in `$23`/`$c4` state (unmodeled block states). **Correction 2026-08-23 (P2.5c-1):** those "koopa deaths" are deaths by **pipe A's piranha plant** (x 1160, up at y 136 between rows 7051 and 7115; the koopa is 70 px further right at those frames) — re-run after the F78 hook fix: 60 trials, 11 with a difference, all `core left GES 8 at f7088–7096` with Mario at x 1140–1180, the lift ride frames identical (`runs/P2.5c/difftest_lift_prefix_fix.log`). | V | `tools/model_difftest.py --case W42Main --first 6584 --lift 0 --root-record 6990 --n 60 --len 120 --mutate 4 --seed 21` (and 7000/33) |
| F82 | **A reduced input set does not make the proof space small**: 1-1 room 1 with deadline 368 (1 frame of slack) has 5.20M unique states at layer 60 with all 16 inputs and 2.93M (4.96M at layer 65, ×1.15/layer) with MrWint's per-state set; his IDA\* finishes in 22 s only because DFS finds the goal early at the optimal bound. | V | `runs/P2.3a/ctrl_w11_d368.log`, `ctrl_w11_d368_reduced.log` (`bfsc … --reduced`) |
| F83 | **4-2 warp-zone route structure**: speed 40 from x 82; over the stairs onto the roof above the pipes (Y 32, x 782–932); drop past the roof's right end at x ≈ 958 (row 7648); run back left 150 px to the world-8 pipe (X 810, row 7724). The x-only bound (329 frames) misses the forced overshoot-and-return and the 9-row climb; the 147-frame gap to the WR is mostly this, not wasted frames. | V | `tools/ram_trace.py data/wr/fceux_wr.ram 7248 7724 Player_PageLoc Player_X_Position Player_Y_Position Player_X_Speed`; `P2.3a-w42-model.md` §P2.3c |
| F84 | **Block-bump law** (small Mario, `BlockBounceTimer` 0): `PlayerHeadCollision` writes `$23` into the cell and `DestroyBlockMetatile` into `VRAM_Buffer1`; the block object (2 slots, `SprDataOffset_Ctrl` toggles) bounces with speed −2 / force +$50 per frame and returns on the 13th frame after the bump (`Block_RepFlag`), independent of the never-initialised `YMF_Dummy` fraction (all 256 values); `BlockObjMT_Updater` restores `Block_Metatile` on the 14th frame **only if `VRAM_Buffer1` is empty** (it runs after the player routine, so the player sees `$23` on frames 1–14 after the bump, timer 15..2). `Block_Metatile` = `$c4` for question/hidden/item bricks (`BrickQBlockMetatiles`), the original for plain bricks; coin bricks `$58/$5d`: first bump sets `BrickCoinTimerFlag` + `BrickCoinTimer` 11 (interval timer, 21 frames per tick), stay coin bricks while flag set & timer > 0, become `$c4` when flag set & timer 0; `BrickWithCoins` (parser) clears the flag when a coin brick is rendered. | V | `smbdis.asm` 7219–7305, 7307–7330, 7443–7520, 7660–7745, 4184; dump rows 6737–6770 (bumps at 6737/6756, RepFlag 6750/6769, restore 6751/6770); `docs/experiments/P2.3b-2-block-states.md` (bounce simulation) |
| F85 | **`VRAM_Buffer1` flush law**: the NMI transfers and clears `VRAM_Buffer1` only when `VRAM_Buffer_AddrCtrl` is 0; when it is 6 (column renderer `RenderAreaGraphics`/`RenderAttributeTables`, coin erase `RemoveCoin_Axe`, `CheckTopOfBlock`) the buffer keeps its content. In-frame writers before `BlockObjMT_Updater`: coin award digits, `DestroyBlockMetatile`; after it: the restore write, `ColorRotation` (`FrameCounter & 7 == 0`), `RunGameTimer` (every 24 frames, `GameTimerCtrlTimer`). A coin award at row 6789 sat in the buffer through row 6793 behind a render burst. | V | `smbdis.asm` 760–775, 1958–1990, 6436–6468, 7084–7100, 5323; dump rows 6585–7173 (`tools/block_state_check.py`: 0 mismatches on the non-empty/AddrCtrl flags over 587 rows) |
| F86 | **Area-parser task schedule** (refines F78): a burst = tasks 7 `AreaParserCore` (column C), 6/5 `RenderAreaGraphics`, 4 `IncrementColumnPos`, 3 `AreaParserCore` (C+1), 2/1 render, 0 increment + `RenderAttributeTables`; tasks 6, 5, 2, 1 and the attribute write leave `VRAM_Buffer_AddrCtrl` = 6 (post-frame `AreaParserTaskNum` 6, 5, 2, 1, 0); the parser is skipped on frames where `AddrCtrl` is already 6; a burst starts when `ScrollThirtyTwo` ≥ 32 and the task counter is 0 (a wrap during a burst is served when it ends). Columns: two per burst from `CurrentPageLoc·16 + CurrentColumnPos` (24 at the 4-2 control row): (43,2) processed at ScreenLeft 334 (burst 10, 2nd half), (77,7) at 874 (burst 27, 2nd half). | V | `smbdis.asm` 3035–3058, 5360–5375, 1895–1940; dump (`tools/block_state_check.py`: AreaParserTaskNum + parser column 0 mismatches over 587 rows) |
| F87 | **4-2 main area exact with block states**: `W42Main` (lift + `BlockStates`) is frame-exact on the WR's 587 frames (bookkeeping fields 0 mismatches vs the dump incl. the `$23`/`$c4` cells) and on 100 random + 240 mutated trials from roots 6720/6770/6990/7000/7030 (fresh bumps in all 240: (28,7) mushroom brick, (51,7) question block, (64,3) vine brick, (66,7) hidden block, (77,7) coin brick, corridor bricks) — the only remaining differences are koopa deaths (28 trials, every earlier frame matching). HappyLee's route itself bumps (28,7), (51,7) and (64,3). | V | `docs/experiments/P2.3b-2-block-states.md` (commands); `tools/model_difftest.py --case W42Main --first 6584 --lift 0 …`; `tools/block_state_check.py` |
| F88 | **HappyLee's last 12 frames before the 4-2 wrong-warp pipe are 3 frames slow.** From the WR's state at record 7159 (x 0x52eb0, jumping at speed 0x28), the exhaustive 12-step search (`bfscx W42Main … 6584 575 587`, all 16 inputs, lift + block states) reaches the pipe-entry state in 10 steps (16,194 goal transitions); the reconstructed inputs [5×L, 2×B+R, 2×R, 1×NIL] + Down replayed on the QuickNES core enter the pipe (GES 3) at frame 7166 vs the WR's 7169 (F79's Down-earlier: 7168) — 0 model/core mismatches. Absorbed by the framerule (4-2 deficit 13 → 10 on the WR's own route). | V | `runs/P2.3c/ctrl_w42main_p575_d587.log`, `runs/P2.3c/ctrl_w42main_path.bin`; `tools/replay_check.py --case W42Main --first 6584 --prefix 575 --path runs/P2.3c/ctrl_w42main_path.bin --lift 0 --down` |
| F89 | **4-2 warp-zone staged bound** (`W42Warp` + `--stage`, P2.3c-2): the pipes lie under the roof (row 2, cols 48–59) whose only way down is the gap at cols 60–61 before the right wall (cols 62–63), so every route reaches X ≥ 957 (both foot points over the gap) and Y ≤ 32 (over the roof) before the 96-px descent to the pipe top (Y 128). Bound = max(x-table steps to X ≥ 957 coupled with the return from the first-crossing class to X ≤ 812 (an end-class-restricted max-displacement DP over the x-class graph), climb/descent at the speed caps 5 / 4.5 px per frame). At the WR's entry state: **461** (x-only bound: 329; WR: 476); 0 violations along all 476 WR steps; the 15 frames of slack lie in the last ~100 steps (braking before the gap, the fall, landing on the pipe), none before step 380. | V | `runs/P2.3c/warp_stage3_p0_d475.log` (`bfscx W42Warp data/wr/wr_inputs.bin 7247 0 475 --stage --check-path 476`); `src/case/w42.rs`, `src/heuristics/xpos.rs::build_overshoot_bound` |
| F90 | **HappyLee's warp-zone return (the last 76 frames, from the drop at x 958.7) is exactly optimal** in the exact model: from the WR's state at record 7647 the complete 76-step search reaches the pipe entry on step 76 (12.4M goal transitions, the WR in the frontier at every layer, 21M states/layer at the peak) and the complete 75-step search dies out at layer 23 with no goal. The x-only return bound from that state is 71: the 5 extra frames are x/y coupling (fall, landing window) no x table sees. | V | `runs/P2.3c/warp_stage_p400_d76.log`, `warp_stage_p400_d75.log` (`bfscx W42Warp … 7247 400 {76,75} --stage --check-path 76`, 2 GB cap) |
| F91 | **Warp-zone proof size**: with the 461 bound (15 frames of slack) the exact frontier is 9.2M states at layer 24 growing ×1.25/layer (`pruned` already 8.7M/layer); at zero slack the return phase alone holds ~2×10⁷ states/layer (F90). A 476-layer proof run is therefore ≥ run-4 scale (10⁸–10⁹ states/layer) — cloud only, and worth running only after an end-game (x,y) bound removes most of the 15 frames. | V | `runs/P2.3c/warp_stage_p0_d475.log` (layers 1–30, bound 460), `warp_stage3_p0_d475.log` (layers 1–24, bound 461) |
| F92 | **4-2 main-area geometry, exact** (`data/blockmaps/w42_main.txt`, cols → x = 16·col, standing on row r ⇔ Player_Y = 16r): ceiling row 0 except cols 1–5 and 57–66; hanging brick mass rows 1–3 cols 20–47 (bottom at screen 96); pillar rows **7–9** cols 22–26 with **row 10 open beneath it** (a 16-px floor-level tunnel, which is how the WR passes it at Y 176); coin shaft cols 27–29 (coins at row 10, (29,7) open); the run rows 7–10 cols 30–47; question blocks (50,3) (51,3) (50,7) (51,7) (54,7) (55,7) (56,7); lift pit cols 57–62; hidden blocks (63,6) (64,5) (65,6) (66,7), vine ledge (64..66,3); pipe A cols 72–73 rows 8–10 (top Y 128); bricks (76,7) (77,7) (80,7) (81,7) (top Y 112); pipe B cols 78–79 rows **4–10** (top Y 64, a wall from the floor to row 4); wrong-warp pipe cols 84–85 rows 8–10 (`$10 $11`, goal X ∈ [1348, 1356] at Y 128); entrance floor pits at cols 11–12, 15–16, 18–20 (single floor blocks at cols 17 and 21). Small Mario's collision points use the crouching adder set (`BlockBufferAdderData[2] = $0e`): head (x+8, Y+18), feet (x+3 / x+12, Y+32), sides (x+2 / x+13, **Y+24**, both "halves" the same row). | V | `data/blockmaps/w42_main.txt` (parsed per row in `docs/experiments/P2.3c-2c-main-area.md`); `smbdis.asm` 11929–11941, 13027–13040; `emu.rs::block_buffer_collision`; WR dump rows 6736–6746 (tunnel at Y 176 under cols 22–26 at speed 40) |
| F93 | **The bottom route's col-30 wall entry costs 31 frames and is pure collision mechanics**: at row 6784 the WR's right side point (x+13, row 8) hits col 30 while falling → `ImpedePlayerMove` zeroes the speed (40 → 0, the sub-speed byte $d8 is kept). He then enters the wall in 17 frames: one Left press (record 6785) makes `Player_MovingDir` LEFT so the **foot-check impede** (feet inside (30,9)/(30,10) with low nybble ≥ 5 → `ImpedePlayerMove` in the moving direction) pushes him **+1 px** toward the wall instead of away, the kept sub-speed drifts him +0.95 px/frame, and two hops avoid the standing side impede; at row 6801 (x 480.4) the left side point is inside col 30 and the side check never impedes rightward motion again (F80). Then he re-accelerates from 0. The mushroom spawned by his (28,7) bump (slot 5 from row 6780) is not involved. The model reproduces every frame (F87). | V | `tools/ram_trace.py data/wr/fceux_wr.ram 6770 6812 Player_X_Position Player_X_Speed Enemy_ID+5 SavedJoypadBits`; `smb-opt tracec W42Main data/wr/wr_inputs.bin 6584 230 --lift 0` steps 200–221; `smbdis.asm` 11975–12061 (`DoFootCheck`/`DoPlayerSideCheck`), 12318–12351 (`ImpedePlayerMove`); `emu.rs::player_bg_collision` |
| F94 | **Where the x-only `W42Main` bound is loose on the WR** (root 540 vs 588): slack 46 → 36 over the entrance fall (steps 1–30: a falling Mario accelerates ~0.6 px/frame² vs the table's runner), 36 → 35 through the tunnel, **35 → 4 in the col-30 wall entry (steps 201–221, F93)**, 4 → 1 over the last 5 steps (F88's braking). Between steps 221 and 583 (x 482 → 1343: re-acceleration, run, lift ride, pipes A/B) the WR is tight to the x-table (slack 3–4 constant). So a top route that avoids the wall has at most ~10 (fall) + couplings above the x-bound — HappyLee's 577 (F36) is 37 above it. | V | `runs/P2.3c/main_xonly_p0_d587_checkpath.log` (`bfscx W42Main data/wr/wr_inputs.bin 6584 0 587 --lift 0 --check-path 588 --stop-step 3`; slack = 588 − step − h per line) |
| F95 | **Hetzner Cloud billing and the account quota.** *Quota (2026-08-22): the project account is new and only the three lowest dedicated tiers (ccx13/ccx23/ccx33, ≤ 8 cores) are offered; ccx43/53/63 need a limits increase and the console refuses the request as the account is too new. Shared-tier availability unconfirmed.* Hourly, rounded up (a partial hour bills as one; no daily minimum), and charged as the *lower* of hours × hourly or the monthly cap (ccx63: 744 h × $1.6138 = $1,200.67 → capped at $1,006.99). **A powered-off server still bills — "we will bill you for your servers until you delete them, independent of their state"; only `hcloud server delete` stops the charge.** Dedicated x86 rates (fsn1/nbg1/hel1, USD net): ccx33 8c/32 GB/240 GB $0.2612/h, ccx43 16c/64 GB/360 GB $0.5216/h, ccx53 32c/128 GB/600 GB $1.0088/h, ccx63 48c/192 GB/960 GB $1.6138/h — **$/core-hour is flat to within 6 %** (0.0315–0.0336), so the $300 cap is ~9,000 dedicated-core-hours and machine size buys wall-clock only. Ancillaries: snapshots $0.0199/GB-month (compressed size), volumes $0.0767/GB-month (billed while they exist), primary IPv4 $0.60/month billed separately (IPv6 free), automatic backups a flat 20 % of the server price. **Shared tiers are 4-10x cheaper per core** (cx53 16c/32 GB/320 GB $0.0561/h = $0.0035/core-h, cax41 ARM 16c $0.0777/h, cpx51 16c $0.1338/h) but are sold as baseline-plus-burst, not sustained full load; Hetzner's FAQ neither permits nor forbids sustained 100 % use, so contention and tolerance are **unmeasured**. | V | <https://docs.hetzner.com/cloud/billing/faq/>; authenticated `GET https://api.hetzner.cloud/v1/pricing`; `hcloud server-type describe ccx{33,43,53,63}`; `docs/experiments/P0.10-cloud-sizing.md` §2–3 |
| F96 | **`bfscx`/`bfs11c` scaling limit is the serial merge.** Expansion is embarrassingly parallel over `--threads N` (each thread slices the parent layer file, expands 16 inputs/parent, sorts/dedups a private `--acc-mb` accumulator, spills sorted runs); the k-way merge that writes the next layer file is a **single-threaded** `BinaryHeap` drain (`third_party/smb-opt/src/main.rs`, expansion ~1165, merge ~1272). Measured on run 4's ~700 M-state layers (i5-1335U, `--threads 12`, `--acc-mb 256`): expand 978–1401 s vs merge 36.6–75.3 s of 43–49 runs = **serial fraction 4–6 %**, i.e. ~14,400 laptop-core-seconds of parallel work per big layer. Amdahl (assuming a Hetzner dedicated core ≈ 1.6× an i5-1335U E-core — **unmeasured**): 16c ≈ 2.1×, 32c ≈ 3.8×, 48c ≈ 5.4× (merge 21 % of wall), 64c ≈ 6.7× (26 %) → **the knee is ~48 cores** until the merge is parallelised (P2.3c-3: bucket run output by key prefix, merge buckets independently, concatenate in key order). Raising `--acc-mb` (192 GB allows ~3000 MB × 48 threads) cuts the run count and shrinks the merge without code changes. | V | `runs/P2.1b-model/room_compact_d238_r85.log` layers 137/141; `third_party/smb-opt/src/main.rs`; `docs/experiments/P0.10-cloud-sizing.md` §4 |
| F97 | **Hetzner dedicated / Server Auction terms and day rates** (the route around the Cloud quota; Robot is a separate product with no vCPU-quota system). **No setup fee** on the auction (*"There are no setup fees for these servers"*), **no minimum contract term**, and Robot lets you *"usually choose to cancel the server immediately"* with no notice period. **Billing is exact to the hour, pro-rata, charged after use — "you will never pay more than the monthly price"** ⇒ the monthly figure is a *ceiling*, not a commitment, so compare **day rates**. Hetzner's "30 days to the end of the month" policy does not bite (Cloud exempt; dedicated cancellable immediately; even under it you lose access at once and pay only for time held). New AX-line servers are worse value: setup fees rose in April 2026 (AX102 €79 → €269) and the 15 June 2026 restructure raised monthly prices — **the auction was explicitly excluded from both**. Live 2026-08-22 (net, incl. €1.70 IPv4): EPYC 7502P 32c/64t 128 GB + 7.7 TB €333.70/mo (~$12.10/day) or + 1.9 TB €266.70 (~$9.67/day); Ryzen 9 5950X 16c/32t 128 GB + 7.7 TB €247.70; Ryzen 9 3900 12c/24t 128 GB + 3.8 TB €178.70 (~$6.48/day); Ryzen 7 7700 8c/16t 64 GB + 2 TB €105.70. **Against the cloud ccx33 (8c/32 GB/240 GB, $6.27/day), the 32c/64t EPYC with 1.9 TB is $9.67/day — 1.5× the price for 4× the cores, 4× the RAM, 8× the disk.** Caveats: provisioning up to one business day and **new accounts may be delayed by verification** (the account-age risk can reappear here — unverified for this account); used, unmanaged hardware, no stated warranty; inventory rotates fast. | V | <https://docs.hetzner.com/robot/general/server-auction-faqs/>; <https://docs.hetzner.com/general/billing-and-account-management/cancellation/cancellations-robot/>; <https://docs.hetzner.com/general/general-terms-and-conditions/30-days-to-the-end-of-the-month/>; <https://www.hetzner.com/pressroom/standardization-and-price-adjustment-of-our-server-products/>; Server Radar (`radar.iodev.org`, browser UA required); `docs/experiments/P0.10-cloud-sizing.md` §8 |
| F98 | **4-2 main-area search size is set by the bound's slack, and a tight bound keeps the frontier flat.** Measured on the S1 segment ladder (`runs/P2.3c/top_s1_x339_y112_d149.log`, 4-2 main area, **2 threads**, deadline 149 = 2 frames above the 147 that has no path): the frontier **plateaus at ~1.4×10⁷ and turns over** — 13.7M, 14.0M, 14.1M, 14.1M, 14.0M, 13.9M at layers 85–90 — at ~131 s/layer, 90 layers in 41 minutes. Contrast the warp zone at 15 frames of slack: 9.2M at layer 24 growing ×1.25/layer (F91), and run 4's ~7×10⁸/layer at ~1000 s. **Machine sizing therefore spans three orders of magnitude on this one number:** at ~10⁷/layer the full 575-layer run is ~4–8 h on the laptop (~190 GB raw, ~7 GB xz at 24 B/record) and needs nothing rented; at ~10⁸ it is ~2 days; at ~10⁹ it is ~3 weeks and wants the auction EPYC (F97). Caveat: S1 is a *segment* search with a fixed position goal, so the full-level frontier will be larger — the mechanism is measured, the magnitude is not. **Do not rent on spec**: a box ordered before the bound lands idles at $10–12/day. | V | `runs/P2.3c/top_s1_x339_y112_d149.log` layers 85–90; `runs/P2.3c/top_s1_x339_y112_d147.log` (no path, layer 29); `docs/experiments/P0.10-cloud-sizing.md` §9 |
| F95 | **Position-goal searches under the x-only bound explode on height variety**: 4-2 main, root → x ≥ 339 with Y ≤ 112 (the pillar top; x-bound 137): deadline 147 dies at layer 29; deadline 149 (2 frames of slack) holds 13M states/layer from layer 75 and 23M at layer 100 growing ×1.2/layer with `max x` ≈ 215 px; deadline 165 doubles per layer from the start. The survivors are the y-variants (heights, fall phases) of few x-classes — a main-area proof run needs a y-coupled term (landing + jump physics at the gates) before any deadline near the optimum is affordable; the same is why the 1-1 room-1 control grows ×1.14/layer at 1 frame of slack (F82). | V | `runs/P2.3c/top_s1_x339_y112_d{147,149,165}.log`, `runs/P2.3c/top_s1_ladder.log` (`tools/bfscx_ladder.sh`, 2 threads, 2 GB cap, 20M-record watchdog) |
| F99 | **4-2 main-area enemy laws reproduce the WR dump exactly** (`tools/w42_enemy_sim.py`, 0 mismatching rows over the 588 rows 6585–7172 on every slot field: flag/id/state/x/y/speed/force/dir/timers/masked bits, plus the enemy-data parser offsets). The objects: the `$3A` group = 3 goombas placed at ScreenRight (+24 each) at Y 112 (y $78) when ScreenLeft + 303 ≥ 736 (row 6857, Mario x 567; F65 law), walking left 0.5 px/frame on the brick run (rows 7–10), never reaching its end before being erased at ScreenLeft − 73 (rows 6967/6975/6983); the green koopa `$00` spawns at x 1232 (col 77) when ScreenLeft + 303 = 1232 (row 7055, a burst-start frame → F78's pre-frame rule), walks left 0.5 px/frame at y 184 and turns at pipe A's right side when its x+0 point enters col 73 (row 7153, x 1183 → speed +8, dir 1); the buzzy beetle `$02` spawns at x 1328 (col 83, row 7095) and walks left. Goomba rules as F56–F59/F64; koopa/beetle walking = the goomba rules with bounding-box control 3. Negative controls: post-frame parser gate 119 mismatching rows, side probe x+1 20 rows, plant spawn on task 7 only 63 rows. | V | `python3 tools/w42_enemy_sim.py` (defaults); controls in `docs/experiments/P2.5c-w42-enemies.md` |
| F100 | **Piranha plants are spawned by the area parser, one per vertical pipe, into the first free enemy slot 0–4 — and there is no "no plant" bit.** `VerticalPipe` (`smbdis.asm` 3843–3884) spawns a plant when it renders the pipe's *left* column unless `AreaNumber|WorldNumber == 0` (1-1) or no slot is free (then the pipe is drawn without one). In 4-2 main: pipe A (col 72) row 7004, pipe B (col 78) row 7042, pipe C (col 89) row 7110 — each on the frame whose parser task is a render task (post-frame `AreaParserTaskNum` 7 or 3) with `CurrentPageLoc·16 + CurrentColumnPos` = the pipe's column; **the wrong-warp pipe (col 84, row 7080) got no plant in the WR only because all five slots were occupied** (plant A, the vine, plant B, the lift, the koopa) — a route that frees a slot (no vine bump, an erased lift, a stomped goomba …) gets a plant in the wrong-warp pipe. Initial RAM: x = col·16 + 8, y = row·16 + 32 (= the pipe top), `PiranhaPlant_Y_Speed` (= Enemy_X_Speed) 1, `MoveFlag` (= Enemy_Y_Speed) 0, DownYPos = y, UpYPos = y − 24, bbox ctrl 9 (x+3..x+13, y+14..y+20); frame timer / collision bits / moving dir inherited from the slot. Movement (`MovePiranhaPlant` 10577–10634): 1 px on odd `FrameCounter` frames, 24 px of travel, then `EnemyFrameTimer` = $40 (63 idle frames; frame timers tick every frame while `TimerControl` = 0); parked at the top it descends unconditionally when the timer expires; parked at the bottom it rises only when the low byte of \|enemy x − Mario x\| (sign from the 16-bit difference) is ≥ $21 — **this distance rule is asm-sourced, the WR never exercises it** (control: threshold $41 → 0 mismatches). Contact = injury/death, no stomp (11338–11339); excluded from enemy–enemy collision; never erased on the right, erased on the left at ScreenLeft − 16 (−271 while ScreenLeft_X ≥ $c7). The dump: A rises 160 → 136 over rows 7005–7051, waits to 7115, descends; B is fully up (y 72) when the WR jumps pipe B (~7150); A erased at 7154. | V (spawn/cycle/erase on the dump) + S (distance rule: asm) | `tools/w42_enemy_sim.py`; `docs/experiments/P2.5c-notes-piranha.md` (line refs); `P2.5c-w42-enemies.md` |
| F101 | **The player's hitbox for enemy collisions is built from Mario's position *before* `PlayerBGCollision`.** In `PlayerCtrlRoutine` the order is `PlayerMovementSubs` → `ScrollHandler` → `RelativePlayerPosition` → `BoundingBoxCore` (5607–5609) → `PlayerBGCollision` (5610); the enemies' `PlayerEnemyCollision` (run later in the frame) compares the enemy box with that box. So a landing snap (`LandPlyr`: Y 129 → 128) or a wall push-back (`ImpedePlayerMove`: x 1235 → 1234) in the same frame does not move the box: Mario landing on pipe A at Y 128 is killed by the rising plant whose box starts at 161 (his box is 149..161, from Y 129 — RAM `$04AC..$04AF` in the dump), and Mario pressing into pipe B is missed by a koopa whose box ends at 134 (his box is 135..145, from x 1235). The box arithmetic itself is the closed-interval test (`SprObjectCollisionCore` 12934–12990, no wrap): `BoundBoxCtrlData` entry 1 (small Mario) = (3,20,13,32), entry 9 (plant) = (3,14,13,20), entry 3 (koopa/beetle) = (2,9,14,21). The model's enemy hook reads the pre-collision position (`emu::pre_bg_pos`, set in `run_step` before `player_bg_collision`); both cases then die on the core's frame (f7132 / f7232). | V | `tools/ram_slots.py` + the box bytes of `runs/P2.5c/keep/{k0,k2t6}/*.ram`; `tools/model_difftest.py --case W42Main --first 6584 --enemies 0 --wr-file runs/P2.5c/keep/k0/wr.bin --wr --root-record 7030 --len 136 --n 0` (death f7132 ok) and `… --wr-file runs/P2.5c/keep/k2t6/koopa_0002_t6.bin --wr --root-record 7106 --len 160 --n 0` (death f7232 ok); `docs/experiments/P2.5c-w42-enemies.md` §step 1b |
| F102 | **The Rust 4-2 main-area enemy module (`W42Main --enemies`) is exact against the QuickNES core on 5,787 trials / 877,058 compared frames with 0 differences**, over three region prefix sets built by `tools/w42_prefix_gen.py` (top floor before the goomba group; between pipes A and B; past pipe B) with random and stomp-selected continuations: 1,825 enemy deaths on the same frame as the core's GES 11, 3,686 stomps and 949 kicks whose following frames (bounce, shell motion, shell kills) matched. Covered: the goomba group, koopa and buzzy beetle walking/turning/stomp → shell/kick/shell kills, the three plants incl. the pipe-A landing race (F101) and the slot-dependent wrong-warp-pipe plant (F100, present in every vine-free prefix), slot occupancy with the lift. Not covered: big Mario/fire, the vine, columns ≥ 98. Searches now prune enemy deaths and refuse item-cell bumps (H34); the WR's own suffix is outside the enemy model (its vine bump), a vine-free pipe-entry path searches end to end. | V | `runs/P2.5c/difftest_enemies_{topfloor2,topfloor_hop,topfloor_stomps,koopa,koopa_stomps,beetle2,beetle_stomps2}.log`; `tools/model_difftest.py --enemies 0 --prefix-dir … [--require-event] [--stop-x 1540]`; `docs/experiments/P2.5c-w42-enemies.md` §step 1b |
| F103 | **4-2 main-area segment S1 — the enemy-free optimum from the room entry (record 6584) to the first frame with x ≥ 339 ∧ Player_Y ≤ 112 (on/over the pillar top, the top route's first milestone) is exactly 149 steps.** Proof: the `bfscx --goal-x 339 --goal-y 112 --lift 0` run with deadline 149 (y-coupled bound, 0-violation audits) reached the goal at layer 149 (45,545,904 goal transitions) and at no earlier layer — a deadline-149 run cannot prune a state on a ≤ 148-step goal path (g + h ≤ 148 ≤ 149), so 148 is excluded by the run itself, and the d147 attempt died at layer 29 (147 impossible). No enemy exists in the segment (the first spawn needs ScreenLeft ≥ 433 > any x reached). The reconstructed path (`--stride 24`: the run predates the 96-byte records) replays on the QuickNES core frame-identically over all 149 steps (`chain_s1.bin`). Frontier: peak ~40M (layer 105), 14.4M at 120, endgame collapse to 1.7M uniques at layer 130 — the y-term prunes exactly where designed. | V | `runs/P2.3c-2c/top_s1_x339_y112_d149.log`, `top_s1_x339_y112_d147.log`, `s1_path_reconstruct.log`; `tools/model_difftest.py --case W42Main --first 6584 --enemies 0 --wr-file runs/P2.3c-2c/chain_s1.bin --wr --root-record 6584 --len 149 --n 0`; `docs/experiments/P2.3c-2c-main-area.md` §"S1 solved" |
| F104 | **The y-coupled position-goal bound admits stomp bounces soundly (enemy-aware searches).** A stomp gives `y_spd = −4 px | fraction` with the fall gravity kept (no A boost; `ChkForPlayerInjury` 11360–11375 requires post-BG-collision `Player_Y_Speed > 0` for ids < $07 — a landing-frame contact injures). Relaxation: a bounce-band pseudo-surface at Y 102 (the highest any stompable 4-2 enemy sits under Mario), x-gated on the group-spawn condition (x ≥ 433), landable at any descending frame with Y ≥ 102, wait allowed, take-offs = −4 arcs per fall gravity; only under `--enemies`. Audited: ~1.66M admissibility checks over random and stomp/kick-dense trajectories (six goals, three region prefix sets), **0 violations**; the enemy-free S1 bound is byte-identical with the band off. Two bugs found by the audit and fixed: the in-band landing rule, and a latent first-frame off-by-one in the y-only T tables (would bite any goal within 4 px below a surface top). | V | `tools/ygate_audit.py --enemies 0 [--prefix-dir …]`; `runs/P2.5c/audit_*.log`; `docs/experiments/P2.5c-w42-enemies.md` §step 4 |
| F105 | **Second host `mac`** (P0.11): Apple M3 Pro (6 P-cores + 6 E-cores = 12), 18 GiB RAM, 539 GB free, macOS/darwin 25.5.0, Docker Desktop 27.5.1. Reachable from the Fedora box as `ssh mac` (mDNS, key auth, per `~/.claude/CLAUDE.md`). Free disk is 3.3x the Fedora box's 163 GB, which matters against F98's ~190 GB full-level layer sizing. | V | `docs/experiments/P0.11-two-box.md` §1 |
| F106 | **Per-thread throughput proxy, Mac vs Fedora** (200M LCG steps + two random accesses into a 64 MB table, `cc -O2`, single thread): Fedora i5-1335U native 3.78 s; Mac native arm64 1.34 s (**~2.8x**); Mac x86_64 under Rosetta 3.69 s (~1.0x). **PROXY, not the engine** — one sample each, different compilers, the Fedora run was `nice`d and shared the box, P/E core assignment unpinned; M3 Pro is 6P+6E so aggregate is ~8.4 P-equivalents, not 12. Superseded when the P0.11a control run lands. | V | `docs/experiments/P0.11-two-box.md` §1 |
| F107 | **The Mac cannot run `smb-opt` natively.** The pinned `nightly-2018-06-01` has no `aarch64-apple-darwin` build (HTTP 404; `x86_64-apple-darwin` is 200). The x86_64 toolchain runs under Rosetta 2 and *compiles*, but Apple's current linker (`ld-1267`, built 2026-06-08) rejects the 2018 rlib layout: `archive member 'rust.metadata.bin' ... is not mach-o or llvm bitcode file`. Refuted workarounds: `-C link-arg=-arch x86_64` (fixes only a separate rustc-x86_64-vs-cc-arm64 mismatch), `-Wl,-ld_classic` (still present, still rejects), `-C prefer-dynamic`, and running the build under `arch -x86_64`. ⇒ a Linux environment is required until smb-opt is ported off the 2018 nightly (P0.11c). | V | `docs/experiments/P0.11-two-box.md` §2 |
| F108 | **`nightly-2018-06-01-aarch64-unknown-linux-gnu` exists** (HTTP 200) and Linux's linker accepts the 2018 rlib layout (the Fedora box builds it daily), so an arm64 Linux container builds `smb-opt` natively on the Mac with no Rosetta penalty. A container also supplies `docker run --memory=N --memory-swap=N` — a real cgroup cap, the Mac's substitute for `systemd-run --scope -p MemoryMax=`, which macOS has no equivalent for. | V | `docs/experiments/P0.11-two-box.md` §3; `tools/Dockerfile.smbopt` |
| F109 | **Docker on the Mac can run and load images but cannot pull or build them** (blocks P0.11). `docker run` on a local image and `docker load` from a tarball both work, and container networking is fine (raw IPv4 and DNS-resolved TCP both connect from inside a container). But `docker pull` hangs against Docker Hub *and* the ECR mirror, before and after a Docker Desktop restart — in-VM DNS returns **IPv6-only** addresses for Hub — and `docker build` hangs in all four variants tried (BuildKit default at "load metadata"; BuildKit `--pull=false` at `resolve <digest>`; legacy builder; legacy builder with an *empty* context, ruling out context transfer). **Worked around, not fixed** (2026-08-23): the image is built on the Fedora box with `podman build --platform linux/arm64` (needs `qemu-user-static`, ~25 min emulated), then `podman save` → `scp` → `docker load` → `docker tag` on the Mac, which uses only the two operations that do work. Only a *Dockerfile* change requires redoing this; engine changes do not (F111). | V | `docs/experiments/P0.11-two-box.md` §4 |
| F110 | **LAN between the two hosts is 96.9 MB/s** (629,145,600 B in 6.49 s over `ssh` + `dd`; gigabit, saturated). At F98's ~190 GB per full-level layer that is ~33 min to move one layer, so splitting a single BFS across the two machines is dominated by renting the F97 auction EPYC. Cross-machine parallelism stays at the granularity of whole independent jobs. | V | `docs/experiments/P0.11-two-box.md` §5 |
| F111 | **The Mac is an operational overflow host** (P0.11a, 2026-08-23). `smb-opt` builds **natively arm64** in the container in 1m 59s (`nightly-2018-06-01-aarch64-unknown-linux-gnu`, image 1.59 GB), and the 4-2 WR-suffix control gate `bfscx W42Main data/wr/wr_inputs.bin 6584 575 587 --lift 0 --check-path 12` reproduces `runs/P2.3c/ctrl_w42main_p575_d587.log` **exactly** — layers 6, 16, 34, 70, 134, 673, 3472, 16472, 69489, 257001 — on repo commit `eae7ec2`. **Staleness is the standing hazard and is mechanised:** `third_party/smb-opt` is untracked and reaches the project only through `tools/smb-opt-modes.patch`, so any pull touching that patch invalidates the Mac's binary; `tools/mac_sync_engine.sh` resets/reapplies/rebuilds and stamps `third_party/smb-opt/.built-from`, and `tools/mac_run.sh` refuses to run the engine (exit 3) when the patch sha256 does not match the stamp. Expand throughput is still **unmeasured** — the control is too small to time, so F106's proxy stands until a `--resume` benchmark runs. | V | `docs/experiments/P0.11-two-box.md` §4–§5 |
| F112 | **4-2 main-area segment S2 — the enemy-aware optimum from the S1 arrival (record 6733 of `chain_s1.bin`) to the first x ≥ 755 ∧ Y ≤ 112 is exactly 166 steps, equal to the y-coupled movement bound: the goomba group costs 0 frames on the top route.** Proof: the deadline-166 `bfscx --enemies 0 --goal-x 755 --goal-y 112` run (bounce band armed) reached the goal at layer 166 (70,609,064 goal transitions) and at no earlier layer; the deadline equals the root bound, so a state on a ≤ 165-step goal path has g + h ≤ 165 ≤ 166 and cannot be pruned — 166 is the optimum with no lower rung needed. The path takes exactly one stomp (S2 step 139, chain step 288, x ≈ 690, in stride mid-jump) and replays on the QuickNES core 315/315 frames identically (`chain_s2.bin`, 6,899 records). Root → x ≥ 755 ∧ Y ≤ 112 = 149 + 166 = **315**. Found on the ladder's first rung (peak frontier 5.54M, 1,996 s, 8 threads). | V | `runs/P2.3c-2c/top_s2_x755_y112_en_d166.log`, `top_s2_probe_d150.log`, `s2_path_reconstruct.log`, `trace_s2_events.log`; `tools/model_difftest.py --case W42Main --first 6584 --enemies 0 --wr-file runs/P2.3c-2c/chain_s2.bin --wr --root-record 6584 --len 315 --n 0`; `docs/experiments/P2.3c-2c-main-area.md` §"S2 solved" |
| F113 | **4-2 main-area segment S2′ — the enemy-aware optimum from the S1 arrival to the first x ≥ 800 ∧ Y ≤ 112 is exactly 184 steps, equal to the y-coupled movement bound; the chain-safe boundary (x 800 = at/over the col-50 block tops) costs nothing over the x-755 segment pair.** Proof as F112 (deadline = root bound; goal at layer 184, 77,448,522 transitions, no earlier layer; first rung). The path takes two stomps (chain steps 280/296, x ≈ 671/711, in stride) and arrives as a max-speed rising arc (Y ≈ 38 at x 800) that lands grounded at x ≈ 865 on the (54,7)–(56,7) blocks, 47 px before the lift-pit lip. `chain_s2p.bin` (6,917 records) replays on the QuickNES core 333/333 frames identically. **Root → x ≥ 800 ∧ Y ≤ 112 = 149 + 184 = 333; the goomba group still costs 0 frames.** The S3-from-x-755 dead end (wavefront frozen at x ≈ 790, d120 blowup) is what motivated the boundary move — a first-arrival goal chained at an airborne boundary can select an arc that cannot survive the next segment. | V | `runs/P2.3c-2c/top_s2p_x800_y112_d184.log`, `top_s2p_probe_d160.log`, `s2p_path_reconstruct.log`, `trace_s2p_events.log`, `top_s3_x1005_d1*.log`; `tools/model_difftest.py --case W42Main --first 6584 --enemies 0 --wr-file runs/P2.3c-2c/chain_s2p.bin --wr --root-record 6584 --len 333 --n 0`; `docs/experiments/P2.3c-2c-main-area.md` §"S2′ solved" |
| F114 | **4-2 main-area segment S3′ — the enemy-aware optimum from the S2′ arrival to the first x ≥ 1005 ∧ Y ≤ 112 (over the lift pit) is exactly 82 steps, equal to the y-coupled movement bound: the pit crossing is free** (no lift ride — a full-power 23-frame-held jump from the lip; the arc passes under the vine ledge and lands on open floor past the pit). Proof as F112/F113 (deadline = root bound 82; goal at layer 82, 45,492,832 transitions, first rung; peak frontier 4.96M, 887.7 s at 8 threads). Goal-parent prefix recovery: exactly 1 match in `layer_081.bin` (the koopa is live in the ext). `chain_s3p.bin` (6,999 records) replays on the QuickNES core 415/415 frames identically, still with exactly 2 stomps. **Chain: root → x ≥ 1005 ∧ Y ≤ 112 = 149 + 184 + 82 = 415 — three segments, each exactly at the movement bound; no enemy or obstacle has cost a frame yet. The S4 x-only root bound from this arrival is 137 (chain floor 552, below the 554 double-framerule line); S4's ladder measures the plant/climb costs.** | V | `runs/P2.3c-2c/top_s3p_x1005_y112_d82.log`, `top_s3p_probe_d70.log`, `s3p_path_reconstruct.log`, `trace_s3p_events.log`, `top_s4_probe_d100.log`; `tools/model_difftest.py --case W42Main --first 6584 --enemies 0 --wr-file runs/P2.3c-2c/chain_s3p.bin --wr --root-record 6584 --len 415 --n 0`; `docs/experiments/P2.3c-2c-main-area.md` §"S3′ solved" |
| F115 | **4-2 main-area segment S4a-i — the enemy-aware optimum from the S3′ arrival to the first x ≥ 1155 ∧ Y ≤ 128 (at/over pipe A's cap) is exactly 60 steps, equal to the movement bound: pipe A and its piranha plant cost 0 frames** (the optimal line jumps the whole pipe; the plant's rising phase executed 1,448,306 branches at layer 58 — the first nonzero death count of the chain — all off-optimum). Proof as F112–F114 (deadline = root bound; goal at layer 60, 2.0×10⁹ goal transitions, first attempt; peak frontier ~130M under a 200M watchdog, 3,957 s, Mac container, build stamped e2089ea). Two goal-pick hazards found: the best-by-(x speed, x) arrival was an above-the-ceiling sky arc (the ceiling top is standable and unreachable-from for the wrong-warp pipe — cols 67–97 solid), and "below screen top" still admits ceiling-top standers — the chain parent must be picked from the last layer restricted to the tunnel band (54,104 candidates; max-speed riser x 1153.1, Y ≈ 110 chosen). `chain_s4ai.bin` (7,059 records) replays 475/475 frames identically on the QuickNES core, still exactly 2 stomps. **Chain: root → pipe A = 149 + 184 + 82 + 60 = 475.** | V | Mac `runs/P2.3c-2c/top_s4ai_x1155_y128_mac_d60.log` (+ local copies of path/logs), `s4ai_path_reconstruct.log`, `pick_parent_59.py`, `trace_s4ai_events.log`; `tools/model_difftest.py --case W42Main --first 6584 --enemies 0 --wr-file runs/P2.3c-2c/chain_s4ai.bin --wr --root-record 6584 --len 475 --n 0`; `docs/experiments/P2.3c-2c-main-area.md` §"S4a-i solved" |
| F116 | **H28 refuted — 1-1 third room, whole-room exact search from the fixed entry (run 4, P2.1b-m3): no flagpole-glitch grab exists at step ≤ 238 (frame ≤ 1283) in the relaxed model + goombas, which over-approximates the real game — so the framerule cannot be saved from the third room; 1-1 is closed.** Record: `bfs11cr` from record 1048 (frame 1045), deadline 238, all 16 inputs, goomba model F56, x/y bounds; layers 0–233 logged `grabs 0` (`room_compact_d238.log` + `room_compact_d238_r85.log`, 148/149 layer lines at 0), the first grabs at layer 234 (33,281,312 transitions, ALL non-FPG at Y 160–161 = F55's slow band; recovered table `room_compact_d238_r233c.log`); layers 235–238 searched by resuming the 216,599,395 layer-234 records in 8 disjoint slices (`room_layers_p0..7`, 27,074,925×7 + 27,074,920; `fpg_part0..7.log`) — parents disjoint ⇒ the union of grab tables is complete for grab existence (cross-slice dedup loss only repeats expansions) — every slice's earliest grab is non-FPG (steps 235–238) and no `true` row exists. Frontier: peak ~693M at layers 200–203; 660.6M at the stairs-top step 183 all within a 24-px window (the seam census, P2.1b file). The whole-file continuation past layer 234 OOM-died 4× (grab-bookkeeping memory; the slicing is the code-free workaround). | V | `runs/P2.1b-model/room_compact_d238*.log`, `fpg_part*.log`, `fpg_driver.log`; `docs/experiments/P2.1b-pole-search.md` §"Run 4 verdict" |
| F117 | **The 4-2 chain's vine-ledge landing is forced, and the WR's pit-to-pipe-B crossing runs at the movement bound.** (1) From the S3′ arrival (`chain_s3p.bin`, step 415: x 1005.56, Y 45.28, just past the apex of a 23-frame-A lift jump), every continuation lands on the vine brick (64,3) at step 418 (x 1013.06, Y 48.50): at step 417 (x 1010.56, Y 47.53) the next frame is x ≥ 1012.9 under any input, which puts the right foot (x+12) in col 64 inside the landing window [48, 53). From the ledge, running off its end (x 1071) or hopping off it arrives at pipe A's face (x 1139–1140) below the cap (Y ≥ 148 / 160) — the 465 chain slams the face at step 469 (speed 40 → 0.44, bound 59 → 71) and the floor-clip ladder d62–d82 (Mac, 36 s) finds no path at any deadline. The 475 chain lands on the cap's left edge (x 1143, step 470) via a ledge hop, so the cap is reachable from the ledge, but not with the pipe-B-top jump. (2) The WR (model trace = dump, F81) passes **under** the ledge: lift jump at step 439 (x 987, Y 128, 10 frames of A), apex Y 77.8 at x 1024, Y 78–144 across x 1024–1071 (side points in row ≥ 4 ⇔ Y ≥ 72; the ledge is row 3), floor landing x 1092 (step 481), jump 483 → pipe A cap x 1177 (515), jump 516 → pipe B top x 1239 Y 64.5 (540), jump 542 → x 1284 at 558: from x 1005 (446) to x 1284 (558) is 112 frames = 279 px / 2.5 px — every frame at the x bound. Our chain reaches x 1005 at step 415, 31 frames before the WR (F93's wall entry), but 30 px too high. | V | `smb-opt traceh W42Main 6584 465 --enemies 0 --goal-x 1284 --goal-y 176 runs/P2.3c-2c/chain_s4aip.bin` (and `chain_s4ai.bin` to 475); `smb-opt tracec W42Main data/wr/wr_inputs.bin 6584 600 --enemies 0`; `runs/P2.3c-2c/top_s4floor_ladder.log` + rung logs; `P2.3c-2c-main-area.md` §"Session 12" |
| F118 | **4-2 main area in 553 frames — two framerules under the WR — model + core verified.** The vine-bumped top-route chain (`runs/P2.3c-2c/chain_s4v4.bin`, 7,137 records = the WR's 0–6583 + 553 new) enters the wrong-warp pipe at record 7136 = main-area step 553, vs the WR's 588 and HappyLee's top-route claim 577; 553 ≤ 554 = the double-framerule line, and the x-only chain floor from the same root is 552 (≥ 1 more frame is provably unobtainable by x physics alone). Every segment is exactly at its y-coupled movement bound (149+184+82+34+35+25+44), enemies cost 2 stomps and 0 frames, the (64,3) vine bump costs 0 frames and suppresses the wrong-warp plant (F100/H34). QuickNES core replay: 553/553 frames, 0 mismatches, GES 3 at frame 7134 with Down on the last record (F74). **Not yet a recorded record: BizHawk/FCEUX verification + movie assembly pending (the evidence standard).** | V (model + QuickNES) | `tools/replay_check.py --case W42Main --first 6584 --prefix 0 --path runs/P2.3c-2c/s4v4_seg553.bin --enemies 0 --down`; `P2.3c-2c-main-area.md` §"THE 553" |
| F119 | **The (64,3) vine-bump geometry: the legal band is Y ∈ [72, 78) while rising.** At Y ∈ [56, 72) the right side point (x+13) is in row 3, so entering x ≥ 1011 face-impedes (speed → 0) five pixels before the head point (x+8) can reach col 64 (x ≥ 1016); at Y ≥ 78 the head never contacts. Corollaries verified on the core: re-pressing A mid-air does not restore the jump's low gravity (the arc's A-hold must be continuous; v_force 0x28 in the record — which the index encoder aliases to V_FORCE_AREA_INIT's index 1); a grounded R-only step with RunningTimer 0 clamps run speed toward the walk cap in one frame. HappyLee's apex graze at Y 77.9 (F93 region) is the unique zero-cost entry — at the movement bound the bump is not merely free but forced (all 408 G1v bound arrivals carry vf 2). | V | `traceh` side-slam trace + census v1–v4 in `P2.3c-2c-main-area.md` §Session 12; `runs/P2.3c-2c/h34_riser_record.txt` |
| F120 | **The scroll-offset latch, and the wrong warp's hidden requirement.** (1) SMB1's screen scrolls by the player's movement-dx once his screen-relative x reaches 112, tracks it exactly (no catch-up), and never scrolls left: the relative offset is a latch that persists until changed. (2) Collision-push displacement (`ImpedePlayerMove` in the moving direction, the F93 foot-check drift) moves the player WITHOUT feeding the scroll: it is the only known offset-minting mechanism. The WR mints +21 px (relative 111 → 132) during its col-30 wall entry, rows 6786–6819, with ScreenLeft frozen at 357; our top-route run stays at 112 from x 160 to the end (dump scan of every offset change, both FCEUX dumps). (3) The 4-2 wrong warp therefore requires ~20 px of minted offset: entry at x ∈ [1348, 1356] with the (page-5, col-15) area-change command unparsed needs ScreenLeft ≤ 1216 ⇔ relative ≥ 132 (F40; parse threshold: SL ≥ 1217 puts the command inside ScreenRight+48 masked to 16). Slack cannot mint offset (hang-back stops the scroll too; the constrained rungs d44–49 died exactly at max x 1328.94 = relative 112). **HappyLee's wall walk is passage + vine + warp key at once.** | V | the dump scan in `P2.3c-2c-main-area.md` §"The scroll-offset discovery"; `runs/P2.3c-2c/s4w4_entry_d4*.log`; F40/F93 |
| F121 | **The x-only case bound is why the drift rungs blew up, and the scroll-aware drift bound is sound.** The two dedicated wrong-warp rungs (local d58, Mac d62) were watchdog-killed at layer 37 (frontier 126M / 249M, `max x 0x530f0` = 1328.94 = the F40 refusal wall, `goals 0`, growth ×1.5–1.6/layer) — memory blowups, **not** verdicts: `W42Main`'s `--enemies` case bound was the x-only table to X 1348, blind to the F120 scroll requirement, so every wall-pinned state looked ~2 frames from the goal and survived. `heuristics::drift::DriftBound` (a relaxed BFS over X∈[1180,1360]×Player_Y×offset m∈[−40,24]×side-collision credit c∈[0,16], transitions = the exact scroll regimes + collision pushes at BB42 sites, distance from X≥1348 within the `X ≤ 1328+m` wall) is an admissible lower bound: control gate byte-identical with drift off (`6 16 34 70 134 673 3472 16472 69489 257001`); the WR's real warp survives `--check-path` with 0 violations and reaches the goal (drift ON), while 32,472 wrong-scroll pipe entries are now pruned (`goal_refused`); `tools/drift_audit.py` — 400 trajectories + WR, 211,675 consistency pairs `h(s)≤h(s′)+1`, **0 violations**, minting exercised to rel 132, `h` up to 67 in the wall band (vs the x-only ~2). Case bound now `max(x-table, drift)`, ON by default under `--enemies`, `--no-drift` to disable. | V | `docs/experiments/P2.3c-2c-main-area.md` §P2.3c-5; `third_party/smb-opt/src/heuristics/drift.rs`; `tools/drift_audit.py`; `runs/P2.3c-2c/s4w4_d58_drift.log` |
| F122 | **The 4-2 top route cannot mint the scroll offset the wrong warp requires — strong evidence the top-route warp is infeasible (not a 2-frame budget miss).** The warp needs offset (x − ScreenLeft) ≥ 132 at entry (WR: exactly 132; parse threshold asm-exact at SL ≥ 1217, `CheckRightBounds`); the top route reaches the pipe at offset **112** (F118's 553). The only minting mechanism is collision-push (`impede_player_move`: moving-dir LEFT + speed 0 + a foot/side point in a solid cell → the col-30 **floor-level** wall walk, F93/F120). Across **five** searches — `bfscx --beam` h-first & offset-first from 509 (d72) and 484 (d100), and two no-beam exhaustive probes (drift from step 530 d35; position-goal 484→x1284 d55, 23 layers exhaustive over x 1177→1234) — the offset **never leaves 112**. Mechanistic reason: the top route's walls are top surfaces (bricks) or jumped-over (pipe B), never a floor-level face in a mint pose, and **F117** shows it cannot reach floor level past pipe A. The drift bound (admissible → assumes minting available) hid this: it read wall-pinned states as ~20 frames from the goal, so P2.3c-5's d58 looked like a tight-deadline miss rather than structural infeasibility. **SUPERSEDED IN ITS CENTRAL CLAIM BY F127 (2026-08-24 session 16): the top route DOES mint — 112 → 126, core-verified. The "offset frozen at 112 / mints nothing / structurally infeasible" framing is withdrawn; what survives is a 6 px shortfall (126 vs the 132 the warp needs), not a 20 px impossibility. Two defects: the beams were single-key (H39/F128) and the two 509-root rows below were never actually measured (`grep -c offset` = 0).** Resolves the P2.3c-5 fork (no cloud needed). NOT formal proof (beams lossy; the exhaustive sweep cap-killed at layer 23; the side-collision-freeze mint argued away, not exhaustively refuted). New tools `bfscx --beam N [--beam-offset] [--log-offset]` (opt-in; **optimal mode byte-identical with beam off**). | V (search records + code argument) | `runs/P2.3c-2c/finale_beam2m*.log`, `finale_beam484.log`, `mint_probe*.log`; `third_party/smb-opt/src/{main.rs,emu.rs}`; `docs/experiments/P2.3c-2c-main-area.md` §P2.3c-6 |
| F123 | **4-2 mint economics — the wrong-warp key costs ~30 frames of slack on any route, so the bottom route cannot reach 575 either (floor ≈ 584–585, deficit ~9–10).** New tool `bfscx --goal-offset N` (goal = scroll offset ≥ N; sound (N−rel+1)/2 bound). From the WR's col-30 arrival (prefix 200): **min 132-offset mint = 27 frames** (85,272 transitions, beamed; exhaustive to layer 18 identical frontier), rate ~0.65 px/f — but every minted state has ~0 speed (mint mechanisms: collision-push +1 px at ~0 speed; **sct-frozen running** — the side-collision timer freezes the scroll 15 frames per impede and every foot impede refreshes it, so the WR mints by sprinting at L+R-doubled accel inside the frozen window, trace steps 219–233 — arming always costs an impede = speed zeroed; no speed-preserving mint exists in the mechanics). Continuation probe from the chained 27-frame mint (`chain_mint27.bin`, 227-root): case bound **h = 357 → total ≥ 584 > 575**; the max-offset mint saves ≤ ~4 end-to-end vs the 13 needed. With F122 (top route mints nothing), **both 4-2 routes are closed for a framerule**: 588 = 553 movement + ~31 key tax + ~4 slack, conserved. Strong evidence + mechanism, NOT formal proof (minted-state census incomplete → H38). Ops lesson: enemy-aware bottom-route searches must root ≥ step 200 — the H34 item-bump refusal walls col-28 (Mac run died there at layer 5). | V (search records + trace + code argument) | `runs/P2.3c-2c/mint_cost_beam.log`, `mint27_path.bin`, `chain_mint27.bin`, the 227-root probe print; `tracec` WR steps 215–233; Mac `runs/P2.3c-2c/mac_bottombeam/`; `docs/experiments/P2.3c-2c-main-area.md` §P2.3c-6b |
| F124 | **1-1 room 1 is optimal at 368 frames with the enemies in the model — H29 refuted, 1-1 closed end to end.** The enemy-aware exhaustive rung `bfscx W11Room1E … 196 0 367 --enemies 0` is **dry in 4.7 s**: root bound exactly 367, frontier 1 → 36 states over layers 1–20, then all 576 children pruned (`no live states left after layer 21`, `no goal found within 367 steps`). The pruning is sound because the bound is admissible under `--enemies`: (a) pipe entry requires the **right** foot on `cv 0x11` (emu.rs:485; `BLOCK_BUFFER_X_ADDER_DATA[0x10] = 0x0c00` ⇒ `x_pos ≥ 0x39400`) and the case bound is `get_steps_until_x_pos_at_least(s, 0x39400)` — the entry threshold *exactly*, never more; (b) the x table is a validated lower bound (F68, 1.9M checks / 0 violations); (c) **`src/w11enemies.rs` never references `x_pos` or `x_spd`** — its only player write is line 315 `s.y_spd = -4<<8` (the stomp bounce), so no enemy interaction can beat the x machine; (d) model = game on this room (F-ledger P2.5b port: WR 368/368 0 diffs, 500 trials / 82,234 frames / 0 diffs / 53 deaths / 169 stomps). Positive control (it runs *before* the search, main.rs:938–950): the WR's own line replays enemy-aware to `StateChangeVerticalPipe(57,7)` **GOAL at step 368** with **0 bound violations over 368 steps** (2 stomps at steps 126/320), and was in-frontier for layers 1–90 of the d368 rung. Forensics: `k + h` = 367 for steps 1–20 and **368 from step 21 onward** — the single unavoidable frame is spent on an airborne frame of the opening jump (x_spd frozen 0x18f0 across the landing), and the WR rides the bound exactly for all 347 remaining steps, entering at x 0x39410 = **0x10 past the threshold** (earliest admitting pixel). Exclusions: the (21,7) powerup bump is refused (mushroom outside the model; growth freeze ≫ 1 frame), pit/contact = death. The d368 *exhaustive* rung was abandoned as infeasible, not informative: 94 GB of layers at layer 90/368, frontier 61.3M growing ×1.10/layer ⇒ ≈1.6 TB and ≥12 h (zero slack gives 36 states; one frame of slack gives tens of millions, because `h` is a coarse x-only bound). | V (search record + code argument) | `runs/P2.5b/w11_d367.log`, `runs/P2.5b/w11_d368.log` (reference-path audit); `third_party/smb-opt/src/{main.rs,emu.rs,w11enemies.rs,case/w11.rs}`; `docs/experiments/P2.5b-room1-search.md` §"The verdict" |
| F125 | **8-4 room 3: the return leg is optimal at 33 frames, so H25's frame is not in the turnaround — and the room must be searched in chained segments.** Cutting the 195-frame room at the WR's apex (max x 3457, speed crossing 0; record 16516 = prefix 162) gives a 33-frame tail: `bfscx W84Room3 … 16354 162 33 --check-path 33` → `at root: Some(33)`, **0 bound violations / 33 steps, 96 goals at layer 33** (7.7 s); `… 162 32` → **dry at layer 1** (`no goal found within 32 steps`). h(apex) = 33 = the WR's actual ⇒ zero slack on the return. The **whole-room** d194 rung is infeasible: root bound 183 vs deadline 194 = slack 11 (F98 frontier law), and rebuilding the coupled overshoot bound at **horizon 200 instead of 96 leaves the root at 183 exactly** — so the 12-frame gap is terrain + the y-coupling of pipe entry, not horizon truncation (F95 confirmed for 8-4). Two engine/ops defects found: (a) `W84Room3`'s bound returned `Some(0)` for every state with SL ≥ 3345 and `x ≤ PIPE_XMAX` — admissible but never pruning (`pruned 0` on every layer of every room-3 rung); fixed by deriving the entry window's left rim from the same foot geometry as F124 (left foot `cv 0x10` col 212 ∧ right foot `cv 0x11` col 213 ⇒ x ∈ [0xd4400, 0xd4d00)) and adding `W84R3_PIPE_XMIN = 0xd4400` with a leftward-overshoot branch; (b) **`bfscx`'s `max_steps` counts layers from the POST-prefix root, not absolute frames** — a prefix-162 rung given deadline 195 has 162 frames of slack, not 33 (it reached 100M states / 27 GB at layer 23 before being killed); the 4-2 control gate `6584 575 587` stops at layer 10 because it finds goals, not because 587 bounds it. | V (search records + code) | `runs/P2.2a/w84r3_apex_d33.log`, `w84r3_apex_d32.log`, `w84r3_d194.log`; `third_party/smb-opt/src/case/w84.rs`; `docs/experiments/P2.2a-84-turnaround.md` §"Session 15" |
| F126 | **The 8-4 flying-cheep-cheep frenzy is state-coupled, not frame-indexed — the `w84enemies` port is materially bigger than budgeted.** `InitFlyingCheepCheep` (`data/disasm/smbdis.asm:8419`) reads **`Player_X_Speed`** (a `cpy #$19` branch doubles a seed term when Mario is moving right at speed) and **`Player_X_Position`/`Player_PageLoc`**: the cheep spawns at `Player_X ± FlyCCXPositionData[y]`, takes `FlyCCXSpeedData[y]` for horizontal speed, and can flip direction on the second LFSR's d1; `FrenzyEnemyTimer` gates the whole routine and is reloaded from `FlyCCTimerData[LFSR+1 & 3]`. So while the LFSR itself is a pure frame function on a lag-free route, **the spawn law is a function of (frame, Mario's x, Mario's speed)** — two search trajectories differing in position or speed see different cheep fields, and the enemy ext multiplies the state space instead of riding along as a frame index. `MoveFlyingCheepCheep` (asm:9922) adds a `PRandomSubtracter`-indexed vertical arc. This corrects the P2.2a checkpoint's assumption ("id $14, LFSR-driven = frame-indexed on the lag-free route"), which held for the 1-1/4-2 goombas but not here. | V (code reading) | `data/disasm/smbdis.asm` §InitFlyingCheepCheep/MoveFlyingCheepCheep; `docs/experiments/P2.2a-84-turnaround.md` §"Session 15" #5 |
| F127 | **The 4-2 TOP route DOES mint scroll offset — F122's central claim is refuted, core-verified: 112 → 126 (+14 px of the +20 the wrong warp needs).** F122 concluded "the offset never leaves 112 across five searches… **no reachable input sequence mints any offset on the top route**" and built the structural-infeasibility verdict on it. Re-running its own probe (`chain_s4v3.bin`, prefix **509**, deadline 72, `--enemies 0`) with the **diversity beam** (P2.3c-8: best 500 per bucket over scroll-offset × y-band × x-speed × subpixel, instead of best 2M by a single global key) carries the offset **112 → 126** over layers 29→46 at ≈0.82 px/frame — *faster* than the bottom route's 0.65 px/f (F123) — dying only when deadline 72 ran out. A d90 rerun reproduces the same climb and the same **ceiling of 126** through layer 67. **Verified on the real core, not just the model:** the 46-step path reconstructed (`mint126_path.bin`, 47 inputs) and replayed on QuickNES — `replay_check` 47/47 frames, **0 mismatches**, and the core's own `ScreenLeft_PageLoc/ScreenLeft_X_Pos` read **offset 112 at frame 7091 → 126 at frame 7136**, with ScreenLeft pinned at **1165** while Mario advances 1274 → 1289 (the sct-frozen-running mint, F123 mechanism (b) — the very mechanism F122 said "cannot fire either" on the top route). **Two independent defects in F122's evidence:** (1) the mint maneuver is **19 frames of held Left** — motion *away* from the goal, so a lowest-h beam deletes it on its first frame (H39); (2) the two 509-root rows in F122's table (`finale_beam2m.log`, `finale_beam2m_off.log`) contain **no offset measurement at all** (`grep -c offset` = 0 — the `--log-offset` print postdates them); those "offset 112" entries were inferred from max-x, and this is the first actual measurement from the 509 root. **What stands:** the warp needs 132 and the searches so far top out at 126, so the top-route warp is **6 px short, not 20 px impossible**; F122's *conclusion* may yet survive, but its stated reason is wrong and its "structurally infeasible" framing is withdrawn. Note also: `replay_check`/`model_difftest` compare player fields only (XP, XSUB, XSPD, XFRAC, YP, YSPD, YFRAC, STATE) — **ScreenLeft is not in the difftest**, which is why the scroll was measured directly from the core RAM dump here. | V (search record + core replay + core RAM) | `runs/P2.3c-8/{f122_retest.log,mint_d90.log,mint126_path.bin,mint126_path.log,chain_mint126.bin}`; `tools/replay_check.py`; `smb-opt offset-census W42Main runs/P2.3c-8/f122_retest_layers/layer_046.bin`; `docs/experiments/P2.3c-8-beam-diversity.md` |
| F128 | **A single-key beam is a per-layer first-arrival gate — the search method itself was losing solutions (H39 confirmed, with F127 as the casualty).** `bfscx --beam N` (lowest-h) and `--beam-offset` (highest-offset, then h) are both *global single-key* orders, so any maneuver that must **pay before it gains** — slow down, back off in x, drop height — has worsening key on exactly the layers it must survive and is deleted before it can pay. That is the same lossy operation as a first-arrival segment seam, applied every frame rather than every seam: a beam does not escape segment-greedy search, it *is* segment-greedy search at 1-frame granularity. Demonstrated: F127's mint requires **19 consecutive frames of held Left**, which h-first prunes on frame 1; under the same root/deadline/budget a bucketed beam finds it and a global one does not. **Fix shipped:** `bfscx --beam N --beam-buckets off,y,spd,sub[,vf] [--beam-max M]` — best N *per physical bucket* (scroll offset, 16 px y band, x-speed band, x subpixel phase (F11/F37), v_force); `--beam-max` shrinks the per-bucket width first and only drops whole buckets as a last resort, printing `WARNING capped` when it does. Also `smb-opt offset-census CASE LAYER_FILE [--top K] [--min N]` — `tools/pick_parent.py` cannot decode `left_screen_edge_pos`, so the scroll offset (the quantity F122/F123 turn on) was invisible to the seam census until now. **Regression: beam-off is byte-identical** — the `--lift 0` control gate still gives 6, 16, 34, 70, 134, 673, 3472, 16472, 69489, 257001 (`runs/P2.3c-8/gate_beamoff.log`). Corollary for seams generally: the runbook's step-3 "reconstruct TWO candidates differing in vertical situation" is this same insight applied by hand at width 2; every first-arrival segment gate in the project carries the same risk. | V (A/B search record + regression gate) | `runs/P2.3c-8/{gate_beamoff.log,probe10.log,f122_retest.log}`; `third_party/smb-opt/src/main.rs` (`parse_beam_buckets`, `bucket_key`, `offset_census`); `docs/experiments/P2.3c-8-beam-diversity.md`; H39 |
| F129 | **The 4-2 wrong warp needs a SECOND condition nobody had modelled: the scroll must be stopped at pipe entry (`Player_X_Scroll` = 0), not merely offset ≥ 132 — and the sct-freeze mint cannot supply it.** F127's top-route path enters the pipe at record 7179 with **x 1348, ScreenLeft 1216, offset 132, AreaPointer $2F, EntrancePage 0** — byte-identical to the WR's entry condition (WR frame 7172) — and the core does enter (GES 3, `replay_check` 87/87, 0 mismatches). **It still warps wrong.** During the pipe descent the scroll keeps advancing at 2 px/frame: ScreenLeft 1216 → 1218 → 1220, crossing the 1217 parse threshold two frames in, so the page-5-col-15 command parses and AreaPointer flips **$2F → $42** with EntrancePage 0 → 8 (Mario re-enters at x 1396 on ScreenLeft page 4, not x 24 on page 0). Cause, from the RAM: at the WR's entry frame `Player_X_Scroll` = `ScrollAmount` = **0** and stay 0 for the whole descent (offset pinned at 132 for 28+ frames); at ours they are **2** and keep applying during GES 3 (offset decays 132 → 130 → 128 → … → 107). So the WR's offset is a **latch** (F120: collision-push, stable) while the sct-frozen-running mint that the top route uses is **transient** — the scroll catches up the moment the timer lapses. **Consequence: the case goal is under-constrained** (the same class of bug as F120). `goal_refused` in `main.rs` tests only `screen_left16 >= 1217` at the goal transition; it must ALSO require the entry frame to advance Mario's integer x by 0 (the model already computes this as `emu.rs:388` `x_scroll`). Until that is added, **every wrong-warp GOAL the engine reports is suspect**, including the 900 goals at layer 87 of `mint_d90.log`. **And it re-prices the top route**: entering stopped at x 1348 is what the WR pays for, so F123's "the mint is speed-priced" now applies to the top route as well — the open question is the cost, not the existence. | V (core RAM, two paths compared) | `runs/P2.3c-8/{warp87_path.bin,chain_warp87.bin,warp87_path.log}`; core dumps vs `data/wr/fceux_wr.ram` rows 7166–7195; `third_party/smb-opt/src/{main.rs (goal_refused), emu.rs:388,406-409}`; `docs/experiments/P2.3c-8-beam-diversity.md` §7 |
| F130 | **The 8-4 flying-cheep frenzy couples to the player far more weakly than F126 assumed: speed enters only as a 3-valued class, and position is an additive spawn base, not a branch — so the `w84enemies` port is much cheaper than budgeted.** Read at code level from `InitFlyingCheepCheep` (`data/disasm/smbdis.asm:8419-8500`). **(a) Speed → coarse classes only.** The seed path is `lda #$00 / ldy Player_X_Speed / beq GSeed` (A=0) `/ lda #$04 / cpy #$19 / bcc GSeed` (A=4) `/ asl` (A=8), then `adc $01` with `$01` = `PseudoRandomBitReg,x & 3` → `tay` → `lda FlyCCXSpeedData,y` (12 entries). So `Player_X_Speed` selects one of **three** rows — zero, 1..$18, or ≥$19 unsigned (which includes every leftward/negative speed byte) — not one of 81 values. It is read a second time at `lda Player_X_Speed / bne D2XPos1`, a **2-valued** (zero / non-zero) gate on the direction flip. **(b) Position is additive, not a branch.** The spawn is placed *relative to Mario*: `Enemy_X_Position = Player_X_Position ± FlyCCXPositionData[y]` with `Player_PageLoc` carried through (`D2XPos1`/`D2XPos2`). Absolute player X never selects a branch, so it does **not** need to be a bucket/key dimension — the spawn geometry is translation-invariant. **(c) What is left as real state:** `FrenzyEnemyTimer` (gates the whole routine; reloaded from `FlyCCTimerData[LFSR+1 & 3]`), the enemy-slot count test `cpx $00` (slot occupancy), and the LFSR itself — which on a lag-free route is a pure function of the frame. Note `$00` is **overwritten** with `PseudoRandomBitReg+2,x & $0f` whenever `PseudoRandomBitReg+1,x & 3 != 0`, so the *position* index is usually pure LFSR with no speed dependence at all. `SecondaryHardMode` (max 3 vs 4 onscreen) is off on this route. **Consequence:** F126's "NOT frame-indexed ⇒ materially bigger than budgeted" stands in direction but is quantitatively far too pessimistic — the frenzy is frame-indexed *up to a 3-valued speed class*, with player-relative spawns. Port cost ≈ 3× a frame-indexed class, not 2^k. | V (code-level, disassembly read in full) | `data/disasm/smbdis.asm:8405-8500` (`FlyCCXPositionData`, `FlyCCXSpeedData`, `FlyCCTimerData`, `InitFlyingCheepCheep`, `GSeed`, `RSeed`, `D2XPos1`, `D2XPos2`); refines F126 |
| F131 | **The F129 warp condition is real but CANNOT be expressed as a goal-side refusal — a `ScreenLeft + 48*d <= 1216` test rejects the WR's own warp. And the control that should have caught that was not applying the refusal at all.** Attempted in session 16 as the P2.3c-9 Part A fix. Measured basis (core, `runs/P2.3c-8/warp87d.ram`): `ChangeAreaTimer` = **48** at the entry frame and the destination is read at 0; `Player_X_Scroll` is **latched on the GES-3 entry frame and never recomputed during the descent**, so the screen scrolls for ~43 of those 48 frames (ScreenLeft 1216 → 1269, +53 px, stopping only at the level's right edge); the instant ScreenLeft reaches 1217 the page-5-col-15 command parses, AreaPointer $2F → $42, and it never flips back (final destination $41, Mario at x 2072 / ScreenLeft page 8 — the F40 signature). The WR survives because its entry frame advances integer x by **0**. **Why the rule fails:** that delta belongs to a frame the search does not simulate. The model's goal fires on the frame Mario *reaches* the entry x — the WR's own goal step advances integer x 1346 → 1348, so **d = 2** — while the core's GES-3 frame is a *later* frame on which x does not advance. Implementing `ScreenLeft + 48*d <= 1216` therefore refused the WR's genuine warp; verified directly (the reference-path audit's ` GOAL` marker disappeared, and returned on revert). Reverted to the sound necessary condition `screen_left16 >= 1217`. **Second, worse finding:** the `--check-path` reference-path audit — the project's standard positive control (F124) — computed its goal flag as `xy_goal(...)` **without** `goal_refused` (`main.rs:1023`). So any goal refusal could have silently rejected the WR and no control would have shown it. **Fixed**: the audit now applies the same refusal the search does, which is what caught this. **Consequence:** warp validity is adjudicated **downstream** (core replay + the destination check, runbook §4.3), not at the goal, until the case models the entry frame under Down (that is the real P2.3c-9 Part A, and it is bigger than an S). A corollary for the user's idea of riding a decaying-but-high offset: it is not over-constrained away, but it prices out — surviving 48 frames of latched scroll needs offset ≥ 132 + 48·d, i.e. ≥ 180 at d = 1 and ≥ 228 at d = 2, against a 256 px screen and a 0.65–0.82 px/frame mint rate (F123/F127). | V (core RAM + a control that now tests the refusal) | `runs/P2.3c-8/warp87d.ram`; `third_party/smb-opt/src/main.rs` (`goal_refused`, check-path `g` at ~1023); `docs/experiments/P2.3c-8-beam-diversity.md` |
| F132 | **Dissolving all seven 4-2 seams at once does not work at practical beam widths — the cross-seam idea has to be applied one seam at a time, seeded from the known chain.** P2.3c-11 ran the whole 4-2 main area as a single bucketed-beam pass (`bfscx W42Main … 6584 0 575 --enemies 0 --beam 50 --beam-buckets off,y,spd,sub --beam-max 300000`, no seams, 340.9 s, ~280k states/layer over ~12k buckets, per-bucket width auto-shrunk 50 → 24). **No goal within 575 steps**; the frontier reached max x **0x53bf0 = 1339.9** at layer 570 — **8 px short of the pipe at 1348** — and died at 571 when the deadline ran out. So the single-pass beam is ≥ 22 frames worse than the seven-segment chained 553 (F118). Per the pre-registered reading (declared in STATUS before the run, runbook §7.2) this is the **uninformative** branch: a beam that finds nothing proves nothing, so it says nothing about whether the seams leak. What it *does* establish is methodological: the seven seams encode enough hard-won structure (the vine bump, plant timing, the coupled pit-arc passage F115) that a naive whole-level beam at 3×10⁵/layer cannot rediscover it, and widening to reach 10⁷/layer over 575 layers is ~165 GB of layer disk — not the cheap validation it looked like. **Corrected design (P2.3c-11a):** dissolve **one** seam at a time — root on the known chain two segments back, deadline = the two segments' known combined cost, bucketed beam. The horizon is then tens of layers instead of 575, so the beam can be wide enough to be well-powered, and the answer is informative in both directions (under the known cost ⇒ that seam was leaking). | V (search record) | `runs/P2.3c-11/{wholelevel.log,wholelevel_launch.sh}`; compare F118 (553 across seven seams) |
| F133 | **8-4 room 3 reduces to one question, and the WR's own return is one frame off the bound's floor.** Measured session 16 (P2.2a′). **(a) The room's structure.** The wrong warp needs `screen_left_abs >= 3345`; the census of the approach's step-162 layer shows **max scroll offset 112 — no mint in room 3** — so `SL >= 3345` forces `x >= 3457`, which is the case's own threshold (`overshoot bound: threshold x 0xd8100`). Room total = (step at which x first reaches 3457) + (return to the pipe). **(b) The return floor is 33 over the WHOLE space, not just the WR's line.** `build_overshoot_bound` reports **30,720 end classes** (moving right ≥ 1 px/frame at the threshold) with **7 distinct return costs [33, 34, 35, 36, 37, 38, 39]** — minimum **33**. This generalises F125, which only showed the return optimal *from the WR's apex*. **(c) The WR crosses the threshold at step 161 and finishes at 195 — a 34-frame return.** Control: `bfscx W84Room3 … 16354 0 162 --goal-x 3457 --check-path 162` → `reference path: step 161 … GOAL`, 0 bound violations, root bound 148. So the WR's arrival class pays **34** where the bound's cheapest class pays **33**. **That 1-frame gap is exactly H25's claim**, and it is now a precisely posed question: *is a 33-cost end class reachable at step ≤ 161 in this room?* **(d) First answer — negative, but not proof.** A diversity-beam approach (`--beam 200 --beam-buckets off,y,spd,sub`, ~300k/layer) stopped at step 162 kept **2,303** distinct apex states; resuming **exhaustively** from all of them to deadline 194 is **dry — the frontier goes extinct at layer 188** (11.6 s), max x 0xd5e70. So no room ≤ 194 exists from any beam-surviving apex. Layers 1–162 were beamed, so this is a beam-level negative, not a refutation (runbook §7.2). Partial soundness argument: the beam ranks by h *within* each physical bucket and the return cost is a function of the bucketed variables (x-class = speed × subpixel, plus y), so the beam should retain the h-minimal representative of each class; the residual risk is bucket **granularity** (y 16 px bands, speed 4 px bands, 3 subpixel bits). | V (search records + bound table + control) | `runs/P2.2a-prime/{approach.log,return.log,approach_launch.sh,return_launch.sh}`; `smb-opt offset-census W84Room3 …/layer_162.bin`; refines F125 |
